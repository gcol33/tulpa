% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace_beta.R
\name{tulpa_laplace_beta}
\alias{tulpa_laplace_beta}
\title{Fit a beta-regression model via Laplace, estimating the precision}
\usage{
tulpa_laplace_beta(
  y,
  X,
  re_list = list(),
  spatial = NULL,
  weights = NULL,
  offset = NULL,
  max_iter = 100L,
  tol = 1e-06,
  n_threads = 1L,
  beta_prior = NULL,
  phi_init = NULL,
  phi_bounds = c(0.1, 10000),
  outer_tol = 1e-04,
  mode = c("laplace", "nuts"),
  control = list()
)
}
\arguments{
\item{y}{Response in \verb{(0, 1)}.}

\item{X}{Fixed-effects design matrix.}

\item{re_list, spatial, weights, offset, max_iter, tol, n_threads, beta_prior}{Passed to \code{\link[=tulpa_laplace]{tulpa_laplace()}} verbatim. \code{beta_prior} places a Gaussian
penalty on the fixed effects (a list with \code{sd}, optional \code{mean}; see
\code{\link[=tulpa_laplace]{tulpa_laplace()}}). It is included in the Laplace log-marginal that the
precision \code{phi} is optimised against, so the penalised model is fit
consistently across the outer \code{phi} search. Not supported with \code{spatial}
(the spatial solver carries its own prior).}

\item{phi_init}{Optional starting value for the precision. If \code{NULL},
a method-of-moments warm start is used.}

\item{phi_bounds}{Numeric length-2 vector with lower/upper bounds on
\code{phi} for the outer optimisation. Default \code{c(0.1, 1e4)}.}

\item{outer_tol}{Tolerance for the outer optimisation. Default 1e-4.}

\item{mode}{Inference method (the method is an argument, not a parallel
verb): \code{"laplace"} (default) is the Laplace + Brent-over-\code{phi} point fit
documented here; \code{"nuts"} delegates to \code{\link[=tulpa_nuts_beta]{tulpa_nuts_beta()}}, which samples
\code{phi} jointly with the coefficients via NUTS. In \code{"nuts"} mode the
Laplace-only arguments (\code{re_list}, \code{spatial}, \code{weights}, \code{offset},
\code{phi_init}, \code{phi_bounds}, \code{outer_tol}) are not used, and NUTS knobs are
passed via \code{control} (see \code{\link[=tulpa_nuts_beta]{tulpa_nuts_beta()}}).}

\item{control}{Passed to \code{\link[=tulpa_nuts_beta]{tulpa_nuts_beta()}} when \code{mode = "nuts"} (ignored
for \code{mode = "laplace"}).}
}
\value{
For \code{mode = "laplace"}, the list returned by \code{\link[=tulpa_laplace]{tulpa_laplace()}} at the
optimum, augmented with \code{phi} (the optimised precision) and
\code{phi_log_marginal} (the optimisation trace). For \code{mode = "nuts"}, the draws
object returned by \code{\link[=tulpa_nuts_beta]{tulpa_nuts_beta()}}.
}
\description{
Thin wrapper around \code{\link[=tulpa_laplace]{tulpa_laplace()}} for \code{family = "beta"}. The
Laplace engine treats \code{phi} as fixed per fit (same contract as gamma
and neg_binomial_2); this wrapper does an outer 1-D optimisation of
the Laplace-approximated log-marginal over \code{phi}, then refits at the
optimum to return betas and Hessian.

The mean-precision parameterisation is \code{y ~ Beta(mu * phi, (1 - mu) * phi)}
with default logit link; \code{y} must be strictly in \verb{(0, 1)}.
}
\examples{
set.seed(1)
n <- 200L
X <- cbind(1, rnorm(n))
mu <- plogis(X \%*\% c(0.2, 0.7)); phi <- 8
y <- rbeta(n, mu * phi, (1 - mu) * phi)
fit <- tulpa_laplace_beta(y, X)
fit$mode
}
