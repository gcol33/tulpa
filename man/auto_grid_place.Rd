% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_laplace_auto_grid.R
\name{auto_grid_place}
\alias{auto_grid_place}
\title{May the placement pass move a marked outer-grid axis?}
\usage{
auto_grid_place(x)
}
\arguments{
\item{x}{Any object.}
}
\value{
\code{FALSE} when \code{x} was marked \code{auto_grid(place = FALSE)}, \code{TRUE}
otherwise -- including for a value carrying no mark at all, which the
engine holds because it reads as a pin rather than because it asked to be
held.
}
\description{
Reads back what \code{\link[=auto_grid]{auto_grid()}}'s \code{place} argument recorded, so a wrapper that
rebuilds a value (\code{as.numeric()} drops every attribute) can re-apply both
halves of the declaration rather than only the provenance half.
}
\examples{
auto_grid_place(auto_grid(c(0.5, 1, 2)))
auto_grid_place(auto_grid(c(0.5, 1, 2), place = FALSE))
}
\seealso{
\code{\link[=auto_grid]{auto_grid()}}, \code{\link[=is_auto_grid]{is_auto_grid()}}
}
