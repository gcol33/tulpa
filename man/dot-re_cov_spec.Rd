% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{.re_cov_spec}
\alias{.re_cov_spec}
\title{Interpret an RE term's covariance specification}
\usage{
.re_cov_spec(r)
}
\arguments{
\item{r}{One element of a \code{re_list} (see \code{\link[=tulpa_laplace]{tulpa_laplace()}}).}
}
\value{
\code{list(pack, Q, diagonal)}.
}
\description{
Maps one \code{re_list} element to the two representations the Laplace path needs:
\code{pack}, the value the C++ kernel consumes (a length-\code{n_coefs} marginal-SD
vector for a diagonal / uncorrelated term, or a packed lower-triangular
Cholesky of length \code{n_coefs (n_coefs + 1) / 2} for a correlated one), and
\code{Q}, the \verb{n_coefs x n_coefs} RE precision \code{Sigma^{-1}} used to build the
marginal fixed-effect SE. A correlated term is signalled by \code{r$L} (a lower-
triangular Cholesky factor, \verb{Sigma = L L'}) or \code{r$cov} (the covariance
matrix); when present these take precedence over \code{r$sigma}.
}
\keyword{internal}
