% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{laplace_gp_at}
\alias{laplace_gp_at}
\title{NNGP Laplace at given hyperparameters}
\usage{
laplace_gp_at(
  y,
  n_trials,
  X,
  spatial,
  family = "binomial",
  phi = 1,
  sigma2_gp = NULL,
  phi_gp = NULL,
  re_idx = NULL,
  n_re_groups = 0L,
  sigma_re = 1,
  max_iter = 100L,
  tol = 1e-06,
  n_threads = 1L,
  offset = NULL,
  weights = NULL
)
}
\arguments{
\item{y}{Response vector.}

\item{n_trials}{Trial sizes (binomial).}

\item{X}{Fixed-effects design matrix.}

\item{spatial}{A \code{tulpa_gp} spec, validated (i.e., \code{neighbor_info} populated).}

\item{family}{Distribution family.}

\item{phi}{Dispersion in the compiled-kernel convention: for \code{gaussian} /
\code{lognormal} the residual SD (variance is \code{phi^2}), for \code{neg_binomial_2}
the size, \code{gamma} the shape, \code{beta} the precision, \code{t} the scale;
\code{binomial} / \code{poisson} ignore it. Callers hand this the already-converted
value (\code{\link[=tulpa_laplace]{tulpa_laplace()}} passes \code{sqrt(phi)} for the variance families).}

\item{sigma2_gp}{Marginal variance (NULL -> 1.0).}

\item{phi_gp}{Range / decay parameter (NULL -> 1.0).}

\item{max_iter}{Newton iterations.}

\item{tol}{Newton tolerance.}

\item{n_threads}{OpenMP threads.}

\item{weights}{Optional per-observation likelihood weights (length
\code{length(y)}), scaling each row's log-density, score and Fisher curvature.}
}
\value{
The raw \code{cpp_laplace_fit_gp} result list, augmented with
\code{sigma2_gp}, \code{phi_gp}, and the spatial spec.
}
\description{
Single-point Laplace approximation for a Matern/exponential GP spatial
field at fixed (sigma2_gp, phi_gp). Used by \code{dispatch_laplace_spatial}
when \code{spatial$type == "gp"}. The neighbor structure is read straight
off the validated spec -- call \code{validate_gp(spatial, data)} first if
constructing manually.
}
\keyword{internal}
