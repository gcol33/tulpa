% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priorsense.R
\name{tulpa_powerscale_sensitivity}
\alias{tulpa_powerscale_sensitivity}
\title{Power-scaling prior / likelihood sensitivity}
\usage{
tulpa_powerscale_sensitivity(
  fit,
  data,
  prior = NULL,
  lower_alpha = 0.99,
  upper_alpha = 1.01,
  threshold = 0.05
)
}
\arguments{
\item{fit}{A \code{tulpa_fit} fitted through \code{\link[=tulpa]{tulpa()}} (fixed-effect / GLMM;
spatial / temporal-field fits are rejected).}

\item{data}{The data frame the model was fit to.}

\item{prior}{The Gaussian fixed-effect prior the fit used, as
\code{list(mean =, sd =)} (scalars recycled). Required for the prior component;
omit to compute the likelihood component only.}

\item{lower_alpha, upper_alpha}{Power-scaling grid endpoints for the gradient
(defaults 0.99 / 1.01, as in priorsense).}

\item{threshold}{Sensitivity flag threshold (default 0.05).}
}
\value{
A data frame with one row per fixed-effect parameter and columns
\code{variable}, \code{prior}, \code{likelihood}, \code{diagnosis}.
}
\description{
Local power-scaling sensitivity (Kallioinen et al. 2024): how much each
fixed-effect posterior moves when the prior or the likelihood is raised to a
power \code{alpha} near 1. The existing draws are importance-reweighted by
\code{exp((alpha - 1) * log_component)} (PSIS-smoothed via \code{\link[=tulpa_psis]{tulpa_psis()}}; no
refits), and the sensitivity is the gradient of the cumulative
Jensen-Shannon distance between the base and power-scaled posteriors with
respect to \code{log2(alpha)}.

Values above \code{threshold} flag sensitivity. High on \strong{both} components
indicates potential prior-data conflict; high prior with low likelihood
indicates a strong prior / weak likelihood.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(x = rnorm(150))
d$y <- rpois(150, exp(0.5 + 0.7 * d$x))
fit <- tulpa(y ~ x, data = d, family = "poisson", mode = "laplace",
             beta_prior = list(mean = 0, sd = 5))
tulpa_powerscale_sensitivity(fit, data = d, prior = list(mean = 0, sd = 5))
}
}
\references{
Kallioinen, Paananen, Buerkner & Vehtari (2024). Detecting and
diagnosing prior and likelihood sensitivity with power-scaling.
\emph{Statistics and Computing} 34:57. Nguyen & Vreeken (2015).
Non-parametric Jensen-Shannon divergence. ECML PKDD.
}
\seealso{
\code{\link[=tulpa_psis]{tulpa_psis()}}, \code{\link[=tulpa_criteria]{tulpa_criteria()}}.
}
