% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kfold.R
\name{tulpa_kfold}
\alias{tulpa_kfold}
\title{K-fold cross-validation for a tulpa fit}
\usage{
tulpa_kfold(object, data, K = 10L, folds = NULL, n_trials = NULL, seed = NULL)
}
\arguments{
\item{object}{A \code{tulpa_fit} fitted through \code{\link[=tulpa]{tulpa()}} (must carry \verb{$call}).}

\item{data}{The data frame the model was fit to.}

\item{K}{Number of folds (default 10).}

\item{folds}{Optional integer vector of fold ids, length \code{nrow(data)}; a
random balanced partition is drawn when \code{NULL}.}

\item{n_trials}{Optional binomial denominators (length \code{nrow(data)});
defaults to the trials stored on the fit, else 1 (Bernoulli). Each fold's
refit receives the training rows' trials, and the held-out density is
scored at the test rows' trials.}

\item{seed}{Optional seed for the random partition.}
}
\value{
A list with \code{elpd_kfold} (summed held-out elpd), \code{se_elpd_kfold} (its
standard error), \code{pointwise} (per-observation held-out elpd), \code{folds}, and
\code{K}.
}
\description{
Splits the data into \code{K} folds, refits the model on each \code{K - 1} training
partition (via the fit's stored \code{tulpa()} call), and accumulates the held-out
fold's pointwise log predictive density
\eqn{\log \frac{1}{S}\sum_s p(y_i \mid \eta_i^{(s)})} over the training
posterior draws. The summed \code{elpd_kfold} is directly comparable to the
\code{elpd_loo} from \code{\link[=tulpa_criteria]{tulpa_criteria()}} -- the exact refit counterpart to the
PSIS-LOO approximation, for when the Pareto k-hat gate flags LOO as
unreliable.

Fixed-effect / GLMM fits only: subsetting the observations breaks a spatial or
temporal field, so those fits are rejected (use PSIS-LOO via
\code{\link[=tulpa_criteria]{tulpa_criteria()}}). Held-out random-effect groups contribute at their prior
mean (population-level held-out prediction), matching \code{\link[=predict]{predict()}}.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(x = rnorm(120))
d$y <- rpois(120, exp(0.4 + 0.6 * d$x))
fit <- tulpa(y ~ x, data = d, family = "poisson", mode = "laplace")
cv  <- tulpa_kfold(fit, data = d, K = 5, seed = 1)
cv$elpd_kfold
}
}
\seealso{
\code{\link[=tulpa_criteria]{tulpa_criteria()}} for PSIS-LOO / WAIC on a single fit.
}
