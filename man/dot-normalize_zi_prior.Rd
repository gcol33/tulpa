% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{.normalize_zi_prior}
\alias{.normalize_zi_prior}
\title{Normalize a zero-inflation prior to a scalar SD}
\usage{
.normalize_zi_prior(zi_prior)
}
\arguments{
\item{zi_prior}{\code{NULL}, or a list with a scalar \code{sd}.}
}
\value{
A scalar prior SD; the engine default when \code{zi_prior} is \code{NULL}.
}
\description{
The compiled zero-inflated kernels carry one mean-zero Gaussian prior over
the whole \code{beta_zi} block (\code{ModelData::zi_prior_sd} on the sampler paths,
the appended \code{BetaPrior} tail on the Laplace path), so the prior is a single
SD rather than the per-coefficient \code{list(mean, sd)} that \code{beta_prior} takes.
The list form is kept so the two priors read alike at the front door and so a
per-coefficient or non-zero-mean ZI prior can be added without a signature
change.
}
\keyword{internal}
