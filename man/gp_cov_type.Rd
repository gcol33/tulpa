% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{gp_cov_type}
\alias{gp_cov_type}
\title{Map a spatial_gp covariance spec to the engine's cov_type code}
\usage{
gp_cov_type(spatial)
}
\description{
One code per kernel, read identically by every path that forms a covariance
from it -- the Laplace NNGP scatter, the Polya-Gamma sweep, the exact-NUTS
NNGP/SVC kernels and the field predictor (\code{tulpa::CovType} in
\code{inst/include/tulpa/types.h}, dispatched by \code{cov_value} in
\code{inst/include/tulpa/cov_kernel.h}): 0 = exponential, 1 = Matern(nu = 1.5),
4 = Matern(nu = 2.5). The smoothness travels IN the code, so no path has to
read a \code{nu} beside it to know which kernel it is evaluating. Anything else is
rejected here rather than silently fitted as a different covariance.
}
\keyword{internal}
