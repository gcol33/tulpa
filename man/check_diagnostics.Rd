% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diagnostics.R
\name{check_diagnostics}
\alias{check_diagnostics}
\title{Quick convergence check}
\usage{
check_diagnostics(
  fit,
  rhat_threshold = 1.01,
  ess_threshold = 400,
  quiet = FALSE
)
}
\arguments{
\item{fit}{A \code{tulpa_fit} object.}

\item{rhat_threshold}{Maximum acceptable Rhat (default 1.01).}

\item{ess_threshold}{Minimum acceptable bulk-ESS (default 400).}

\item{quiet}{Logical; if TRUE, suppress messages (default FALSE).}
}
\value{
Invisibly, \code{TRUE} if all checks pass, \code{FALSE} if any fail, or \code{NA}
for a non-chain (approximation) fit where Rhat/ESS do not apply.
}
\description{
Runs the core convergence diagnostics on a fit and reports whether Rhat,
bulk-ESS, and divergence thresholds are all met. A terse companion to
\code{\link[=diagnostic_summary]{diagnostic_summary()}} for use in scripts and tests.
}
\examples{
\donttest{
set.seed(123)
df <- data.frame(x = rnorm(60))
df$y <- rpois(60, exp(0.5 + 0.3 * df$x))
fit <- tulpa(y ~ x, data = df, family = "poisson", mode = "hmc",
             control = list(n_iter = 500L, warmup = 250L, n_chains = 2L,
                            seed = 1L))
check_diagnostics(fit)
}

}
\seealso{
\code{\link[=diagnostic_summary]{diagnostic_summary()}}, \code{\link[=diagnostics]{diagnostics()}}, \code{\link[=n_divergent]{n_divergent()}}
}
