% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_generic.R
\name{ranef}
\alias{ranef}
\alias{ranef.tulpa_fit}
\title{Random-effect summaries}
\usage{
ranef(object, ...)

\method{ranef}{tulpa_fit}(object, ...)
}
\arguments{
\item{object}{A \code{tulpa_fit} object.}

\item{...}{Ignored.}
}
\value{
Data frame with one row per random-effect coefficient: term, group,
level, estimate, and (sampler tier only) sd and credible bounds.
}
\description{
Random-effect summaries
}
\examples{
\donttest{
set.seed(1)
df <- data.frame(x = rnorm(100), g = factor(rep(1:10, 10)))
df$y <- rpois(100, exp(0.3 * df$x))
fit <- tulpa(y ~ x + (1 | g), data = df, family = "poisson")
ranef(fit)
}
}
