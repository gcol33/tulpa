% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tgmrf.R
\name{tgmrf}
\alias{tgmrf}
\title{User-defined GMRF latent block}
\usage{
tgmrf(
  Q,
  prior,
  init,
  mu = NULL,
  graph = NULL,
  bounds = NULL,
  obs_idx = NULL,
  name = NULL
)
}
\arguments{
\item{Q}{A function \verb{function(theta)} returning a sparse precision matrix
of class \code{Matrix::sparseMatrix} (typically \code{dgCMatrix}). The matrix must
be square and symmetric. Called once at registration with \code{theta = init}
to infer \code{n_latent} and capture the sparsity pattern.}

\item{prior}{A function \verb{function(theta)} returning a finite numeric
scalar -- the log-prior density at \code{theta}.}

\item{init}{Numeric vector of starting values for \code{theta}. Names, if
present, become the canonical theta names.}

\item{mu}{Optional function \verb{function(theta)} returning a numeric vector
of length \code{n_latent}. Default \code{NULL} is equivalent to a zero mean.}

\item{graph}{Optional sparse matrix specifying the upper bound on \code{Q}'s
sparsity pattern. If supplied, the registration check verifies that
the nonzero pattern of \code{Q(init)} is a subset.}

\item{bounds}{Optional list with components \code{lower} and \code{upper}, each a
numeric vector of length \code{length(init)}. Used by \code{nested_laplace + CCD}
to build the outer grid; unused by NUTS / VI.}

\item{obs_idx}{Optional integer vector mapping each observation to a
latent slot in \verb{[1, n_latent]}. If \code{NULL} (default), the fit-time
driver assumes \code{N == n_latent} and uses \code{seq_len(N)} -- i.e. one
observation per latent slot, in row order.}

\item{name}{Optional character; cosmetic label used by \code{print()} /
\code{summary()}.}
}
\value{
An object of class \code{c("tgmrf", "tulpa_latent_block")} with
components:
\itemize{
\item \code{Q}, \code{prior}, \code{mu} -- the user closures (or \code{NULL} for \code{mu}).
\item \code{init}, \code{theta_names}, \code{theta_dim} -- hyperparameter metadata.
\item \code{n_latent} -- inferred from \code{Q(init)}.
\item \code{pattern} -- captured sparsity pattern as a \code{dgCMatrix} of 1s.
\item \code{graph} -- user-supplied pattern, validated against \code{pattern} if given.
\item \code{bounds}, \code{name} -- passed through.
}
}
\description{
\code{tgmrf()} lets a user define a Gaussian Markov random field as a latent
block in a tulpa model by supplying two R closures: a precision-matrix
factory \code{Q(theta)} and a log-prior on theta \code{prior(theta)}. The resulting
object plugs into a formula via \code{latent(tgmrf(...))} and is consumed by
every tulpa inference tier (Laplace, EM+Laplace, VI, nested_laplace+CCD,
NUTS, IMH-Laplace).

This is the latent-side dual of \code{LikelihoodSpec}. \code{LikelihoodSpec} lets a
model package own the observation model; \code{tgmrf()} lets a script own a
single latent block.
}
\details{
For a Gaussian latent block \code{z ~ N(mu(theta), Q(theta)^{-1})}:

\deqn{\log p(z\mid\theta) = \tfrac{1}{2}\log\det Q(\theta) - \tfrac{1}{2}(z - \mu(\theta))^\top Q(\theta)(z - \mu(\theta)) + \mathrm{const}}

the gradient \code{-Q(z - mu)} and Hessian \code{-Q} wrt \code{z} are closed form, so the
user never writes gradient code. The Laplace inner step needs only
\code{Q(theta)} and \code{mu(theta)} at numeric \code{theta}. NUTS and VI additionally
use a forward finite-difference gradient on theta, costing \code{dim(theta)}
extra \code{Q} calls per outer step -- cheap for the typical \code{dim(theta) <= 5}.
}
\examples{
# Periodic AR(1) block, wrap-around tridiagonal precision.
periodic_ar1 <- function(n) {
  tgmrf(
    Q = function(theta) {
      sigma <- exp(theta[1]); rho <- tanh(theta[2])
      d <- rep((1 + rho^2) / sigma^2, n)
      o <- rep(-rho / sigma^2, n)
      M <- Matrix::bandSparse(
        n, k = c(-1, 0, 1), diagonals = list(o, d, o)
      )
      M[1, n] <- M[n, 1] <- -rho / sigma^2
      methods::as(M, "CsparseMatrix")
    },
    prior = function(theta) {
      stats::dnorm(theta[1], 0, 1, log = TRUE) +
        stats::dnorm(theta[2], 0, 1, log = TRUE)
    },
    init = c(log_sigma = 0, atanh_rho = 0),
    name = "periodic_ar1"
  )
}

blk <- periodic_ar1(20)
print(blk)

}
\seealso{
\code{\link[=latent]{latent()}} for the formula slot that consumes a \code{tgmrf} block.
}
