% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{laplace_hsgp_at}
\alias{laplace_hsgp_at}
\title{HSGP Laplace at given hyperparameters}
\usage{
laplace_hsgp_at(
  y,
  n_trials,
  X,
  spatial,
  family = "binomial",
  phi = 1,
  sigma2 = NULL,
  lengthscale = NULL,
  re_idx = NULL,
  n_re_groups = 0L,
  sigma_re = 1,
  max_iter = 100L,
  tol = 1e-06,
  n_threads = 1L,
  offset = NULL
)
}
\description{
Single-point Laplace for a Hilbert-space GP field at a fixed
\verb{(sigma2, lengthscale)}, the conditional counterpart of the nested HSGP
integrator. Reuses the shared \code{make_hsgp_block} factory + dense spec solver
(DENSE_BASIS scatter) via \code{cpp_laplace_fit_hsgp}, so the mode equals the
nested kernel at that one grid cell. \code{sigma2} / \code{lengthscale} default to the
spec's fields (or \code{1}) and are recorded on the result.
}
\keyword{internal}
