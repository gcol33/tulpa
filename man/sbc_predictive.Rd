% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sbc.R
\name{sbc_predictive}
\alias{sbc_predictive}
\alias{sbc_mixture}
\alias{sbc_normal}
\alias{sbc_discrete}
\alias{sbc_rank}
\alias{sbc_draws}
\title{Predictive shapes an SBC fitter reports}
\usage{
sbc_mixture(mu, var, w = NULL)

sbc_normal(mean, sd)

sbc_discrete(support, probs)

sbc_rank(rank, n_ref)

sbc_draws(x)
}
\arguments{
\item{mu, var, w}{Component means, variances and weights. \code{w} defaults to
equal weights and is normalized.}

\item{mean, sd}{Mean and standard deviation of a single Gaussian.}

\item{support, probs}{Finite support and its probabilities, normalized.}

\item{rank, n_ref}{The rank in \code{0:n_ref} of the truth among \code{n_ref} reference
values, and that reference count.}

\item{x}{Posterior draws.}
}
\value{
A list carrying a \code{kind} tag and that shape's parameters.
}
\description{
The tagged representations \code{\link[=sbc]{sbc()}} reads. A \code{fitter} (or a posterior-SBC
\code{arms}) callback returns a named list of ARMS, each a named list over
quantities, and each entry is one of these -- the shape the backend actually
reports for that quantity. Everything downstream (the PIT, the CRPS, drawing
from a predictive) dispatches on the \code{kind} tag, so a new backend shape is
one entry in three switches rather than a parallel scorer.
}
\details{
These are the extension point, not alternative front doors: \code{\link[=sbc]{sbc()}} is the
verb, and these are the argument type it consumes.

\code{sbc_mixture()} is what an outer hyperparameter grid defines for a fixed
effect -- component \code{k} is \code{N(mu_k, var_k)} with weight \code{w_k}, which is
exactly the mixture a nested-Laplace fit reports. \code{sbc_normal()} is the
one-component case, with its own constructor so a collapsed-moment read says
what it is. \code{sbc_discrete()} is a distribution on a finite support, which is
what a discrete hyperparameter grid defines for its own axis. \code{sbc_rank()} is
a rank of the truth among \code{n_ref} reference values, which is what a joint
log-likelihood comparison against posterior draws produces -- it needs no
entry in the simulator's \code{theta}, since the comparison against the truth
already happened when the rank was formed. \code{sbc_draws()} is for a backend
reporting no analytic marginal.

The last three have ATOMS, so their PIT is randomized within the atom by
\code{\link[=sbc]{sbc()}}; reading a rank against a continuous uniform is the classic silent
SBC bug.
}
\examples{
sbc_normal(0.3, 0.1)
sbc_mixture(mu = c(0, 1), var = c(1, 4), w = c(0.7, 0.3))
sbc_discrete(support = c(0.5, 1, 2), probs = c(0.2, 0.5, 0.3))
}
\seealso{
\code{\link[=sbc]{sbc()}}
}
