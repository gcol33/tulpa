% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/glmm_logpost.R
\name{build_glmm_logpost}
\alias{build_glmm_logpost}
\title{Build a GLMM log-posterior (and gradient) from a model-data bundle.}
\usage{
build_glmm_logpost(
  bundle,
  family,
  sigma_re = NULL,
  n_trials = NULL,
  phi = 1,
  beta_prior = .tulpa_default_beta_prior("glmm_logpost"),
  weights = NULL,
  phi2 = NULL
)
}
\arguments{
\item{bundle}{Output of \code{\link[=tulpa_build_model_data]{tulpa_build_model_data()}} (needs \code{y}, \code{X},
\code{offset}, \code{re_terms}, \code{n_obs}, \code{n_fixed}).}

\item{family}{Character family name (see \code{\link[=family_names]{family_names()}}).}

\item{sigma_re}{Numeric vector of random-effect SDs, one per RE term. Length
must equal \code{length(bundle$re_terms)}. Ignored when there are no RE terms.}

\item{n_trials}{Binomial denominators (or \code{NULL}).}

\item{phi}{Dispersion passed to the family (held fixed), in the R family
registry convention: for \code{gaussian} / \code{lognormal} this is the residual
VARIANCE (matching \code{\link[=tulpa]{tulpa()}} and \code{\link[=tulpa_laplace]{tulpa_laplace()}}), for
\code{neg_binomial_2} the size, \code{gamma} the shape, \code{beta} the precision, \code{t}
the scale; \code{binomial} / \code{poisson} ignore it. The compiled-kernel doors
(\code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}}, \code{\link[=fit_spde]{fit_spde()}}) take the residual SD instead.}

\item{beta_prior}{\code{list(mean, sd)} Gaussian prior on the fixed effects
(scalars, recycled). Defaults to the engine default, \code{prior_normal(0, 2.5)}.}

\item{weights}{Optional per-observation likelihood weights (length \code{n_obs}):
each row's log-likelihood and score contribution is scaled by its weight.}

\item{phi2}{Optional second dispersion (Student-t degrees of freedom).}
}
\value{
A list with:
\itemize{
\item \code{log_posterior(theta)} -- scalar log-posterior (up to a constant).
\item \code{grad_log_posterior(theta)} -- gradient vector.
\item \code{dim} -- length of \code{theta}.
\item \code{init} -- a zero starting vector of length \code{dim}.
\item \code{unpack(theta)} -- list(\code{beta}, \code{u} = list of n_groups x n_coefs matrices).
\item \code{param_names} -- character labels aligned with \code{theta}.
}
}
\description{
Build a GLMM log-posterior (and gradient) from a model-data bundle.
}
\keyword{internal}
