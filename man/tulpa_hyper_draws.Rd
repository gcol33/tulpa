% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_draws_hyper.R
\name{tulpa_hyper_draws}
\alias{tulpa_hyper_draws}
\title{Hyperparameter draws from a nested-Laplace fit}
\usage{
tulpa_hyper_draws(fit, cells = NULL, n = 1000, within = NULL)
}
\arguments{
\item{fit}{A nested-Laplace fit (\code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}} or
\code{\link[=tulpa_nested_laplace_joint]{tulpa_nested_laplace_joint()}}).}

\item{cells}{Integer vector of outer-grid cell indices, one per draw -- the
\code{"cells"} attribute of a \code{\link[=tulpa_posterior_draws]{tulpa_posterior_draws()}} matrix, so the
hyperparameter row and the latent row of a draw come from the same cell.
\code{NULL} (default) allocates \code{n} fresh draws across the cells by weight.}

\item{n}{Number of draws when \code{cells} is \code{NULL} (default 1000); ignored
otherwise.}

\item{within}{Within-cell construction, \code{"box_uniform"} or \code{"chord"}.
\code{NULL} (default) takes the one the fit was read with
(\code{fit$within_cell_requested}).}
}
\value{
A numeric matrix \verb{[n x n_axes]} with the outer grid's axis names as
columns, or \code{NULL} when the fit carries no outer-grid axis. Carries
\code{attr(., "cells")} -- the cell each row's coordinates were drawn in --
plus \code{attr(., "within_cell")} and \code{attr(., "within_cell_declined")}, the
per-axis construction that ran and why a requested one did not, and
\code{attr(., "within_cell_copula")}, the Gaussian-copula correlation matrix
that ties the axes' within-cell draws together so the draws keep the
grid's own correlation between axes (the identity where the grid's axes
are uncorrelated). Each axis's marginal is the same whatever the copula.
}
\description{
The hyperparameter half of a draw from the outer-grid mixture: one row per
draw, one column per outer-grid axis. A draw's coordinate on each axis is
sampled from the within-cell density its cell carries under the fit's own
within-cell read, so the columns are a continuous marginal rather than the
handful of grid-node values \code{fit$theta_grid[cells, ]} returns.

An axis carrying a declared POINT MASS -- the copy scale's \code{alpha = 0}, the
"no coupling" model whose prior probability is stated rather than read off a
node count -- keeps it: draws in that cell are exactly the level, in the
proportion the fit reports as \code{fit$copy_atom$posterior_mass}, and the
continuum above it is continuized on its own partition. The level is a model
and not a cell representative, so spreading it over a box would both put mass
where the axis has none and leave none on the level itself.

Reading the node coordinate directly is what
\code{\link[=tulpa_posterior_draws]{tulpa_posterior_draws()}} consumers used to do, and on a 5- to 15-node axis
it makes every draw an atom: a truth between two nodes, or past the
outermost one, has no draw that can land near it. The continuization is the
cell-conditional of the SAME construction the fit reports its interval from
(\code{box_uniform} by default, \code{chord} when the fit asked for it or its support
does not admit the box read), so the draws reproduce the fit's own
\code{theta_ci_lo} / \code{theta_median} / \code{theta_ci_hi} to Monte Carlo error.
}
\examples{
\donttest{
set.seed(1)
S <- 30L                                   # spatial units in a chain
nb <- lapply(seq_len(S), function(s) setdiff(c(s - 1L, s + 1L), c(0L, S + 1L)))
nn <- lengths(nb)
field <- as.numeric(scale(cumsum(rnorm(S, 0, 0.4))))
idx <- rep(seq_len(S), each = 6L); n <- length(idx); x <- rnorm(n)
y <- rbinom(n, 1L, plogis(-0.2 + 0.6 * x + field[idx]))
prior <- list(type = "icar", n_spatial_units = S, spatial_idx = idx,
              adj_row_ptr = c(0L, cumsum(nn)), adj_col_idx = unlist(nb) - 1L,
              n_neighbors = nn, tau_grid = c(0.5, 1, 2, 4, 8))
fit <- tulpa_nested_laplace(y, rep(1L, n), cbind(1, x), prior = prior,
                            family = "binomial",
                            control = list(progress = FALSE))
th <- tulpa_hyper_draws(fit, n = 200)
quantile(th[, "tau"], c(0.025, 0.5, 0.975))
}
}
\seealso{
\code{\link[=tulpa_posterior_draws]{tulpa_posterior_draws()}}, \code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}}
}
