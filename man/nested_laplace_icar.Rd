% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_laplace.R
\name{nested_laplace_icar}
\alias{nested_laplace_icar}
\title{Nested Laplace Approximation for ICAR Spatial Models}
\usage{
nested_laplace_icar(
  y,
  n_trials,
  X,
  spatial_idx,
  n_spatial_units,
  adj_row_ptr,
  adj_col_idx,
  n_neighbors,
  family = "binomial",
  phi = 1,
  tau_mode = NULL,
  n_grid = 9L,
  grid_width = 4,
  max_iter = 50L,
  tol = 1e-06,
  n_threads = 1L,
  verbose = FALSE
)
}
\arguments{
\item{y}{Integer response vector}

\item{n_trials}{Integer vector of trial sizes (binomial)}

\item{X}{Design matrix}

\item{spatial_idx}{Integer vector of site assignments (1-based)}

\item{n_spatial_units}{Number of spatial units}

\item{adj_row_ptr}{CSR row pointers for adjacency (0-based)}

\item{adj_col_idx}{CSR column indices (0-based)}

\item{n_neighbors}{Integer vector of neighbor counts}

\item{family}{Distribution family ("binomial", "poisson", "neg_binomial_2")}

\item{phi}{Dispersion parameter (negbin only)}

\item{tau_mode}{Initial mode for tau_spatial (positive)}

\item{n_grid}{Number of grid points (odd; default 9)}

\item{grid_width}{Width of grid in log-tau scale (default 4 SD)}

\item{max_iter}{Max Newton iterations per grid point}

\item{tol}{Newton convergence tolerance}

\item{n_threads}{OpenMP threads}

\item{verbose}{Print progress}
}
\value{
List with:
\itemize{
\item tau_grid: grid of tau values
\item log_tau_grid: grid on log scale
\item log_marginal: log p(y | tau) at each grid point
\item weights: normalized integration weights
\item n_iter: Newton iterations at each grid point
\item tau_mean: posterior mean of tau
\item tau_sd: posterior SD of tau
\item mode_at_tau_mode: mode vector at the modal tau
}
}
\description{
Phase 3a proof of concept: R-level outer loop over a 1D grid of
tau_spatial values, with inner Laplace at each grid point via
cpp_laplace_fit_spatial. Uses warm-starting (hot start) from the
previous grid point's mode for 5-10x fewer Newton iterations.
}
\keyword{internal}
