% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_car.R
\name{spatial_car_proper}
\alias{spatial_car_proper}
\title{Proper CAR spatial structure}
\usage{
spatial_car_proper(
  adjacency,
  level = c("group", "obs"),
  group_var = NULL,
  shared = NULL
)
}
\arguments{
\item{adjacency}{Symmetric adjacency matrix (\verb{[n_units x n_units]}).}

\item{level}{Either \code{"group"} (one effect per level of \code{group_var}) or
\code{"obs"} (one effect per row of the data; \code{nrow(data)} must equal
\code{nrow(adjacency)}).}

\item{group_var}{Name of the grouping variable in the data; required
when \code{level = "group"}.}

\item{shared}{Optional shared-effect handle (see model docs).}
}
\value{
A \code{tulpa_spatial} object with \code{type = "car_proper"}.
}
\description{
Convenience wrapper for \code{spatial_car(..., proper = TRUE)}. Creates a
proper conditional autoregressive (CAR) spatial random effect with the
autocorrelation parameter ρ estimated from the data.

Use this when you want spatial autocorrelation to be a parameter of the
model rather than fixed at 1 (as in ICAR). ρ ≈ 0 collapses to IID,
ρ ≈ 1 approaches ICAR.
}
\examples{
adj <- matrix(0, 10, 10)
for (i in 1:9) adj[i, i+1] <- adj[i+1, i] <- 1
spec <- spatial_car_proper(adj, level = "group", group_var = "site")
print(spec)

}
\seealso{
\code{\link[=spatial_car]{spatial_car()}} for ICAR (ρ fixed at 1),
\code{\link[=spatial_bym2]{spatial_bym2()}} for the BYM2 decomposition.
}
