% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{spatial_spde}
\alias{spatial_spde}
\title{SPDE Spatial Field (Matérn via Triangular Mesh)}
\usage{
spatial_spde(
  coords,
  data = NULL,
  mesh = NULL,
  boundary = NULL,
  max_edge = NULL,
  cutoff = 0,
  nu = 1,
  prior_range = c(0.5, 0.5),
  prior_sigma = c(1, 0.5)
)
}
\arguments{
\item{coords}{A formula \code{~ x + y} or a two-column matrix of coordinates.}

\item{data}{Optional data.frame for formula evaluation.}

\item{mesh}{A pre-built \code{tulpa_mesh} object. If NULL (default), a mesh
is built automatically from \code{coords}.}

\item{boundary}{Optional boundary: a two-column matrix of polygon vertices,
an sf polygon, or NULL for convex hull with extension.}

\item{max_edge}{Maximum edge length for mesh refinement. A single value
or \code{c(inner, outer)}.}

\item{cutoff}{Minimum distance between mesh vertices. Default 0.}

\item{nu}{Matérn smoothness parameter. Default 1 (exponential covariance).
Currently only integer values supported; fractional smoothness via
rational SPDE is planned.}

\item{prior_range}{Prior for the spatial range. A numeric vector \code{c(U, alpha)}
where P(range < U) = alpha. Default \code{c(0.5, 0.5)}.}

\item{prior_sigma}{Prior for the marginal standard deviation. A numeric vector
\code{c(U, alpha)} where P(sigma > U) = alpha. Default \code{c(1, 0.5)}.}
}
\value{
A \code{tulpa_spatial} object with type \code{"spde"}.
}
\description{
Specify a continuous Matérn spatial field using the SPDE approach
(Lindgren, Rue & Lindström 2011). Builds a triangular mesh from
observation coordinates, computes FEM matrices, and passes them
to tulpa's SPDE Laplace engine with CHOLMOD sparse solver.
}
\examples{
set.seed(42)
coords <- cbind(runif(50), runif(50))
spec <- spatial_spde(coords)
print(spec)
}
