% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tgmrf_cpp.R
\name{tgmrf_cpp}
\alias{tgmrf_cpp}
\title{User-defined GMRF latent block, compiled C++ backend}
\usage{
tgmrf_cpp(
  cpp_file,
  id,
  init,
  mu = NULL,
  graph = NULL,
  bounds = NULL,
  obs_idx = NULL,
  name = NULL,
  cache_dir = tulpa_cache_dir(),
  rebuild = FALSE
)
}
\arguments{
\item{cpp_file}{Absolute path to the user's \code{.cpp} file. The file is
compiled via \code{\link[Rcpp:sourceCpp]{Rcpp::sourceCpp()}} with caching keyed on
\code{digest::sha256(file_contents) + TULPA_ABI_VERSION} so repeated calls
with an unchanged source skip the rebuild.}

\item{id}{Character: the stable id used as the registry key. Must match
the first argument of \code{TULPA_REGISTER_TGMRF} in the user's \code{.cpp}.}

\item{init, mu, graph, bounds, obs_idx, name}{Same arguments as \code{\link[=tgmrf]{tgmrf()}}.
\code{init} is required and supplies the canonical theta names. \code{mu} is
currently ignored on the C++ path -- the mu kernel registered by
\code{TULPA_REGISTER_TGMRF} is the source of truth; pass the argument here
only to keep call sites symmetric with \code{tgmrf()}.}

\item{cache_dir}{Directory for Rcpp's \code{sourceCpp} cache. Defaults to a
per-user location under \code{tools::R_user_dir("tulpa", "cache")}.}

\item{rebuild}{Force a recompile even if the cached DLL is up to date.
Default \code{FALSE}.}
}
\value{
An object of class \code{c("tgmrf", "tulpa_latent_block")} with the
same fields as \code{\link[=tgmrf]{tgmrf()}} plus \code{backend = "cpp"} and \code{cpp_id = id}. The
\code{Q} / \code{prior} fields are R wrapper closures around the registered C++
kernels, so callers (e.g. NUTS-over-theta) that hold the object can
still call \code{block$Q(theta)} directly.
}
\description{
\code{tgmrf_cpp()} is the compiled-C++ analogue of \code{\link[=tgmrf]{tgmrf()}}. Where \code{tgmrf()}
takes R closures \code{Q(theta)} / \code{prior(theta)}, \code{tgmrf_cpp()} takes a
user-written \code{.cpp} file that defines the same kernels as templated C++
(so the same source compiles against every AD type tulpa uses). The
returned object has the same S3 class as \code{tgmrf()} -- \code{c("tgmrf", "tulpa_latent_block")} -- so every downstream consumer (formula parser,
inference layers, S3 methods) treats the two paths identically. Only the
dispatch on \code{Q(theta)} differs: registry-stored function pointer
(\code{tgmrf_cpp()}) vs Rcpp::Function callback (\code{tgmrf()}).

The user \code{.cpp} file must include \verb{<tulpa/tgmrf.h>} and call the
\code{TULPA_REGISTER_TGMRF(id, Q_fn, mu_fn, log_prior_fn)} macro once. See
\code{inst/examples/tgmrf_periodic_ar1.cpp} for a worked example.
}
\seealso{
\code{\link[=tgmrf]{tgmrf()}} for the R-closure path; \code{\link[=latent]{latent()}} for the formula
slot that consumes a \code{tgmrf} block.
}
