% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{laplace_spde_at}
\alias{laplace_spde_at}
\title{SPDE Laplace at given hyperparameters}
\usage{
laplace_spde_at(
  y,
  n_trials,
  X,
  spatial,
  family = "binomial",
  phi = 1,
  range = NULL,
  sigma = NULL,
  re_idx = NULL,
  n_re_groups = 0L,
  sigma_re = 1,
  max_iter = 100L,
  tol = 1e-06,
  n_threads = 1L,
  offset = NULL,
  weights = NULL
)
}
\arguments{
\item{y}{Response vector.}

\item{n_trials}{Trial sizes (binomial).}

\item{X}{Fixed-effects design matrix.}

\item{spatial}{A \code{tulpa_spatial} object of type \code{"spde"}.}

\item{family}{Distribution family.}

\item{phi}{Dispersion passed to the family, held fixed. One convention at
every door: for \code{gaussian} / \code{lognormal} this is the residual VARIANCE
(the SD is \code{sqrt(phi)}), for \code{neg_binomial_2} the size, \code{gamma} the
shape, \code{beta} the precision, \code{t} the scale; \code{binomial} and \code{poisson}
ignore it. The compiled kernels parameterize the two variance families by
the residual SD and are handed \code{sqrt(phi)} at the boundary.}

\item{range}{Spatial range (NULL -> use \code{spatial$prior_range[1]}).}

\item{sigma}{Marginal SD (NULL -> use \code{spatial$prior_sigma[1]}).}

\item{max_iter}{Newton iterations.}

\item{tol}{Newton tolerance.}

\item{n_threads}{OpenMP threads.}

\item{weights}{Optional per-observation likelihood weights (length
\code{length(y)}), scaling each row's log-density, score and Fisher curvature.}
}
\value{
The raw \code{cpp_laplace_fit_spde} result list (mode, log_det_Q,
log_marginal, n_iter, converged), augmented with \code{range}, \code{sigma},
and the spatial spec for downstream prediction.
}
\description{
Single-point Laplace approximation for an SPDE spatial field at a fixed
(range, sigma). Used by both \code{dispatch_laplace_spatial} (single-point
path) and \code{fit_spde} (single-point branch) so the call site stays a
single source of truth.
}
\keyword{internal}
