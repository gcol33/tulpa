% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_car.R
\name{spatial_car}
\alias{spatial_car}
\title{CAR / ICAR spatial structure}
\usage{
spatial_car(
  adjacency,
  level = c("group", "obs"),
  group_var = NULL,
  proper = FALSE,
  shared = NULL,
  parameterization = c("standard", "collapsed")
)
}
\arguments{
\item{adjacency}{Symmetric adjacency matrix (\verb{[n_units x n_units]}).}

\item{level}{Either \code{"group"} (one effect per level of \code{group_var}) or
\code{"obs"} (one effect per row of the data; \code{nrow(data)} must equal
\code{nrow(adjacency)}).}

\item{group_var}{Name of the grouping variable in the data; required
when \code{level = "group"}.}

\item{proper}{If \code{TRUE}, use proper CAR (\code{type = "car_proper"}); else
ICAR (\code{type = "car"}).}

\item{shared}{Optional shared-effect handle (see model docs).}

\item{parameterization}{\code{"standard"} (default) or \code{"collapsed"} (deprecated).}
}
\value{
A \code{tulpa_spatial} object with \code{type = "car"} (or
\code{"car_proper"} when \code{proper = TRUE}).
}
\description{
Constructs a conditional autoregressive spatial random effect from an
adjacency matrix. With \code{proper = FALSE} (the default) this is the
improper CAR / ICAR (\code{type = "car"}); with \code{proper = TRUE} it returns
the same object as \code{\link[=spatial_car_proper]{spatial_car_proper()}}.
}
\seealso{
\code{\link[=spatial_car_proper]{spatial_car_proper()}}, \code{\link[=spatial_bym2]{spatial_bym2()}}.
}
