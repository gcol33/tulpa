% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_ar2.R
\name{temporal_ar}
\alias{temporal_ar}
\title{AR(p) temporal latent field (general-order autoregressive)}
\usage{
temporal_ar(
  time_idx,
  p = 2L,
  n_times = NULL,
  prior_tau_sd = 2,
  prior_psi_sd = 1.5,
  name = NULL
)
}
\arguments{
\item{time_idx}{Integer vector (1-based) of the time point for each
observation.}

\item{p}{Autoregressive order (>= 1).}

\item{n_times}{Number of distinct time points; defaults to \code{max(time_idx)}.}

\item{prior_tau_sd, prior_psi_sd}{Prior SDs for \code{log_tau} and the
\code{atanh_psi} hyperparameters (weakly-informative Gaussian). Defaults 2 / 1.5.}

\item{name}{Optional block label (default \verb{ar<p>}).}
}
\value{
A \code{tgmrf} / \code{tulpa_latent_block} object.
}
\description{
A stationary autoregressive process of order \code{p},
\eqn{w_t = \sum_{j=1}^{p} \phi_j w_{t-j} + \varepsilon_t}, as a
user-defined GMRF latent block for a model formula via
\code{latent(temporal_ar(...))} -- the general-order extension of
\code{\link[=temporal_ar2]{temporal_ar2()}}, sharing the same construction: the precision is the exact
stationary AR(p) GMRF (the banded inverse of the Yule-Walker Toeplitz
autocovariance), and stationarity is enforced by the partial-autocorrelation
parameterization (Levinson-Durbin map of \code{p} unconstrained
\code{atanh_psi} hyperparameters), so the hyperparameter integration never
leaves the stationary region.

The block's hyperparameters are \code{log_tau} (log innovation precision) and
\code{atanh_psi1}..\verb{atanh_psi<p>}; the AR coefficients are recovered through
\code{psi_j = tanh(atanh_psi_j)} and the Levinson-Durbin recursion. Note the
outer integration cost grows with the hyperparameter count \code{p + 1}; orders
beyond 3-4 are better served by a sampler tier.
}
\examples{
\donttest{
set.seed(1)
Tt <- 150L
w <- numeric(Tt); w[1:3] <- rnorm(3)
for (t in 4:Tt) w[t] <- 0.4 * w[t-1] + 0.2 * w[t-2] - 0.2 * w[t-3] + rnorm(1, 0, 0.4)
d <- data.frame(t = seq_len(Tt), y = w + rnorm(Tt, 0, 0.3))
fit <- tulpa(y ~ latent(temporal_ar(d$t, p = 3)), data = d,
             family = "gaussian", mode = "nested_laplace")
}
}
\seealso{
\code{\link[=temporal_ar2]{temporal_ar2()}} (the \code{p = 2} shorthand), \code{\link[=temporal_ar1]{temporal_ar1()}} for the
first-order formula-integrated process, \code{\link[=tgmrf]{tgmrf()}} for the general
user-defined GMRF interface.
}
