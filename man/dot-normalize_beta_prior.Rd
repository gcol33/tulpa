% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{.normalize_beta_prior}
\alias{.normalize_beta_prior}
\title{Normalize an optional fixed-effect Gaussian prior}
\usage{
.normalize_beta_prior(beta_prior, p)
}
\arguments{
\item{beta_prior}{\code{NULL}, or a list with \code{sd} (required) and optional \code{mean}.}

\item{p}{Number of fixed effects (\code{ncol(X)}).}
}
\description{
Validates \code{beta_prior} and recycles scalar \code{mean} / \code{sd} to length \code{p}.
Returns \code{NULL} (use the built-in weak prior) or \code{list(mean, sd)} with both
vectors of length \code{p}. Shared by \code{\link[=tulpa_laplace]{tulpa_laplace()}} and the EM driver so the
validation rules live in one place.
}
\keyword{internal}
