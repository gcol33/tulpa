% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccd_grid.R
\name{ccd_weights}
\alias{ccd_weights}
\title{Corrected R-INLA CCD integration weights}
\usage{
ccd_weights(ccd)
}
\arguments{
\item{ccd}{A \code{\link[=ccd_grid]{ccd_grid()}} result. The standardized scaling is recovered as
\code{f0 = ccd$f_0 / sqrt(m)}: a \code{ccd_grid(m, f_0 = sqrt(m) * f0)} design places
the factorial corners at \verb{+/- f0}, matching the INLA convention (\code{f0 = 1.1}
is the INLA default).}
}
\value{
Numeric vector of length \code{ccd$n_points}: the design weight for each
point (the centre weight \code{w0} for the central point, \code{w} otherwise).
}
\description{
Per-point design weights for a \code{\link[=ccd_grid]{ccd_grid()}} used as nested-Laplace integration
nodes. Implements the corrected R-INLA central-composite-design weights: the
formula in Rue, Martino and Chopin (2009) has a typo, corrected by the R-INLA
team to give a positive central weight. With \code{m} hyperparameters, \code{np} design
points and standardized scaling \code{f0}, every non-centre point gets
\deqn{w = 1 / ((np - 1)(1 + e^{-m f0^2 / 2}(f0^2 - 1)))}
and the centre point gets \eqn{w_0 = 1 - (np - 1) w}. Used as
\eqn{\Delta_k}: the integration weight of node \eqn{k} is
\eqn{\Delta_k \exp(\log\,\mathrm{marginal}_k)}, renormalized. On a standardized
(whitened) hyperparameter posterior these reproduce the Gaussian moments
exactly.
}
\seealso{
\code{\link[=ccd_grid]{ccd_grid()}}, \code{\link[=ccd_to_theta]{ccd_to_theta()}}.
}
\keyword{internal}
