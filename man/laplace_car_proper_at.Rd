% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{laplace_car_proper_at}
\alias{laplace_car_proper_at}
\title{Proper-CAR Laplace at given hyperparameters}
\usage{
laplace_car_proper_at(
  y,
  n_trials,
  X,
  spatial,
  family = "binomial",
  phi = 1,
  tau = NULL,
  rho = NULL,
  re_idx = NULL,
  n_re_groups = 0L,
  sigma_re = 1,
  max_iter = 100L,
  tol = 1e-06,
  n_threads = 1L,
  offset = NULL
)
}
\description{
Single-point Laplace for a proper-CAR areal field at a fixed \verb{(tau, rho)},
the conditional counterpart of the nested CAR_proper integrator. Reuses the
shared \code{make_car_proper_latent_blocks} factory + dense spec solver via
\code{cpp_laplace_fit_car_proper}, so the mode + log-marginal equal the nested
kernel at that one grid cell. \code{tau} / \code{rho} default to the spec's fields (or
\code{tau = 1}, \code{rho} = midpoint of the eigenvalue-derived \code{rho_bounds}) and are
recorded on the result.
}
\keyword{internal}
