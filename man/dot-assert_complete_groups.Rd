% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_loglik.R
\name{.assert_complete_groups}
\alias{.assert_complete_groups}
\title{Reject an incomplete random-effect design.}
\usage{
.assert_complete_groups(re_terms, where = NULL)
}
\arguments{
\item{re_terms}{The \code{re_terms} of \code{\link[=tulpa_build_model_data]{tulpa_build_model_data()}}.}

\item{where}{Caller name for the message prefix, or \code{NULL} for none.}
}
\description{
The formula's grouping factors and random-slope covariates are built with
\code{na.action = na.pass} as the fixed design is, so an NA group or slope value
survives into \code{re_terms}. There it reached each backend in its own words --
an \code{re_idx_list} index message on the Laplace door, "NAs are not allowed in
subscripted assignments" on the samplers (gcol33/tulpa#880). Refused here
with the message \code{.assert_finite_model_inputs()} gives the predictors.
}
\keyword{internal}
