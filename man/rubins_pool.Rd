% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/em_laplace.R
\name{rubins_pool}
\alias{rubins_pool}
\title{Pool multiple imputation draws via Rubin's rules}
\usage{
rubins_pool(draws)
}
\arguments{
\item{draws}{List of K draws. Each draw is a named list of submodel
results, each with \code{beta} (numeric vector), \code{se} (numeric vector, same
length as \code{beta}), and optionally \code{gamma} (numeric vector, same length as
\code{beta}). Every draw of a submodel must carry the same number of
coefficients. A submodel absent from some draws is pooled over the draws
that carry it; one carried by a single draw is skipped with a warning.}
}
\value{
A named list of pooled submodel summaries (every submodel any draw
carries), each with \code{mean}, \code{se}, \code{V_within}, \code{V_between}, \code{V_total}, \code{K}
(number of draws that contributed), and when applicable \code{gamma} (pooled
skewness) and \code{kappa3} (pooled third cumulant).
}
\description{
Pools per-imputation block fits via Rubin's rules. When every draw also
carries a per-coefficient skewness vector \verb{$gamma}, the third cumulant
is pooled by the law of total cumulants
\deqn{\kappa_3 = E[\kappa_3(X | k)] + 3\,\mathrm{Cov}(\mu_k, \sigma_k^2) + \kappa_3(\mu_k),}
giving a pooled skewness \verb{$gamma} alongside the usual \verb{$mean} / \verb{$se}.
If any draw is missing \verb{$gamma}, the third-cumulant path is skipped.

\code{kappa3} is the third cumulant of the equal-weight mixture of the draws, so
\code{gamma} is standardized by that mixture's variance,
\code{mean(se^2) + mean((beta - mean(beta))^2)}, not by \code{V_total}: Rubin's
\code{(1 + 1/K)} inflation of the between-draw variance is a correction to the
standard error for finite \code{K}, not a moment of the mixture.
}
\examples{
# Three imputation draws of a two-coefficient submodel.
draws <- list(
  list(m = list(beta = c(1.0, 0.50), se = c(0.2, 0.10))),
  list(m = list(beta = c(1.2, 0.45), se = c(0.2, 0.10))),
  list(m = list(beta = c(0.9, 0.55), se = c(0.2, 0.10)))
)
pooled <- rubins_pool(draws)
pooled$m$mean
pooled$m$se
}
