% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sample_glmm.R
\name{tulpa_sample_glmm}
\alias{tulpa_sample_glmm}
\title{Fit a fixed-effect GLM with a model-agnostic sampler kernel}
\usage{
tulpa_sample_glmm(
  y,
  n_trials,
  X,
  family,
  backend,
  phi = 1,
  phi2 = NULL,
  offset = NULL,
  fixed_names = NULL,
  re_spec = NULL,
  spatial_spec = NULL,
  temporal_spec = NULL,
  svc_spec = NULL,
  tvc_spec = NULL,
  zi_spec = NULL,
  sigma_re_scale = 2.5,
  sigma_beta = 10,
  control = list()
)
}
\arguments{
\item{y}{Response vector.}

\item{n_trials}{Binomial denominators (or \code{NULL} -> all 1).}

\item{X}{Fixed-effect design matrix (\code{nrow(X) == length(y)}).}

\item{family}{Character family name (see \code{\link[=family_names]{family_names()}}).}

\item{backend}{One of \code{"hmc"}, \code{"ess"}, \code{"sghmc"}, \code{"sgld"}, \code{"mclmc"},
\code{"smc"}, \code{"vi"}.}

\item{phi}{Dispersion/precision passed to the family (held fixed). The
kernel parameterization: for \code{gaussian} / \code{lognormal} this is the residual
SD (the \code{\link[=tulpa]{tulpa()}} front door passes \code{sqrt(phi)}, its \code{phi} being the
variance); for \code{t} the scale.}

\item{phi2}{Optional second dispersion: the Student-t degrees of freedom
(\code{family = "t"}; default 4 when \code{NULL}).}

\item{offset}{Optional fixed additive term on the linear predictor
(\verb{eta = offset + X beta}), length \code{length(y)}; \code{NULL} -> no offset.}

\item{fixed_names}{Optional fixed-effect names for the draw columns.}

\item{re_spec}{Optional random-effect spec: a list with \code{idx} (list of
per-term 1-based group-index vectors), \code{ngroups}, \code{ncoefs}, \code{correlated}
(per-term), and \code{Z} (per-term RE design or \code{NULL}). \code{NULL} -> no RE.}

\item{spatial_spec}{Optional areal spatial spec: a list with \code{type}
(\code{"icar"}/\code{"bym2"}), \code{spatial_idx}, \code{n_spatial_units}, \code{adj_row_ptr},
\code{adj_col_idx}, \code{n_neighbors}, and \code{scale_factor} (BYM2). \code{NULL} -> none.}

\item{temporal_spec}{Optional temporal spec: a list with \code{type}
(\code{"rw1"}/\code{"rw2"}/\code{"ar1"}), \code{time_idx}, \code{n_times}, \code{n_groups}, \code{group_idx},
and \code{cyclic}. \code{NULL} -> none.}

\item{sigma_re_scale}{Half-Cauchy scale for the RE / BYM2 standard-deviation
hyperprior (sampled jointly with the latent effects).}

\item{control}{List of kernel tuning knobs (\code{n_iter}, \code{warmup}, \code{seed},
\code{sigma_beta}, \code{n_chains}, \code{max_treedepth}, \code{adapt_delta}, \code{epsilon}, \code{L},
\code{batch_size}, \code{alpha}, \code{n_particles}, \code{n_mcmc_steps}, \code{ess_threshold},
\code{vi_variant}, \code{vi_mc_samples}, \code{vi_max_iter}, \code{n_draws}, \code{verbose}).}
}
\value{
A \code{tulpa_fit} with \code{draws}, \code{means}, \code{param_names}, the kernel's
diagnostics, and (for \code{"hmc"}) \code{chain_id} / \code{n_chains} so chain diagnostics
apply.
}
\description{
Drives one of tulpa's ModelData sampler kernels -- NUTS (\code{"hmc"}), elliptical
slice sampling (\code{"ess"}), SGHMC (\code{"sghmc"}), SGLD (\code{"sgld"}), MCLMC
(\code{"mclmc"}), sequential Monte Carlo (\code{"smc"}), or variational inference
(\code{"vi"}) -- on a fixed-effect GLM. The model (design + per-observation
likelihood) is built once through the same built-in-family scaffold the
single-point Laplace fit uses, so no likelihood / link logic is duplicated.
}
\keyword{internal}
