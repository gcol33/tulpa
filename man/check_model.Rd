% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics_sim.R
\name{check_model}
\alias{check_model}
\alias{check_model.default}
\title{Diagnostic panel plot}
\usage{
check_model(object, ...)

\method{check_model}{default}(object, coords = NULL, nsim = 250L, seed = 123L, ...)
}
\arguments{
\item{object}{A fitted model with \code{simulate()}, \code{fitted()}, \code{residuals()}}

\item{...}{Passed to methods.}

\item{coords}{Optional N x 2 coordinate matrix for spatial panel}

\item{nsim}{Number of simulations (default 250)}

\item{seed}{Random seed (default 123)}
}
\value{
Invisible list with \code{ks_p}, \code{disp_ratio}, \code{moran} (if spatial)
}
\description{
Produces a 2x2 or 1x3 panel of diagnostic plots:
\enumerate{
\item QQ plot of PIT residuals vs Uniform (with KS p-value)
\item Residuals vs fitted (with lowess smoother)
\item Dispersion histogram (observed variance vs simulated)
\item Spatial correlogram via Moran's I (if coords provided)
}
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(x = rnorm(80))
d$y <- rpois(80, exp(0.3 + 0.5 * d$x))
fit <- tulpa(y ~ x, data = d, family = "poisson", mode = "laplace")
check_model(fit, nsim = 50)
}
}
