% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_gibbs.R
\name{tulpa_rpg}
\alias{tulpa_rpg}
\title{Draw Polya-Gamma random variates}
\usage{
tulpa_rpg(b, z)
}
\arguments{
\item{b}{Integer vector of PG shape parameters (trial counts); one draw
per element.}

\item{z}{Numeric vector of tilting parameters, the same length as \code{b}.}
}
\value{
A numeric vector of \code{PG(b, z)} draws, the same length as \code{b}.
}
\description{
Vectorized \code{PG(b, z)} draws via the Polson, Scott & Windle (2013) sampler
(\code{tulpa::rpg_vec()}), the kernel every Polya-Gamma Gibbs fitter in this
package draws its auxiliary weights from. Exposed as a door for consumer
packages fitting their own Polya-Gamma Gibbs models, which cannot reach an
RNG through \code{LinkingTo} the way a likelihood kernel would.
}
\examples{
tulpa_rpg(rep(1L, 5), rnorm(5))
}
