% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_interop.R
\name{as_draws}
\alias{as_draws}
\alias{as_draws.tulpa_fit}
\alias{as_draws_array}
\alias{as_draws_array.tulpa_fit}
\alias{as_draws_matrix}
\alias{as_draws_matrix.tulpa_fit}
\alias{as_draws_df}
\alias{as_draws_df.tulpa_fit}
\alias{as_draws_rvars}
\alias{as_draws_rvars.tulpa_fit}
\title{Posterior draws in the posterior package's format}
\usage{
as_draws(x, ...)

\method{as_draws}{tulpa_fit}(x, n_draws = NULL, seed = NULL, ...)

as_draws_array(x, ...)

\method{as_draws_array}{tulpa_fit}(x, n_draws = NULL, seed = NULL, ...)

as_draws_matrix(x, ...)

\method{as_draws_matrix}{tulpa_fit}(x, n_draws = NULL, seed = NULL, ...)

as_draws_df(x, ...)

\method{as_draws_df}{tulpa_fit}(x, n_draws = NULL, seed = NULL, ...)

as_draws_rvars(x, ...)

\method{as_draws_rvars}{tulpa_fit}(x, n_draws = NULL, seed = NULL, ...)
}
\arguments{
\item{x}{A \code{tulpa_fit} object.}

\item{...}{Passed to the corresponding \code{posterior} converter.}

\item{n_draws}{Number of draws to synthesize from the Gaussian approximation
for a fit that carries none. \code{NULL} (default) errors on such a fit rather
than silently approximating. Ignored, with a warning, when the fit already
carries draws.}

\item{seed}{Optional integer seed for the synthesis. The RNG state is
restored afterwards.}
}
\value{
A \code{posterior} draws object of the requested shape.
}
\description{
Convert a fit's posterior draws to a \code{posterior} draws object. \code{as_draws()}
returns the \code{draws_array} shape; \code{as_draws_array()}, \code{as_draws_matrix()},
\code{as_draws_df()} and \code{as_draws_rvars()} return theirs. When the \code{posterior}
package is installed these are also registered against its generics, so
\code{posterior::as_draws(fit)} works.
}
\details{
Fits differ in whether they carry draws at all. Sampler and nested-Laplace
fits do, and convert directly. A Gaussian-approximation fit (\code{mode = "laplace"}, \code{mode = "eb"}) carries a mode and a precision instead, and
converting it means \emph{drawing from the approximation} -- which is a modelling
decision, not a format change, because every downstream \code{posterior} summary
would then treat a normal approximation as a posterior sample. So it is
opt-in: pass \code{n_draws} to synthesize that many draws from
\code{N(coef(object), vcov(object))}, or get an error naming the alternative.
Synthesized draws form a single chain and cover the fixed effects only.
}
\examples{
\donttest{
set.seed(1)
df <- data.frame(x = rnorm(80))
df$y <- rpois(80, exp(0.5 + 0.3 * df$x))
fit <- tulpa(y ~ x, data = df, family = "poisson")
if (requireNamespace("posterior", quietly = TRUE)) {
  posterior::summarise_draws(as_draws(fit))
}
}
}
\seealso{
\code{\link[=tulpa_draws_array]{tulpa_draws_array()}} for the base R array without the dependency,
\code{\link[=posterior_sample]{posterior_sample()}} for the raw matrix.
}
