% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tgmrf_nuts.R
\name{tulpa_tgmrf_nuts}
\alias{tulpa_tgmrf_nuts}
\title{NUTS over a tgmrf block's hyperparameters}
\usage{
tulpa_tgmrf_nuts(
  y,
  n_trials,
  X,
  block,
  family = "binomial",
  phi = 1,
  re_idx = NULL,
  n_re_groups = 0L,
  sigma_re = 1,
  n_iter = 500L,
  warmup = n_iter\%/\%2L,
  epsilon = NULL,
  max_depth = 6L,
  target_accept = 0.65,
  pilot_axis_points = 5L,
  fd_gradient_step = 0.02,
  max_iter = 50L,
  tol = 1e-07,
  n_threads = 1L,
  verbose = FALSE
)
}
\arguments{
\item{y, n_trials, X}{Response, trials per obs (1L vector for non-binomial),
and fixed-effects design matrix -- same arguments as
\code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}}.}

\item{block}{A \code{tgmrf} object.}

\item{family, phi}{Likelihood family and dispersion.}

\item{re_idx, n_re_groups, sigma_re}{Optional RE wiring (forwarded to
the inner Laplace).}

\item{n_iter}{Total number of NUTS iterations (including warmup). Default
\code{500L}.}

\item{warmup}{Number of warmup iterations used for step-size adaptation and
then discarded. Must satisfy \verb{1 <= warmup < n_iter}. Default \code{n_iter \%/\% 2L}.}

\item{epsilon}{Initial leapfrog step size. Default is \code{0.2 / sqrt(theta_dim)}.}

\item{max_depth}{Maximum NUTS tree depth (\code{2^max_depth} leapfrog steps
per draw at the limit). Default 6.}

\item{target_accept}{Target acceptance for the warmup step-size
adaptation. Default 0.65.}

\item{pilot_axis_points}{Per-axis grid resolution for the pilot fit
(default 5).}

\item{fd_gradient_step}{Finite-difference step for \verb{d/dtheta_j log_marginal}. Default 0.02.}

\item{max_iter, tol}{Inner-Newton budget per proposal.}

\item{n_threads}{OpenMP threads inside each inner Laplace solve.}

\item{verbose}{Forwarded to \code{\link[=imh_laplace]{imh_laplace()}}.}
}
\value{
A list with class \code{c("tulpa_tgmrf_nuts", "tulpa_fit")}:
\itemize{
\item \code{draws}, \code{means}, \code{sds} — posterior draws of \code{theta} and moments.
\item \code{mean_accept} — mean Metropolis acceptance probability across the
post-warmup tree expansion steps.
\item \code{tree_depth} — depth of the NUTS tree at each iteration.
\item \code{epsilon} — final step size after warmup adaptation.
\item \code{pilot}, \code{mode_theta}, \code{hessian_theta} — pilot diagnostics (same as
\code{\link[=tulpa_tgmrf_imh]{tulpa_tgmrf_imh()}}).
\item \code{inference_mode}, \code{inference_tier}, \code{backend} —
\code{"exact"}, \code{1L}, \code{"tgmrf_nuts"}.
}
}
\description{
No-U-Turn Sampler (Hoffman & Gelman 2014) over the user-defined
hyperparameter vector \code{theta} of a \code{\link[=tgmrf]{tgmrf()}} block, with a finite-
difference gradient on \code{log_marginal(theta)}. Same Tier 1
(MCMC-corrected) composition as \code{\link[=tulpa_tgmrf_imh]{tulpa_tgmrf_imh()}} but the proposal
uses gradient information through leapfrog integration, so mixing
degrades more gracefully with \code{theta_dim} than independence-MH does.

Each leapfrog step evaluates the gradient via \code{2 * theta_dim} extra
inner Laplace solves (central FD on \code{log_marginal}); a NUTS tree of
depth \code{j} integrates \code{2^j} leapfrog steps. The implementation is the
textbook Hoffman & Gelman (2014) Algorithm 3 (NUTS \emph{without} dual
averaging): pilot Laplace gives the initial step size and mass-matrix
diagonal; warmup performs a simple Robbins-Monro adaptation toward
the target acceptance.
}
\references{
Hoffman & Gelman (2014). The No-U-Turn Sampler. JMLR
15:1593-1623.
}
\seealso{
\code{\link[=tulpa_tgmrf_imh]{tulpa_tgmrf_imh()}} for the independence-MH counterpart.
}
