% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psis.R
\name{tulpa_batched_pareto_k}
\alias{tulpa_batched_pareto_k}
\title{Outer Pareto-k-hat for a batched-target grid-integrated fit}
\usage{
tulpa_batched_pareto_k(
  theta_hat,
  L_scale,
  log_target_batched,
  n_samples = .nl_diag("k_samples"),
  radius_cap = Inf,
  return_draws = FALSE,
  tail_points = NULL,
  Z = NULL
)
}
\arguments{
\item{theta_hat, L_scale}{Mean and Cholesky scale of the Gaussian proposal
\verb{N(theta_hat, L_scale L_scale')}, in the integrator's own coordinate
space (e.g. \verb{(log range, log sigma)} for an SPDE field).}

\item{log_target_batched}{Function taking an \verb{S x d} matrix of proposal
draws and returning the integrator's unnormalized log posterior at each
row, in that same coordinate space. Any change-of-variables Jacobian is
the caller's responsibility.}

\item{n_samples}{Number of draws from the proposal.}

\item{radius_cap}{Whitened-radius cap on evaluated draws (\code{Inf} keeps
every draw); draws past the cap are folded back in under heavy-tail
escalation rather than dropped.}

\item{return_draws}{If \code{TRUE}, include the draws in the returned list.}

\item{tail_points}{Optional override for the PSIS tail size; \code{NULL} uses
the automatic rule.}

\item{Z}{Optional pre-drawn \verb{S x d} whitened sample; \code{NULL} draws it here.}
}
\value{
A list with \code{pareto_k}, \code{is_ess}, \code{n_eval}, and \code{declined} (a
reason string, or \code{NULL} when the diagnostic ran to completion).
}
\description{
Outer Pareto-k-hat reliability diagnostic for a grid-integrated nested fit
whose inner marginal comes from a BATCHED re-fit rather than a per-sample
closure: fits a Gaussian proposal at \verb{(theta_hat, L_scale)}, draws from it,
evaluates the caller's batched log-target, applies a radius cap with
heavy-tail escalation, then reports \code{pareto_k} / \code{is_ess} via the shared
PSIS/GPD core. This is more than plain PSIS smoothing over an
already-computed log-ratio vector -- see \code{\link[=tulpa_psis]{tulpa_psis()}} for that -- because
it also builds the proposal, draws, and evaluates the target.
}
\seealso{
\code{\link[=tulpa_psis]{tulpa_psis()}}
}
