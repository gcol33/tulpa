% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_modes.R
\name{tulpa_dispatch}
\alias{tulpa_dispatch}
\title{Dispatch a fit to the backend chosen by the mode/tier system.}
\usage{
tulpa_dispatch(
  mode,
  fitter_args = list(),
  family = NULL,
  n_obs = NULL,
  has_spatial = FALSE,
  has_temporal = FALSE,
  has_latent = FALSE,
  spatial_type = NULL,
  temporal = NULL
)
}
\arguments{
\item{mode}{User-specified mode (\code{"auto"}, a tier, or a backend name).}

\item{fitter_args}{Named list of arguments forwarded to the backend fitter.}

\item{family, n_obs, has_spatial, has_temporal, has_latent, spatial_type, temporal}{Model characteristics forwarded to \code{\link[=select_inference_mode]{select_inference_mode()}}.}
}
\value{
The fitter's result, with \code{inference_mode}, \code{inference_tier},
\code{backend}, and \code{selection_reason} ensured.
}
\description{
The routing spine. Resolves \code{mode} to a concrete backend via
\code{\link[=select_inference_mode]{select_inference_mode()}}, asserts the backend is R-reachable (fails loudly
otherwise), then calls its fitter with \code{fitter_args}. The caller supplies
\code{fitter_args} matching the backend's input contract
(\verb{BACKEND_REGISTRY$<backend>$input}).

The selected mode/tier/backend are stamped onto the returned fit (without
overwriting any the fitter already set), so the inference contract is always
visible in the output.
}
\keyword{internal}
