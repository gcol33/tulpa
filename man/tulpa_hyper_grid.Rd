% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyper_grid.R
\name{tulpa_hyper_grid}
\alias{tulpa_hyper_grid}
\title{Outer hyperparameter-grid integration with a user-supplied inner fit}
\usage{
tulpa_hyper_grid(
  hyper_specs,
  inner_fit,
  combine = c("law_of_total_cov", "weighted_mean_only", "none"),
  n_draws = 2000L,
  seed = NULL,
  beta_names = NULL
)
}
\arguments{
\item{hyper_specs}{A list of axis specs. Each spec is either a
\code{hyper_axis_spec()} object or a plain list with the same fields (the
driver auto-wraps); see \code{\link[=hyper_axis_spec]{hyper_axis_spec()}}. The outer grid is the
Cartesian product of the per-axis grids; the joint log-prior is the sum
of per-axis \code{log_prior} contributions (axes with \code{log_prior = NULL} are
flat / improper).}

\item{inner_fit}{\verb{function(hypers)} returning a list with:
\itemize{
\item \code{log_marginal} -- scalar; the inner-solve log marginal at this cell.
Non-finite values are mapped to \code{-Inf} (the cell gets zero weight).
\item \code{beta_mean} -- numeric vector of fixed-effect posterior mean at the
cell. Required when \code{combine != "none"}.
\item \code{beta_cov} -- \verb{p x p} numeric matrix of fixed-effect marginal
covariance at the cell. Required when \code{combine = "law_of_total_cov"}.
\code{hypers} is a named numeric vector with the current cell's axis values
(names match \verb{vapply(hyper_specs, }[[\verb{, character(1), "name")}).
Errors thrown by \code{inner_fit} are caught and treated as a failed cell
(\code{log_marginal = -Inf}, no beta contribution).
}}

\item{combine}{How to pool per-cell fixed-effect posteriors into a single
posterior over the betas. One of:
\itemize{
\item \code{"law_of_total_cov"} (default) -- compute
\code{E[Cov(beta | theta)] + Cov(E[beta | theta])} from the per-cell
\verb{(beta_mean, beta_cov)}; synthesise \code{n_draws} posterior draws by
mixture sampling. Requires \code{beta_mean} and \code{beta_cov} per cell.
\item \code{"weighted_mean_only"} -- pool only the per-cell \code{beta_mean} into the
weighted posterior mean. \code{beta_cov} is ignored; no draws.
\item \code{"none"} -- do not assemble a fixed-effect posterior. Only the
hyperparameter posterior is returned. \code{beta_mean} / \code{beta_cov} from
\code{inner_fit} are ignored if supplied.
}}

\item{n_draws}{Number of posterior draws of the fixed effects to
synthesise from the cell mixture (default 2000). Used only when
\code{combine = "law_of_total_cov"}. \code{0} disables draw synthesis (the
law-of-total-cov mean and covariance are still returned).}

\item{seed}{Optional integer seed for the draw synthesis.}

\item{beta_names}{Optional character vector naming the fixed-effect
coordinates. When \code{NULL} (default) the names are taken from the first
successful cell's \code{beta_mean} (or \code{beta1}, \code{beta2}, ... if it is
unnamed).}
}
\value{
A list of class \code{c("tulpa_hyper_grid", "tulpa_fit", "list")} with:
\itemize{
\item \code{theta_grid} -- numeric matrix \verb{[n_cells x n_axes]} of outer-grid
hyperparameter values; columns named after the axes.
\item \code{theta_names} -- character vector of axis names.
\item \code{log_marginal} -- numeric \verb{[n_cells]}; per-cell log integrand
\code{inner$log_marginal + log_prior(theta_cell)}. Non-finite / failed cells
are \code{-Inf}.
\item \code{log_prior} -- numeric \verb{[n_cells]}; per-cell log-prior contribution
(the sum across axes of \code{axis$log_prior(theta_cell[axis])}). \code{0} when
all axes have \code{log_prior = NULL}.
\item \code{weights} -- numeric \verb{[n_cells]} summing to 1 (or all \code{NA} with a
warning when no cell carries finite mass).
\item \code{theta_mean}, \code{theta_sd} -- named numeric vectors; weighted posterior
mean and SD per axis. SDs refit via the 3-point Laplace-at-mode
parabola where possible (gcol33/tulpa#20).
\item \code{theta_median}, \code{theta_ci_lo}, \code{theta_ci_hi} -- named numeric vectors;
weighted-quantile median and 2.5 / 97.5\\% empirical CI per axis
(the recommended summary for right-skewed scale-like axes).
\item \code{beta}, \code{beta_cov}, \code{draws} -- fixed-effect posterior, present per
\code{combine}:
\itemize{
\item \code{combine = "law_of_total_cov"}: weighted mean, total covariance
(\code{E[V] + V[E]}), \verb{[n_draws x p]} mixture draws.
\item \code{combine = "weighted_mean_only"}: weighted mean only; \code{beta_cov}
and \code{draws} are \code{NULL}.
\item \code{combine = "none"}: all three are \code{NULL}.
}
\item \code{means}, \code{param_names}, \code{process_info}, \code{n_samples}, \code{n_params},
\code{N} -- the \code{tulpa_fit} accessor surface, populated when a
fixed-effect posterior is assembled.
\item \code{hyper_specs} -- echoed list of \code{hyper_axis_spec} objects (the
normalised form).
\item \code{combine}, \code{n_failed}, \code{n_grid} -- diagnostic fields.
}
}
\description{
Generic driver for nested-Laplace-style outer integration over a small
hyperparameter block. The user supplies per-axis specs (values + optional
log-prior + log-scale / bounds / refinable metadata) and an \code{inner_fit}
callback that, at every hyperparameter cell, returns the inner log marginal
and optionally the fixed-effect posterior mode + marginal covariance.
The driver builds the Cartesian outer grid, normalises the log-marginals to
integration weights, reports per-axis posterior moments and weighted
quantiles, and (when the inner fit supplies them) law-of-total-covariance
fixed-effect posterior + mixture draws.

This factors the per-family outer-grid plumbing in \code{tulpa_nested_laplace()}
/ \code{tulpa_nested_laplace_joint()} / \code{tulpa_re_cov_nested()} into one
callback-driven entry point: downstream consumers (occupancy / N-mixture /
cover hurdle families in tulpaObs, custom user families) drop in their own
per-cell inner fit and get the outer integration for free.
}
\seealso{
\code{\link[=hyper_axis_spec]{hyper_axis_spec()}} for the axis-spec constructor;
\code{\link[=tulpa_nested_laplace_joint]{tulpa_nested_laplace_joint()}} for the family-specific outer-grid
driver that this helper generalises.
}
