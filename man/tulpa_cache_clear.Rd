% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tgmrf_cpp.R
\name{tulpa_cache_clear}
\alias{tulpa_cache_clear}
\title{Remove compiled blocks from the tgmrf_cpp() cache}
\usage{
tulpa_cache_clear(older_than = 0)
}
\arguments{
\item{older_than}{Remove only entries whose last modification is more than
this many days ago. \code{0} (the default) removes every entry.}
}
\value{
The number of entries removed, invisibly.
}
\description{
Deletes cached compilation output under \code{\link[=tulpa_cache_dir]{tulpa_cache_dir()}}. The cache holds
build artefacts only -- a removed entry is rebuilt by the next \code{\link[=tgmrf_cpp]{tgmrf_cpp()}}
call on the same source -- so it can be emptied at any time.
}
\examples{
\dontrun{
# Deletes the caller's own cached builds, so it is not run on check.
tulpa_cache_clear()              # empty the cache
tulpa_cache_clear(older_than = 30)  # keep the last 30 days
}

}
\seealso{
\code{\link[=tulpa_cache_dir]{tulpa_cache_dir()}} for the location.
}
