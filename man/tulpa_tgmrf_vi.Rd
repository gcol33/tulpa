% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tgmrf_vi.R
\name{tulpa_tgmrf_vi}
\alias{tulpa_tgmrf_vi}
\title{Variational inference over a tgmrf block's hyperparameters}
\usage{
tulpa_tgmrf_vi(
  y,
  n_trials,
  X,
  block,
  family = "binomial",
  phi = 1,
  re_idx = NULL,
  n_re_groups = 0L,
  sigma_re = 1,
  n_draws = 1000L,
  max_lbfgs = 100L,
  lbfgs_tol = 1e-06,
  pilot_axis_points = 5L,
  max_iter = 50L,
  tol = 1e-07,
  n_threads = 1L,
  verbose = FALSE
)
}
\arguments{
\item{y, n_trials, X}{Response, trials per obs (1L vector for non-binomial),
and fixed-effects design matrix — same arguments as
\code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}}.}

\item{block}{A \code{tgmrf} object.}

\item{family, phi}{Likelihood family and dispersion.}

\item{re_idx, n_re_groups, sigma_re}{Optional RE wiring (forwarded to
the inner Laplace).}

\item{n_draws}{Number of variational draws to return (default 1000).}

\item{max_lbfgs}{L-BFGS iteration cap (default 100).}

\item{lbfgs_tol}{L-BFGS gradient-norm tolerance (default 1e-6).}

\item{pilot_axis_points}{Per-axis grid resolution for the pilot fit
(default 5).}

\item{max_iter, tol}{Inner-Newton budget per proposal.}

\item{n_threads}{OpenMP threads inside each inner Laplace solve.}

\item{verbose}{Forwarded to \code{\link[=imh_laplace]{imh_laplace()}}.}
}
\value{
A list with class \code{c("tulpa_tgmrf_vi", "tulpa_fit")}:
\itemize{
\item \code{draws} — \verb{n_draws x theta_dim} matrix of variational draws.
\item \code{means}, \code{sds} — variational moments.
\item \code{mode_theta} — L-BFGS mode in \code{theta}.
\item \code{cov} — variational covariance (\code{solve(-hessian)}).
\item \code{elbo} — MC estimate of \verb{E_q[log_marginal(theta) - log q(theta)]}.
\item \code{pilot} — the pilot \code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}} object.
\item \code{converged} — logical, L-BFGS convergence flag.
\item \code{inference_mode}, \code{inference_tier}, \code{backend} — \code{"structured"},
\code{2L}, \code{"tgmrf_vi"}.
}
}
\description{
Single-path Pathfinder VI (Zhang, Carpenter, Gelman, Vehtari 2022)
over the user-defined hyperparameter vector \code{theta} of a \code{\link[=tgmrf]{tgmrf()}}
block. L-BFGS finds the mode of \code{log_marginal(theta)}, the optimum's
inverse Hessian becomes the variational covariance, and the resulting
Gaussian is the variational posterior \code{q(theta)}. ELBO is computed by
MC averaging over draws from \code{q}.

This is the Tier-2 (structured) sibling of \code{\link[=tulpa_tgmrf_imh]{tulpa_tgmrf_imh()}} and
\code{\link[=tulpa_tgmrf_nuts]{tulpa_tgmrf_nuts()}}: same Laplace body for \verb{(beta, z) | theta}, but
the outer integration over \code{theta} is the L-BFGS Gaussian fit rather
than MH or NUTS. No bias correction — the Gaussian fit \emph{is} the
approximation. Pair with \code{\link[=tulpa_tgmrf_imh]{tulpa_tgmrf_imh()}} for a Tier-1 upgrade
(the VI fit is a perfect IMH proposal).

The implementation simply composes:
\enumerate{
\item Pilot grid Laplace → init \code{theta} (grid argmax).
\item \code{\link[=pathfinder]{pathfinder()}} on \code{log_marginal(theta)} with FD-based L-BFGS
(gradients from \code{stats::optim}'s internal finite differences).
\item Wrap the result with tgmrf-specific metadata + block hyperparameter
names.
}
}
\references{
Zhang, Carpenter, Gelman, Vehtari (2022). Pathfinder:
parallel quasi-Newton variational inference. JMLR 23(306):1-49.
}
\seealso{
\code{\link[=tulpa_tgmrf_imh]{tulpa_tgmrf_imh()}} (Tier 1 MH), \code{\link[=tulpa_tgmrf_nuts]{tulpa_tgmrf_nuts()}}
(Tier 1 NUTS), \code{\link[=pathfinder]{pathfinder()}} (underlying VI engine).
}
