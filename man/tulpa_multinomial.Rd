% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multinomial.R
\name{tulpa_multinomial}
\alias{tulpa_multinomial}
\title{Multinomial (nominal K-class) logistic regression via Laplace}
\usage{
tulpa_multinomial(formula, data, beta_prior_sd = 10, control = list())
}
\arguments{
\item{formula}{Model formula; the response must be a factor (or coercible to
one) with >= 3 levels. The baseline is the last level.}

\item{data}{A data frame.}

\item{beta_prior_sd}{SD of the mean-zero Gaussian prior on every coefficient
(ridge; default 10). A finite value keeps the mode finite under separation.}

\item{control}{List of numerical knobs: \code{max_iter} (default 100), \code{tol}
(default 1e-8), \code{n_draws} (posterior draws, default 2000), \code{seed}.}
}
\value{
A \code{tulpa_fit} (subclass \code{tulpa_multinomial}) with \code{coef} (named
\code{class:term}), \code{vcov}, \code{draws}, \code{log_marginal}, \code{classes}, \code{baseline}, and
the standard generic-method support.
}
\description{
Fits a baseline-category multinomial logit model: for a K-level unordered
response, classes \verb{1..K-1} each get their own linear predictor and class \code{K}
is the baseline. The coupled multinomial likelihood is solved by a Newton
step to the penalized mode (a Gaussian ridge prior on the coefficients) and
summarized by a Laplace approximation, reusing the native multinomial kernel.
}
\examples{
\donttest{
set.seed(1)
n <- 300L; x <- rnorm(n)
eta <- cbind(0.5 + 1.0 * x, -0.3 - 0.8 * x)          # classes 1, 2 vs baseline 3
P <- cbind(exp(eta), 1); P <- P / rowSums(P)
y <- factor(apply(P, 1, function(pr) sample.int(3L, 1L, prob = pr)))
fit <- tulpa_multinomial(y ~ x, data = data.frame(y = y, x = x))
coef(fit)
}
}
\seealso{
\code{\link[=tulpa]{tulpa()}} for single-process GLMMs.
}
