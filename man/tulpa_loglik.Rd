% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{tulpa_loglik}
\alias{tulpa_loglik}
\title{Streaming pointwise log-likelihood}
\usage{
tulpa_loglik(x, n_obs = NULL, n_draws = NULL)
}
\arguments{
\item{x}{Either a numeric \verb{[n_draws x n_obs]} matrix, an existing
\code{tulpa_loglik}, or a function \code{f(cols)} returning the
\verb{[n_draws x length(cols)]} submatrix for the integer column indices \code{cols}.}

\item{n_obs, n_draws}{Required when \code{x} is a generator function; the column
and row counts of the implied matrix.}
}
\value{
A \code{tulpa_loglik} object: a list with \code{get(cols)}, \code{n_obs},
\code{n_draws}, and \code{materialized}.
}
\description{
Wrap a pointwise log-likelihood for \code{\link[=tulpa_criteria]{tulpa_criteria()}} without materializing
the whole \verb{[n_draws x n_obs]} matrix. A plain matrix is wrapped directly; a
block generator (a function of an integer column vector returning the
\verb{[n_draws x length(cols)]} submatrix) lets the criteria accumulators stream
over observation blocks, so an EVA-scale \verb{[200 x 1.16M]} log-likelihood is
consumed a few thousand columns at a time.
}
\seealso{
\code{\link[=tulpa_criteria]{tulpa_criteria()}}
}
