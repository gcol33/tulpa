% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_field.R
\name{tulpa_bar_field_replicate}
\alias{tulpa_bar_field_replicate}
\title{Replicate an areal graph across the levels of a factor (replicated CAR)}
\usage{
tulpa_bar_field_replicate(adjacency, node, by)
}
\arguments{
\item{adjacency}{Symmetric adjacency matrix of the base graph
(\verb{[n_node x n_node]}, dense or sparse).}

\item{node}{Integer vector of 1-based graph-node indices, one per
observation (the resolved bar right-hand side).}

\item{by}{A vector of the same length as \code{node} giving each observation's
replication level; coerced to a factor. With \code{L} distinct levels the field
is replicated \code{L} times.}
}
\value{
A list:
\describe{
\item{\code{adjacency}}{the \verb{[L*n_node x L*n_node]} block-diagonal Kronecker
adjacency \verb{I_L (x) Q} (the base graph for \code{L == 1}).}
\item{\code{index}}{integer vector, one per observation: the node index
offset into its level's copy (\code{node + (level - 1) * n_node}).}
\item{\code{n_levels}}{the number of replication levels \code{L}.}
\item{\code{n_nodes}}{the base graph node count \code{n_node}.}
\item{\code{levels}}{the factor levels of \code{by}, in replicate order.}
}
}
\description{
Build the block-diagonal Kronecker graph \verb{I_L (x) Q} and the level-offset
node index for a replicated areal field: one independent copy of the graph
per level of a \code{by} factor, all sharing one precision. This is the
graph-side counterpart to \code{\link[=tulpa_bar_field_specs]{tulpa_bar_field_specs()}} -- that helper expands
the coefficient columns and is graph-agnostic, while replication needs the
graph, so it is a sibling rather than a new argument. A downstream package
composes the two (column expansion x replication) from the one
implementation rather than re-deriving the Kronecker remap.
}
\examples{
adj <- matrix(0, 4, 4)
for (i in 1:3) adj[i, i + 1] <- adj[i + 1, i] <- 1
node <- rep(1:4, times = 2)
lev  <- rep(c("a", "b"), each = 4)
rep_info <- tulpa_bar_field_replicate(adj, node, lev)
dim(rep_info$adjacency)   # 8 x 8 (I_2 (x) Q)
rep_info$index            # level b nodes offset by 4

}
\seealso{
\code{\link[=tulpa_bar_field_specs]{tulpa_bar_field_specs()}} for the coefficient-column expansion,
\code{\link[=spatial]{spatial()}} for the inline areal field constructor whose \verb{by =} argument
this powers.
}
