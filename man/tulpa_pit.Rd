% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{tulpa_pit}
\alias{tulpa_pit}
\title{Probability integral transform from a predictive CDF}
\usage{
tulpa_pit(cdf, cdf_lower = NULL, jitter = TRUE)
}
\arguments{
\item{cdf}{Posterior-predictive CDF at the observed value, \code{P(Y <= y)}. A
\verb{[n_draws x n_obs]} matrix (averaged over draws here) or an \verb{[n_obs]}
vector.}

\item{cdf_lower}{Optional left-limit CDF \code{P(Y < y)}, same shape as \code{cdf}, for
the randomized PIT of a discrete / mixed response.}

\item{jitter}{If \code{TRUE} (default) and \code{cdf_lower} is \code{NULL}, add a tiny
uniform jitter to break ties from a discretized CDF; ignored when
\code{cdf_lower} is supplied (the interpolation already randomizes).}
}
\value{
Numeric vector of length \code{n_obs} of PIT values in \verb{[0, 1]}.
}
\description{
The generic, family-agnostic half of a PIT residual check: the model package
supplies the posterior-predictive CDF evaluated at each observation (a
\verb{[n_draws x n_obs]} matrix, or a draw-averaged \verb{[n_obs]} vector), and this
returns the PIT value per observation. For a discrete or mixed response
(a hurdle has a point mass at zero) supply the left limit \code{cdf_lower}
(\code{P(Y < y)}); the randomized PIT then draws one uniform per observation and
interpolates \verb{F(y^-) + U (F(y) - F(y^-))}, which is uniform under a correct
model. With \code{cdf_lower = NULL} the response is treated as continuous and the
PIT is the draw-averaged CDF.
}
\seealso{
\code{\link[=tulpa_criteria]{tulpa_criteria()}}
}
