% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agq.R
\name{agq_fit}
\alias{agq_fit}
\title{Adaptive Gauss-Hermite quadrature for one-RE GLMMs}
\usage{
agq_fit(
  y,
  X,
  group,
  n_groups = max(group),
  family = c("binomial", "poisson", "gaussian"),
  n_trials = NULL,
  sigma_eps = 1,
  n_quad = 7L,
  beta_init = NULL,
  sigma_init = 1,
  max_iter = 200L,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{y}{Response vector.}

\item{X}{Fixed-effects design matrix (\verb{n_obs × p}).}

\item{group}{Integer cluster labels (1-based, length \code{n_obs}).}

\item{n_groups}{Number of clusters (default \code{max(group)}).}

\item{family}{One of \code{"binomial"}, \code{"poisson"}, \code{"gaussian"}.}

\item{n_trials}{Trial sizes (binomial only; default \code{rep(1, n_obs)}).}

\item{sigma_eps}{Residual SD (gaussian only; default \code{1}).}

\item{n_quad}{Number of Gauss-Hermite quadrature nodes per cluster.
\code{1} recovers Laplace; common choices are \code{5} or \code{7}. Default \code{7}.}

\item{beta_init}{Initial fixed-effects (default zeros).}

\item{sigma_init}{Initial RE SD (default \code{1}).}

\item{max_iter}{Optimiser iteration cap (default \code{200}).}

\item{tol}{Convergence tolerance (default \code{1e-6}).}

\item{verbose}{Print optimiser summary (default \code{FALSE}).}
}
\value{
A list with class \code{tulpa_fit} carrying:
\itemize{
\item \code{means}: named vector \code{c(beta, log_sigma_re)}.
\item \code{mode}: same as \code{means} (Gaussian fit at the optimum).
\item \code{cov}: covariance matrix of \verb{(beta, log_sigma_re)} from inverse
observed information.
\item \code{sigma_re}: estimated RE SD.
\item \code{log_marginal}: AGQ-approximated log marginal likelihood at the
fitted values.
\item \code{n_quad}: quadrature order used.
\item \code{n_iter}, \code{converged}: optimiser diagnostics.
\item \code{inference_mode}: \code{"structured"}.
\item \code{inference_tier}: \code{2L}.
\item \code{backend}: \code{"agq"}.
}
}
\description{
Marginal-likelihood approximation for a generalised linear mixed
model with one cluster-level intercept random effect. Adaptive
Gauss-Hermite quadrature (AGQ) generalises Laplace by replacing the
single-point Gaussian integral around the cluster's posterior mode
with \code{n_quad}-point quadrature. AGQ at \code{n_quad = 1} recovers the
Laplace approximation; higher \code{n_quad} reduces approximation error,
especially for clusters with few observations or non-Gaussian
likelihoods.

Scoped to the \code{lme4::glmer(..., nAGQ = N)} use case: one intercept-
only RE term, families \code{binomial}, \code{poisson}, or \code{gaussian}. For
multi-RE or random-slope models, use \code{\link[=tulpa_laplace]{tulpa_laplace()}} (Laplace at
the joint mode) or HMC.
}
\section{Tier}{

Tier 2 (Structured). AGQ is exact in the limit \code{n_quad -> infinity}
but at finite \code{n_quad} it is a controlled approximation — same
epistemic class as Laplace.
}

\examples{
\dontrun{
  set.seed(1)
  n_g <- 30; n_per <- 5; n <- n_g * n_per
  group <- rep(seq_len(n_g), each = n_per)
  x <- rnorm(n)
  X <- cbind(1, x)
  u <- rnorm(n_g, 0, 0.8)
  eta <- 0.3 + 0.7 * x + u[group]
  y <- rbinom(n, 1, plogis(eta))

  # Compare Laplace (n_quad = 1) and AGQ-7.
  fit_lap <- agq_fit(y, X, group, family = "binomial", n_quad = 1)
  fit_agq <- agq_fit(y, X, group, family = "binomial", n_quad = 7)
  c(fit_lap$log_marginal, fit_agq$log_marginal)
}

}
\references{
Pinheiro, J. C., & Bates, D. M. (1995). Approximations to the
log-likelihood function in the nonlinear mixed-effects model.
\emph{Journal of Computational and Graphical Statistics}, 4(1), 12–35.
}
\seealso{
\code{\link[=tulpa_laplace]{tulpa_laplace()}} for the joint-mode Laplace path
(handles multi-RE and spatial structure).
}
