% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics_sim.R
\name{test_uniformity}
\alias{test_uniformity}
\title{Test uniformity of PIT residuals}
\usage{
test_uniformity(
  object,
  observed = NULL,
  nsim = 250L,
  seed = 123L,
  plot = FALSE
)
}
\arguments{
\item{object}{A fitted model, a numeric vector of PIT residuals, or a
matrix of simulations}

\item{observed}{Observed data (if object is simulations matrix)}

\item{nsim}{Number of simulations (default 250)}

\item{seed}{Random seed (default 123)}

\item{plot}{If TRUE, draws a QQ plot}
}
\value{
An \code{htest} object (KS test result)
}
\description{
If the model is correct, PIT residuals should follow Uniform(0, 1).
Applies a Kolmogorov-Smirnov test against that null.
Equivalent to \code{DHARMa::testUniformity()}.
}
\examples{
set.seed(1)
test_uniformity(runif(50))   # PIT residuals supplied directly
\donttest{
set.seed(1)
d <- data.frame(x = rnorm(80))
d$y <- rpois(80, exp(0.3 + 0.5 * d$x))
fit <- tulpa(y ~ x, data = d, family = "poisson", mode = "laplace")
test_uniformity(fit, nsim = 50)
}
}
