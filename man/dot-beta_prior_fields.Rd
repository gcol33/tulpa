% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{.beta_prior_fields}
\alias{.beta_prior_fields}
\title{Resolve a supplied fixed-effect prior to its Gaussian \verb{(mean, sd)} fields}
\usage{
.beta_prior_fields(beta_prior)
}
\arguments{
\item{beta_prior}{A list (or \code{tulpa_prior} object) carrying \code{sd} and
optionally \code{mean}.}
}
\value{
\code{list(mean, sd)}, unrecycled.
}
\description{
The one place that decides whether a \code{beta_prior} argument can be expressed
as a Gaussian prior on the fixed effects at all. \code{\link[=.normalize_beta_prior]{.normalize_beta_prior()}}
(per-coefficient) and \code{\link[=.beta_prior_ridge_sd]{.beta_prior_ridge_sd()}} (scalar ridge) both resolve
through it and differ only in the shape they recycle the fields to, so a
prior one accepts is a prior the other accepts.
}
\details{
A prior with no \code{sd} is an input the fitters cannot express: substituting the
default for it would replace the user's modelling statement with a different
one and leave no trace on the posterior.
}
\keyword{internal}
