% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatiotemporal.R
\name{spatiotemporal_gp}
\alias{spatiotemporal_gp}
\title{Non-separable spatiotemporal GP}
\usage{
spatiotemporal_gp(
  coords,
  time_var,
  cov_space = c("exponential", "matern", "gaussian", "spherical"),
  cov_time = c("exponential", "matern", "gaussian"),
  nonsep_type = c("product", "sum", "gneiting", "cressie_huang"),
  nn = 15,
  shared = NULL
)
}
\arguments{
\item{coords}{A one-sided formula specifying coordinate columns (e.g.,
\code{~ lon + lat}), or a character vector of length 2.}

\item{time_var}{Name of the time variable in data.}

\item{cov_space}{Spatial covariance: \code{"exponential"} (default), \code{"matern"},
\code{"gaussian"}, or \code{"spherical"}.}

\item{cov_time}{Temporal covariance: \code{"exponential"} (default), \code{"matern"},
or \code{"gaussian"}.}

\item{nonsep_type}{Non-separability type:
\itemize{
\item \code{"product"}: \eqn{C_{st} = C_s \cdot C_t} (separable, for reference)
\item \code{"sum"}: \eqn{C_{st} = C_s + C_t}
\item \code{"gneiting"}: Gneiting (2002) non-separable class
\item \code{"cressie_huang"}: Cressie-Huang (1999) non-separable class
}}

\item{nn}{Number of nearest neighbors for NNGP approximation. Default 15.}

\item{shared}{Logical; if TRUE (default), effect enters both processes.}
}
\value{
Nothing: the call always signals an error.
}
\description{
Specify a non-separable Gaussian Process for spatiotemporal effects.
Unlike separable models where the covariance factors as \eqn{C_s \otimes C_t},
non-separable models allow for direct space-time interaction in the covariance.

No tulpa backend fits a joint space-time covariance, so this constructor
errors. A spatial GP alongside a temporal field is fitted directly by
\code{\link[=fit_st_nested]{fit_st_nested()}} (\code{spatial_type = "hsgp"} or \code{"nngp"}); \code{tulpa()}'s
formula front door does not yet route a continuous spatial field together
with a temporal field (it does for an areal one -- icar/bym2/car_proper --
via \code{tulpa(spatial = , temporal = )}; gcol33/tulpa#812).
}
\details{
The non-separable covariance functions allow for more flexible space-time
dependence:

\strong{Gneiting class:}
\deqn{C(h, u) = \frac{\sigma^2}{(a|u|^{2\alpha} + 1)^{\tau}} \exp\left(-\frac{c\|h\|^{2\gamma}}{(a|u|^{2\alpha} + 1)^{\beta\gamma}}\right)}

where h is spatial lag, u is temporal lag, and parameters control the
space-time interaction.

\strong{Cressie-Huang class:}
Constructed via Fourier transform methods to ensure positive definiteness.
}
\examples{
# No backend fits a joint space-time covariance, so the constructor refuses:
try(spatiotemporal_gp(~ lon + lat, "year", nonsep_type = "gneiting"))
}
\references{
Gneiting, T. (2002). Nonseparable, stationary covariance functions for
space-time data. Journal of the American Statistical Association, 97(458), 590-600.

Cressie, N., & Huang, H. C. (1999). Classes of nonseparable, spatio-temporal
stationary covariance functions. Journal of the American Statistical
Association, 94(448), 1330-1340.
}
