% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_gibbs.R
\name{tulpa_gibbs}
\alias{tulpa_gibbs}
\title{Fit via Polya-Gamma Gibbs sampler}
\usage{
tulpa_gibbs(
  y,
  n_trials,
  X,
  group,
  n_groups,
  family = "binomial",
  n_iter = 2000L,
  warmup = 1000L,
  prior_beta_sd = 10,
  prior_sigma_scale = 2.5,
  spatial = NULL,
  temporal = NULL,
  verbose = FALSE,
  n_threads = 1L
)
}
\arguments{
\item{y}{Response vector}

\item{n_trials}{Trial sizes (binomial)}

\item{X}{Design matrix}

\item{group}{Integer vector of group indices (1-based)}

\item{n_groups}{Number of groups}

\item{family}{Character: "binomial" or "neg_binomial_2"}

\item{n_iter}{Total iterations}

\item{warmup}{Warmup iterations}

\item{prior_beta_sd}{Prior SD for betas}

\item{prior_sigma_scale}{Prior scale for RE sigma}

\item{spatial}{Optional spatial spec. When supplied the fit routes to the
matching spatial Polya-Gamma Gibbs sampler via \code{\link[=dispatch_gibbs_spatial]{dispatch_gibbs_spatial()}};
\code{group}/\code{n_groups} are the iid random-effect block carried alongside the
field. The full areal + continuous family is available for
\code{family = "binomial"}; \code{family = "neg_binomial_2"} is backed by the areal
ICAR negbin sampler only. Supported \code{type}s:
\itemize{
\item areal -- \code{"icar"}, \code{"bym2"}, \code{"rsr"}: a list with \code{type}, \code{adjacency} and
a 1-based \code{spatial_idx} per observation (e.g.
\code{list(type = "icar", adjacency = W, spatial_idx = unit)}). \code{"rsr"} reuses
\code{spatial$rsr_projection} if present, else builds the unit-level projector
from the design.
\item continuous -- \code{"gp"}/\code{"nngp"} (a validated \code{\link[=spatial_gp]{spatial_gp()}} spec) and
\code{"multiscale_gp"} (a validated \code{\link[=spatial_multiscale]{spatial_multiscale()}} spec). These
samplers carry no observation->location map, so they require one
observation per unique location in coordinate order.
}}

\item{temporal}{Optional temporal spec: a validated \code{\link[=temporal_multiscale]{temporal_multiscale()}}
object. Routes to the multiscale temporal Polya-Gamma sampler via
\code{\link[=dispatch_gibbs_temporal]{dispatch_gibbs_temporal()}} (binomial only; RW1 trend + cyclic seasonal +
AR1/IID short-term). Cannot be combined with \code{spatial}.}

\item{verbose}{Print progress}

\item{n_threads}{Number of threads}
}
\value{
List with beta draws, RE draws, sigma_re draws (plus the spatial
field draws when \code{spatial} is supplied)
}
\description{
Public API for PG Gibbs sampling. Used by model packages for
binomial and negative binomial GLMMs.
}
\details{
For \code{family = "neg_binomial_2"} the Polya-Gamma weights are drawn
at the exact real shape \code{PG(y + r, eta)} and the dispersion \code{r} is
updated by a random-walk Metropolis-Hastings step on \code{log(r)} whose
stationary support is bounded to \code{r} in \verb{[0.1, 500]}; data favouring a
dispersion outside that range pile up at the boundary.
}
\examples{
set.seed(1)
G <- 20L; npg <- 15L; n <- G * npg
grp <- rep(seq_len(G), each = npg)
X <- cbind(1, rnorm(n))
b <- rnorm(G, 0, 0.6)
y <- rbinom(n, 1, plogis(X \%*\% c(-0.2, 0.5) + b[grp]))
\donttest{
fit <- tulpa_gibbs(y, rep(1L, n), X, grp, G, family = "binomial",
                   n_iter = 500L, warmup = 250L)
colMeans(fit$beta)
}
}
