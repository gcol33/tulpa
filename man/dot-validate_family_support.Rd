% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_loglik.R
\name{.validate_family_support}
\alias{.validate_family_support}
\title{Validate the response against a family's support.}
\usage{
.validate_family_support(family, y, n_trials = NULL, zi = FALSE)
}
\arguments{
\item{family}{Family identifier, canonical or aliased.}

\item{y}{Response vector, or \code{NULL} (a model with no response passes).}

\item{n_trials}{Binomial denominators, when known. Supplied, the binomial
rule is \verb{0 <= y <= n_trials}; absent, the response is the 0/1 form.}

\item{zi}{Whether a zero-inflation component is fitted alongside the family,
passed through to the count rule for the hurdle case.}
}
\description{
The one support rule set, shared by the \code{\link[=tulpa]{tulpa()}} front door and by the
engine fitters (\code{\link[=tulpa_laplace]{tulpa_laplace()}}, \code{\link[=tulpa_laplace_beta]{tulpa_laplace_beta()}},
\code{\link[=tulpa_nuts_beta]{tulpa_nuts_beta()}}). Holding it in a single function is what keeps a
boundary response rejected with the same message wherever it enters: a
per-fitter copy is only as good as the set of fitters that carry it, and
\code{tulpa()} reaches beta-with-RE through a sampler that carried none.
}
\details{
Counts delegate to \code{\link[=.validate_family_counts]{.validate_family_counts()}}; the continuous families and
the binomial denominator add their rules here.
}
\keyword{internal}
