% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tgmrf_imh.R
\name{tulpa_tgmrf}
\alias{tulpa_tgmrf}
\title{Fit a custom tgmrf latent block}
\usage{
tulpa_tgmrf(
  y,
  n_trials,
  X,
  block,
  family = "binomial",
  phi = 1,
  re_idx = NULL,
  n_re_groups = 0L,
  sigma_re = 1,
  mode = c("imh", "nuts", "vi", "nuts_joint"),
  control = list(),
  ...
)
}
\arguments{
\item{y, n_trials, X}{Response, binomial trial counts (or \code{NULL}), and the
fixed-effect design matrix.}

\item{block}{A \code{\link[=tgmrf]{tgmrf()}} latent block.}

\item{family, phi}{Observation family and its dispersion.}

\item{re_idx, n_re_groups, sigma_re}{Optional scalar random-intercept structure.}

\item{mode}{Inference engine: \code{"imh"}, \code{"nuts"}, \code{"vi"}, or \code{"nuts_joint"}.}

\item{control}{A list of numerical / tuning knobs for the chosen \code{mode}
(e.g. \code{n_iter}, \code{warmup}, \code{thin}, \code{scale} for \code{"imh"}; \code{epsilon},
\code{max_depth}, \code{target_accept} for the NUTS modes; \code{n_draws}, \code{max_lbfgs},
\code{lbfgs_tol} for \code{"vi"}; plus the shared \code{pilot_axis_points}, \code{max_iter},
\code{tol}, \code{n_threads}, \code{verbose}). An unknown knob for the chosen \code{mode}
errors rather than being silently ignored.}

\item{...}{Individual \code{control} knobs may also be passed directly by name;
they are merged into \code{control} (a named argument here wins over the same
name inside \code{control}).}
}
\value{
A \code{tulpa_tgmrf} / \code{tulpa_fit} object; \verb{$backend} and \verb{$mode} record
the engine used.
}
\description{
One front door for inference over a user-defined \code{\link[=tgmrf]{tgmrf()}} latent block's
hyperparameter vector \code{theta}. \code{mode} selects the inference engine, all of
which share the same Laplace body for \verb{(beta, z) | theta}:
\itemize{
\item \code{"imh"} (default) -- Tier-1 exact MCMC: the Laplace body plus an
independence-Metropolis bias correction over \code{theta} (the
"Laplace + MH debias" composition).
\item \code{"nuts"} -- Tier-1 exact MCMC: NUTS over the marginal \code{theta} posterior.
\item \code{"vi"} -- Tier-2 structured: a single-path Pathfinder Gaussian fit of the
\code{theta} posterior (no bias correction).
\item \code{"nuts_joint"} -- Tier-1 exact MCMC sampling the FULL joint
\verb{(beta, z, theta)} rather than the Laplace-marginalized \code{theta} (requires a
C++-backend block, see \code{\link[=tgmrf_cpp]{tgmrf_cpp()}}).
}

The inference method is an argument, not a parallel verb.
}
\seealso{
\code{\link[=tgmrf]{tgmrf()}} for the block, \code{\link[=tgmrf_cpp]{tgmrf_cpp()}} for the compiled-block form.
}
