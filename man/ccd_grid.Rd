% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ccd_grid.R
\name{ccd_grid}
\alias{ccd_grid}
\title{Central Composite Design (CCD) grid for nested-Laplace integration}
\usage{
ccd_grid(k, f_0 = sqrt(k))
}
\arguments{
\item{k}{Number of hyperparameters (length of θ). Must be ≥ 1.}

\item{f_0}{Radius of the design sphere (default \eqn{\sqrt{k}}). Larger
\code{f_0} spreads points further out; smaller concentrates near the
centre. INLA's default scales like \eqn{\sqrt{k}}.}
}
\value{
A list with components:
\itemize{
\item \code{z}: numeric matrix \verb{[n_points × k]} of standardised hyperparameter
coordinates.
\item \code{n_points}: integer; total grid size.
\item \code{kind}: character vector labelling each point as
\code{"center"}, \code{"axial"}, or \code{"factorial"}.
\item \code{f_0}: the sphere radius used.
}
}
\description{
Produces a structured set of standardised hyperparameter points
\eqn{z \in \mathbb{R}^k} for use as integration nodes in a nested
Laplace approximation when \eqn{k \ge 3}. CCD scales much better than
the full tensor \code{expand.grid()} used by the 1D and 2D backends:
a CCD has \eqn{1 + 2k + 2^{k - q}} points (1 centre, 2k axial,
\eqn{2^{k - q}} factorial), versus \eqn{m^k} for an \code{m}-per-axis
tensor product.

Point layout (with centre+axial+factorial scaling \eqn{f_0}):
\itemize{
\item 1 centre point at the origin;
\item 2k axial points at \eqn{\pm f_0} along each coordinate axis;
\item \eqn{2^{k - q}} factorial points at corners of the hypercube,
scaled to lie on a sphere of radius \eqn{f_0}.
}

For \eqn{k \le 6} the factorial portion is the full \eqn{2^k}
design. For \eqn{k \ge 7} a half-fraction (\eqn{q = 1}) using the
defining word \eqn{x_1 \cdots x_k} keeps the point count reasonable
while preserving Resolution V.

Used by \code{\link[=nested_laplace]{nested_laplace()}} for higher-dimensional hyperparameter
blocks. The standardised z-coordinates are mapped to physical
hyperparameters \eqn{\theta} via \code{\link[=ccd_to_theta]{ccd_to_theta()}}.
}
\seealso{
\code{\link[=ccd_to_theta]{ccd_to_theta()}} to map z-coordinates to physical θ.
}
\keyword{internal}
