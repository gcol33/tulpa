% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_modes.R
\name{.finalize_fit}
\alias{.finalize_fit}
\title{Attach the standard \code{tulpa_fit} contract to a fitter's result.}
\usage{
.finalize_fit(
  fit,
  backend = NULL,
  draws_kind = NULL,
  n_fixed = NULL,
  fixed_names = NULL,
  param_names = NULL,
  extra_class = NULL
)
}
\arguments{
\item{fit}{The fitter result (a list); returned unchanged if not a list.}

\item{backend}{Backend key (sets \verb{$backend}; supplies the default
\code{draws_kind} via the registry \code{emits} property when it is a registry key).}

\item{draws_kind}{Explicit \code{"chain"} / \code{"iid"} / \code{"point"} tag; used when the
backend is absent from \code{BACKEND_REGISTRY} or to override the registry.}

\item{n_fixed, fixed_names, param_names}{Fixed-effect layout, each filled only
when the fitter left it unset.}

\item{extra_class}{Subclass(es) to prepend before \code{tulpa_fit}.}
}
\value{
The enriched fit, classed \code{c(extra_class, ..., "tulpa_fit")}.
}
\description{
Every exported front-door fitter routes its return value through this helper,
so a directly-called fitter and a \code{tulpa()}-dispatched one yield the same
enriched object: the \code{tulpa_fit} class (so the generic S3 methods --
\code{coef} / \code{summary} / \code{vcov} / \code{confint} / \code{tidy} / \code{glance} / \code{ranef} --
dispatch), the fixed-effect layout (\code{n_fixed} / \code{fixed_names} / \code{param_names},
which the \code{.fit_fixed_table} summary path reads), and an explicit
posterior-provenance tag (\code{draws_kind}) the chain-vs-iid diagnostic gate
(\code{.tulpa_draws_kind()}) reads to decide whether Rhat/ESS apply. Each field is
filled only when the fitter did not already set it, so a fitter that knows
better wins and the helper is idempotent under \code{tulpa_dispatch()}.
}
\details{
\code{draws_kind} precedence is: a value already on the fit, then the explicit
\code{draws_kind} argument, then the registry \code{emits} property for \code{backend}.
Backends that are not registry keys (the \verb{tgmrf_*} fitters) must pass
\code{draws_kind} explicitly, since their \code{emits} cannot be looked up.
}
\keyword{internal}
