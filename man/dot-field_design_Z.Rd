% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/marginal_se_spatial.R
\name{.field_design_Z}
\alias{.field_design_Z}
\title{Build a length-n_obs -> length-n_units indicator design matrix Z.}
\usage{
.field_design_Z(obs_to_unit, n_units, n_obs)
}
\arguments{
\item{obs_to_unit}{Per-observation unit index, or empty for one observation
per unit.}
}
\description{
The latent field lives on units -- unique coordinates for an NNGP field,
adjacency units for an areal one -- and each observation belongs to exactly
one, so \code{Z[i, j]} = 1 iff observation i sits on unit j.
}
\details{
The map is passed in rather than read off the spec, because the two specs
that reach here name it differently: a GP spec carries \code{obs_to_loc}, an areal
spec carries \code{spatial_idx}. Reading one name served only one caller; for the
other the field was absent, the map silently became \code{seq_len(n_obs)}, and the
column index ran past \code{n_units} as soon as a location carried more than one
observation.
}
\keyword{internal}
