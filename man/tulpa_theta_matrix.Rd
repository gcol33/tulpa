% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_laplace_moments.R
\name{tulpa_theta_matrix}
\alias{tulpa_theta_matrix}
\title{A fit's outer grid as a named matrix}
\usage{
tulpa_theta_matrix(res)
}
\arguments{
\item{res}{A \code{tulpa_fit} object (or any list carrying \code{theta_grid} and
\code{theta_names}).}
}
\value{
\code{res$theta_grid} as an \verb{[n_cells x n_axes]} matrix with axis names
as column names, or \code{NULL} if the fit has no grid.
}
\description{
Coerces a nested-Laplace fit's \code{theta_grid} to matrix form with named axis
columns. A single-axis grid is stored on the fit as a bare vector named by
\code{theta_names}; every downstream reader that keys an axis by name needs the
named matrix form, so this is the one place that coercion happens.
Intended for reconstructing a fit's outer-grid posterior cell weights (see
\code{\link[=tulpa_grid_log_quad]{tulpa_grid_log_quad()}}, \code{\link[=tulpa_normalise_weights_safe]{tulpa_normalise_weights_safe()}}) for a fit or
path whose driver doesn't already carry \code{fit$log_quad}.
}
\seealso{
\code{\link[=tulpa_grid_log_quad]{tulpa_grid_log_quad()}}, \code{\link[=tulpa_normalise_weights_safe]{tulpa_normalise_weights_safe()}}
}
