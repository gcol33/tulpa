% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/psis.R
\name{tulpa_psis}
\alias{tulpa_psis}
\title{Pareto-smoothed importance sampling}
\usage{
tulpa_psis(log_ratios, tail_points = NULL)
}
\arguments{
\item{log_ratios}{Numeric vector of (unnormalized) log importance ratios
\verb{log p_target(x) - log q_proposal(x)} evaluated at draws \code{x ~ q}.}

\item{tail_points}{Number of upper-tail order statistics for the
generalized-Pareto fit, or \code{NULL} (default) for the automatic PSIS rule
\code{ceil(min(0.2 * S, 3 * sqrt(S)))}. An explicit value is an expert
tail-threshold control, capped at \code{floor(0.2 * S)} (with a warning) so the
fit stays an extreme tail; it is NOT a precision knob (raise the draw count
for a tighter shape estimate).}
}
\value{
A list with \code{pareto_k} (the tail shape, \code{NA} if the sample is too
small to fit), \code{is_ess} (importance-sampling effective sample size,
\code{1 / sum(w^2)} on the normalized smoothed weights), \code{log_weights}
(the normalized smoothed log weights), and \code{tail_len} (the tail size used).
}
\description{
Smooths a set of importance ratios by replacing the largest weights with the
order statistics of a generalized-Pareto fit to their upper tail, and
returns the Pareto shape diagnostic \code{pareto_k} together with the smoothed
(normalized) log weights and their importance-sampling effective sample
size. \code{pareto_k} estimates the number of finite moments of the raw weight
distribution: \verb{< 0.5} is good (finite variance). The usable upper boundary is
sample-size dependent, \code{min(1 - 1/log10(S), 0.7)} for \code{S} draws (Vehtari et al.
2024): about 0.565 at \code{S = 200}, reaching the 0.7 cap only past \code{S} ~ 2154.
Above it the proposal cannot be reliably corrected to the target.
}
\examples{
set.seed(1)
# Well-behaved importance ratios: k-hat is small.
ps <- tulpa_psis(rnorm(2000))
ps$pareto_k
ps$is_ess
}
\references{
Vehtari, Simpson, Gelman, Yao & Gabry (2024). Pareto smoothed
importance sampling. \emph{JMLR} 25(72):1-58.
}
\seealso{
\code{\link[=mcmc_diagnostics]{mcmc_diagnostics()}} for the MCMC-chain counterpart.
}
