% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{.validate_glm_design}
\alias{.validate_glm_design}
\title{Validate a GLM design bundle (\code{y}, \code{X}, \code{n_trials}) at a fitter's front door.}
\usage{
.validate_glm_design(y, X, n_trials, where)
}
\arguments{
\item{y, X}{Response vector and design matrix.}

\item{n_trials}{Binomial denominators, or \code{NULL} (defaults to 1 per row).}

\item{where}{Caller name for the error message.}
}
\value{
List \code{list(N, n_trials)} with \code{n_trials} coerced to a length-\code{N}
integer vector.
}
\description{
Shared by the flagship drivers (\code{tulpa_nested_laplace()}, \code{tulpa_gibbs()}, the
\code{re_cov} fitters) so \code{nrow(X) == length(y)} and \code{length(n_trials) == length(y)} are enforced in one place -- otherwise a mismatched \code{n_trials}
(\code{as.integer(NULL)} -> \code{integer(0)}) reaches the C++ kernel silently.
}
\keyword{internal}
