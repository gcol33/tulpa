% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/priors.R
\name{priors_default}
\alias{priors_default}
\title{Show default priors for a tulpa family}
\usage{
priors_default(family = NULL, spatial = FALSE, temporal = FALSE)
}
\arguments{
\item{family}{\code{NULL} (default) for the general printout, or a
\code{tulpa_family} object -- a model package's family constructor, such as a
tulpaRatio ratio family, or one built with \code{\link[=tulpa_family]{tulpa_family()}} -- for the
parameters that family carries. A family NAME (\code{"poisson"}) is not a
\code{tulpa_family} and is refused; the built-in \code{\link[=family_names]{family_names()}} share the
general printout.}

\item{spatial}{Logical; if TRUE, include spatial priors. Default FALSE.}

\item{temporal}{Logical; if TRUE, include temporal priors. Default FALSE.}
}
\value{
Invisibly returns a \code{tulpa_priors} object with the defaults.
Primarily called for its side effect of printing.
}
\description{
Display the default \code{\link[=tulpa_priors]{tulpa_priors()}} specification: the priors the
simulation tools (\code{\link[=prior_predict]{prior_predict()}}, \code{\link[=tulpa_simulate]{tulpa_simulate()}}) draw from, and a
starting point for customizing them. \code{\link[=tulpa]{tulpa()}} does not read a
\code{tulpa_priors} object; the no-family printout closes with the defaults a
\code{tulpa()} fit does use, which differ for the random-effect SDs (see
\code{\link[=tulpa_priors]{tulpa_priors()}}).
}
\details{
The family-specific overdispersion lines name the ratio families a model
package registers (\code{negbin_negbin}, \code{negbin_gamma}, \code{poisson_gamma},
\code{gamma_gamma}, \code{beta_binomial}); they are not among tulpa's own
\code{\link[=family_names]{family_names()}}.

Default priors in \code{\link[=tulpa_priors]{tulpa_priors()}} follow these principles:
\itemize{
\item \strong{Fixed effects (beta)}: Normal(0, 2.5) - weakly informative, allows
coefficients roughly in [-5, 5] on the link scale.
\item \strong{Random effect SD (sigma)}: PC prior with P(sigma > 1) = 0.01 - favors
simpler models with smaller variance components.
\item \strong{Overdispersion (phi)}: PC prior with P(phi > 10) = 0.01 on the NB2 size
\code{phi} (larger \code{phi} is less overdispersion; \code{phi -> Inf} is the Poisson
limit). The default keeps \code{phi} finite, allowing overdispersion while
penalising extreme values.
\item \strong{Temporal correlation (rho)}: Beta(2, 2) - symmetric prior centered
at 0.5, appropriate for AR(1) correlation.
\item \strong{Spatial mixing (rho_spatial)}: Beta(1, 1) = Uniform(0, 1) - no
prior preference for structured vs. unstructured spatial variation.
}
}
\examples{
# Defaults for all families (no family argument)
priors_default()

# Family-specific defaults take a tulpa_family object. Model packages
# (e.g. tulpaRatio) register rich families; a minimal one is enough here.
fam <- tulpa_family(
  name = "poisson_gamma",
  simulate_fn = function(eta, params, n_obs, ...) rpois(n_obs, exp(eta[[1]]))
)
priors_default(fam)

# Including spatial parameters
priors_default(fam, spatial = TRUE)

# Use as a starting point for customization
my_priors <- priors_default(fam)
my_priors$beta <- prior_normal(0, 1)  # Tighter prior on fixed effects

}
\seealso{
\code{\link[=tulpa_priors]{tulpa_priors()}} for creating custom priors
}
