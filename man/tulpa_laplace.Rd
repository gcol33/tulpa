% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{tulpa_laplace}
\alias{tulpa_laplace}
\title{Fit a model via Laplace approximation}
\usage{
tulpa_laplace(
  y,
  n_trials,
  X,
  re_list = list(),
  family = "binomial",
  phi = 1,
  spatial = NULL,
  weights = NULL,
  offset = NULL,
  max_iter = 100L,
  tol = 1e-06,
  n_threads = 1L,
  return_hessian = TRUE
)
}
\arguments{
\item{y}{Response vector (integer for binomial/poisson/negbin, numeric for gaussian)}

\item{n_trials}{Trial sizes (integer vector, used for binomial only)}

\item{X}{Fixed-effects design matrix}

\item{re_list}{List of RE specifications. Each element is a list with:
\itemize{
\item \code{idx}: integer vector of group indices (1-based)
\item \code{n_groups}: number of groups
\item \code{sigma}: RE standard deviation
\item \code{Z}: optional slope design matrix (n_obs x n_coefs). If NULL, intercept-only.
}}

\item{family}{Character: \code{"binomial"}, \code{"poisson"}, \code{"neg_binomial_2"}, \code{"gaussian"}}

\item{phi}{Dispersion parameter (neg_binomial_2 and gamma only)}

\item{spatial}{Optional spatial specification (tulpa_spatial object)}

\item{max_iter}{Maximum Newton iterations (default 100)}

\item{tol}{Convergence tolerance (default 1e-6)}

\item{n_threads}{Number of threads (default 1)}

\item{return_hessian}{Logical: return the fixed-effect Hessian block? (default TRUE)}
}
\value{
A list with:
\itemize{
\item \code{mode}: full mode vector (beta, then RE values per term)
\item \code{log_marginal}: Laplace-approximated log-marginal likelihood
\item \code{n_iter}: number of Newton iterations
\item \code{converged}: logical
\item \code{log_det_Q}: log-determinant of the Hessian
\item \code{H_beta}: fixed-effect block of the Hessian (if return_hessian = TRUE)
}
}
\description{
General-purpose Laplace approximation for latent Gaussian models.
Finds the mode of the latent field (beta + random effects) and
returns the Laplace-approximated marginal likelihood.

This is the public API for model packages (tulpaGlmm, tulpaObs, etc.)
to call tulpa's Laplace engine.
}
