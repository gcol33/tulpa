% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_laplace.R
\name{tulpa_laplace}
\alias{tulpa_laplace}
\title{Fit a model via Laplace approximation}
\usage{
tulpa_laplace(
  y,
  n_trials,
  X,
  re_list = list(),
  family = "binomial",
  phi = 1,
  phi2 = NULL,
  spatial = NULL,
  weights = NULL,
  offset = NULL,
  max_iter = 100L,
  tol = 1e-06,
  n_threads = 1L,
  return_hessian = TRUE,
  beta_prior = NULL,
  return_re_cov = FALSE
)
}
\arguments{
\item{y}{Response vector (integer for binomial/poisson/negbin, numeric for gaussian)}

\item{n_trials}{Trial sizes (integer vector, used for binomial only)}

\item{X}{Fixed-effects design matrix}

\item{re_list}{List of RE specifications. Each element is a list with:
\itemize{
\item \code{idx}: integer vector of group indices (1-based)
\item \code{n_groups}: number of groups
\item \code{n_coefs}: coefficients per group (1 = intercept-only, >1 = random slopes)
\item \code{sigma}: per-coefficient RE standard deviation(s), a diagonal covariance
(uncorrelated, lme4 \code{(x || g)}). A scalar is recycled to \code{n_coefs}.
\item \code{Z}: slope design matrix (n_obs x n_coefs) when \code{n_coefs > 1}; \code{NULL}
means intercept-only.
\item \code{L} / \code{cov}: optional \verb{n_coefs x n_coefs} covariance for a \emph{correlated}
term (lme4 \code{(1 + x | g)}) -- supply either a lower-triangular Cholesky
factor \code{L} (covariance = \verb{L L'}) or the covariance matrix \code{cov}. When
present these take precedence over \code{sigma}; the off-diagonal enters both
the joint Hessian (mode finding) and the marginal fixed-effect SE.
}}

\item{family}{Character: \code{"binomial"}, \code{"poisson"}, \code{"neg_binomial_2"}, \code{"gaussian"}}

\item{phi}{Dispersion parameter. For \code{gaussian} / \code{lognormal} this is the
residual VARIANCE (matching the R-side family registry and \code{\link[=tulpa]{tulpa()}});
the SD-parameterized compiled kernels receive \code{sqrt(phi)} internally.
For \code{neg_binomial_2} the size, \code{beta} the precision, \code{t} the scale.}

\item{phi2}{Optional second dispersion: the Student-t degrees of freedom
(\code{family = "t"}; default 4 when \code{NULL}). Non-spatial path only.}

\item{spatial}{Optional spatial specification (tulpa_spatial object)}

\item{weights}{Optional observation weights (numeric vector, length \code{length(y)}).
Scales each observation's likelihood contribution. \code{NULL} (default) uses 1.}

\item{offset}{Optional observation-level offset on the linear predictor
(numeric vector, length \code{length(y)}). \code{NULL} (default) uses 0.}

\item{max_iter}{Maximum Newton iterations (default 100)}

\item{tol}{Convergence tolerance (default 1e-6)}

\item{n_threads}{Number of threads (default 1)}

\item{return_hessian}{Logical: return the fixed-effect Hessian block? (default TRUE)}

\item{beta_prior}{Optional Gaussian prior on the fixed effects. \code{NULL}
(default) keeps the weak built-in prior \code{beta ~ N(0, 100^2)}. Otherwise a
list with element \code{sd} (prior standard deviation, required) and optional
\code{mean} (prior mean, default 0). Each may be a scalar (applied to every
coefficient) or a length-\code{ncol(X)} vector. Adds
\code{sum((beta - mean)^2 / (2 * sd^2))} to the negative log-posterior, so the
mode is the penalized (MAP) estimate. A coefficient's \code{sd} may be \code{+Inf},
which sets its precision to 0 (no penalty on that coefficient). Not
supported on the spatial path.}

\item{return_re_cov}{If \code{TRUE}, additionally return per-group marginal
posterior covariance blocks \code{Cov(u_g | y, Sigma)} -- one
\verb{n_coefs x n_coefs} matrix per (RE term, group), with the fixed effects
and other groups marginalized out (each block is a diagonal block of the
full inverse Hessian, not the inverse of a diagonal block). Used by the EM
M-step for a full random-effect covariance. Non-spatial multi-RE path only.}
}
\value{
A list with:
\itemize{
\item \code{mode}: full mode vector (beta, then RE values per term)
\item \code{log_marginal}: Laplace-approximated log-marginal likelihood
\item \code{n_iter}: number of Newton iterations
\item \code{converged}: logical
\item \code{log_det_Q}: log-determinant of the Hessian
\item \code{H_beta}: fixed-effect block of the Hessian (if return_hessian = TRUE)
\item \code{cov_blocks}: list of per-group posterior covariance matrices, one per
(RE term, group) in term-major then group order (if return_re_cov = TRUE)
}
}
\description{
General-purpose Laplace approximation for latent Gaussian models.
Finds the mode of the latent field (beta + random effects) and
returns the Laplace-approximated marginal likelihood.

This is the public API for model packages (tulpaGlmm, tulpaObs, etc.)
to call tulpa's Laplace engine. As the low-level engine entry point (not a
front-door fitter), it keeps its numerical controls (\code{max_iter}, \code{tol},
\code{n_threads}, \code{return_hessian}) inline in the signature rather than in a
\code{control} list, so callers assembling many solves pass them positionally.
}
\examples{
set.seed(1)
n <- 200L
X <- cbind(1, rnorm(n))
eta <- X \%*\% c(-0.3, 0.8)
y <- rbinom(n, 1, plogis(eta))
fit <- tulpa_laplace(y, rep(1L, n), X, family = "binomial")
fit$mode          # posterior mode of the fixed effects
}
