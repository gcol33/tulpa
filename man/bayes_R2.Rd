% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayes_r2.R
\name{bayes_R2}
\alias{bayes_R2}
\alias{bayes_R2.tulpa_fit}
\title{Bayesian R-squared}
\usage{
bayes_R2(object, ...)

\method{bayes_R2}{tulpa_fit}(
  object,
  ndraws = NULL,
  summary = TRUE,
  probs = c(0.025, 0.975),
  seed = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{tulpa_fit} object from \code{\link[=tulpa]{tulpa()}}.}

\item{...}{Passed to methods.}

\item{ndraws}{Number of posterior draws to use. Defaults to all stored
draws, or 400 on the draw-free Laplace tier.}

\item{summary}{Summarize the per-draw values (default \code{TRUE}).}

\item{probs}{Quantiles reported by the summary (default 2.5\% / 97.5\%).}

\item{seed}{Optional integer seed (RNG state is restored on exit), used by
the Gaussian fixed-effect sampling on draw-free fits.}
}
\value{
With \code{summary = TRUE} (default) a one-row data frame with
\code{estimate} (posterior median), \code{std.error}, and the \code{probs} quantiles;
with \code{summary = FALSE} the vector of per-draw R^2 values.
}
\description{
Per posterior draw \code{s}, \code{R2_s = Var_i(mu_si) / (Var_i(mu_si) + Var_res_s)},
where \code{mu_si} are the response-scale fitted means and \code{Var_res_s} is the
family's residual variance averaged over observations (Gelman et al. 2019).
The linear predictor is rebuilt per draw exactly as in
\code{\link[=posterior_predict]{posterior_predict()}} (fixed effects + random effects + offset at the
training data).
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(x = rnorm(200))
d$y <- rnorm(200, 2 * d$x, 1)
fit <- tulpa(y ~ x, data = d, family = "gaussian", mode = "laplace", phi = 1)
bayes_R2(fit)
}
}
\references{
Gelman, Goodrich, Gabry & Vehtari (2019). R-squared for Bayesian
regression models. \emph{The American Statistician} 73(3):307-309.
}
