% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference_modes.R
\docType{data}
\name{BACKEND_REGISTRY}
\alias{BACKEND_REGISTRY}
\title{Backend registry -- single source of truth for the inference backends.}
\format{
An object of class \code{list} of length 16.
}
\usage{
BACKEND_REGISTRY
}
\description{
One entry per backend. Adding a backend is a single entry here; tier
membership, family support, the input contract, and R-level reachability
all derive from this list. Replaces the previously scattered per-tier
\code{backends} vectors and the separate \code{BACKEND_FAMILY_SUPPORT} table.

Fields:
\itemize{
\item \code{tier}     -- tier key (\code{"exact"}, \code{"structured"}, \code{"optimized"}).
\item \code{input}    -- the input contract the backend's fitter consumes:
\itemize{
\item \code{"design"}    : design-matrix bundle (\code{y}, \code{n_trials}, \code{X}, RE structure).
\item \code{"logpost"}   : a \code{log_posterior(theta)} closure plus dimension/init.
\item \code{"modeldata"} : a tulpa \code{ModelData} / \code{LikelihoodSpec} (C-ABI NUTS path).
\item \code{"nested"}    : a design-matrix bundle plus one or more latent prior
blocks (\code{latent(tgmrf(...))}), integrated over the block
hyperparameters by the nested-Laplace driver.
}
\item \code{fitter}   -- name of the R function implementing the backend, or \code{NULL}
when only a C++ kernel exists with no R entry point yet. Stored as a
\emph{string} (not the function object) so the registry is independent of
source-load order; resolved lazily via \code{\link[=resolve_backend_fitter]{resolve_backend_fitter()}}.
\code{NULL} => not selectable from R; dispatch fails loudly.
\item \code{families} -- character vector of supported family identifiers, or \code{NULL}
for an unrestricted backend.
\item \code{cabi}     -- the registered C-ABI callable backing the backend (the symbol
a model package reaches via \code{LinkingTo: tulpa}, and the one an R wrapper
would call), or \code{NULL}.
\item \code{note}     -- optional human-readable note.
}

Family identity (for \code{families}) is checked against \code{family$name},
\code{family$distribution}, and \code{family$numerator$distribution} (numdenom-style
ratio families nest a per-process distribution).
}
\keyword{internal}
