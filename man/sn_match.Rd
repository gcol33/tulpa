% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/skew_normal.R
\name{sn_match}
\alias{sn_match}
\title{Match three cumulants to a skew-normal parameterisation}
\usage{
sn_match(mu, sigma, gamma)
}
\arguments{
\item{mu}{Posterior mean (numeric, length 1).}

\item{sigma}{Posterior standard deviation (positive numeric, length 1).}

\item{gamma}{Posterior skewness (numeric, length 1).}
}
\value{
Named list with elements \code{xi}, \code{omega}, \code{alpha};
or \code{NULL} if \code{|gamma| >= .SN_GAMMA_MAX}.
}
\description{
Inverts the skew-normal moment formulas to convert
\code{(mu, sigma, gamma)} into \code{(xi, omega, alpha)} parameters.
Returns \code{NULL} with a warning when \code{|gamma|} exceeds the
skew-normal ceiling (~0.9953) -- in that regime the third cumulant
cannot be matched by any skew-normal and the caller should fall back
to direct-quadrature quantiles.
}
\references{
Azzalini, A. (1985). A class of distributions which
includes the normal ones. \emph{Scand. J. Statist.} \strong{12}: 171-178.
}
