% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula.R
\name{tulpa_is_spatial_bar}
\alias{tulpa_is_spatial_bar}
\title{Recognize an inline varying-coefficient bar}
\usage{
tulpa_is_spatial_bar(x)
}
\arguments{
\item{x}{A one-sided formula (e.g. \code{~ 1 + w || node}) or the bar language
object itself (\code{quote(1 + w || node)}).}
}
\value{
A single logical: \code{TRUE} when \code{x} is (or wraps) a \code{|} / \code{||} bar,
\code{FALSE} otherwise (a plain formula such as \code{~ 1 + w}, a non-bar term, or a
non-formula / non-language input).
}
\description{
Test whether a one-sided formula carries a single varying-coefficient
grouping bar of the form \code{~ 1 + w || node} (independent fields, \code{||}) or
\code{~ 1 + w | node} (correlated fields, \code{|}). This is the same grammar
\code{\link[=spatial]{spatial()}} and the inline \code{temporal()} field constructor accept, and the
grammar \code{\link[=tulpa_bar_field_specs]{tulpa_bar_field_specs()}} expands. A downstream package can use it to
branch on "is this term a spatial / temporal varying-coefficient bar?" before
calling \code{\link[=tulpa_bar_field_specs]{tulpa_bar_field_specs()}}.
}
\examples{
tulpa_is_spatial_bar(~ 1 + w || cell)   # TRUE
tulpa_is_spatial_bar(~ 1 + w | cell)    # TRUE (correlated)
tulpa_is_spatial_bar(~ 1 + w)           # FALSE (no bar)

}
\seealso{
\code{\link[=tulpa_bar_field_specs]{tulpa_bar_field_specs()}} for expanding the bar into per-column
field specs.
}
