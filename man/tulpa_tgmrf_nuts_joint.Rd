% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tgmrf_nuts_joint.R
\name{tulpa_tgmrf_nuts_joint}
\alias{tulpa_tgmrf_nuts_joint}
\title{Joint NUTS over (beta, z, theta) for a tgmrf C++-backend block}
\usage{
tulpa_tgmrf_nuts_joint(
  y,
  n_trials,
  X,
  block,
  family = "binomial",
  phi = 1,
  re_idx = NULL,
  n_re_groups = 0L,
  sigma_re = 1,
  n_iter = 500L,
  warmup = n_iter\%/\%2L,
  epsilon = NULL,
  max_depth = 6L,
  target_accept = 0.8,
  pilot_axis_points = 5L,
  fd_step = 0.001,
  max_iter = 50L,
  tol = 1e-07,
  n_threads = 1L,
  verbose = FALSE,
  debug_gradient_check = FALSE,
  seed = NULL
)
}
\arguments{
\item{y, n_trials, X}{Response, trials per obs (1L vector for non-binomial),
and fixed-effects design matrix -- same arguments as
\code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}}.}

\item{block}{A tgmrf block with \code{backend == "cpp"}. R-closure blocks are
rejected with an error pointing to \code{\link[=tgmrf_cpp]{tgmrf_cpp()}} or
\code{\link[=tulpa_tgmrf_nuts]{tulpa_tgmrf_nuts()}}.}

\item{family, phi}{Likelihood family and dispersion.}

\item{re_idx, n_re_groups, sigma_re}{Optional RE wiring (forwarded to
the inner Laplace).}

\item{n_iter}{Total number of NUTS iterations (including warmup). Default
\code{500L}.}

\item{warmup}{Number of warmup iterations used for dual-averaging step-size
adaptation and then discarded. Must satisfy \verb{1 <= warmup < n_iter}. Default
\code{n_iter \%/\% 2L}.}

\item{epsilon}{Initial leapfrog step size. Default is \code{0.2 / sqrt(theta_dim)}.}

\item{max_depth}{Maximum NUTS tree depth (\code{2^max_depth} leapfrog steps
per draw at the limit). Default 6.}

\item{target_accept}{Target acceptance for dual-averaging step-size
adaptation. Default 0.8 (higher than plain NUTS's 0.65 because joint
dimensionality is large).}

\item{pilot_axis_points}{Per-axis grid resolution for the pilot fit
(default 5).}

\item{fd_step}{Central finite-difference step on theta for
\eqn{\partial_m Q}, \eqn{\partial_m \mu}, \eqn{d\log p(\theta)/d\theta_m}.
Default 1e-3.}

\item{max_iter, tol}{Inner-Newton budget per proposal.}

\item{n_threads}{OpenMP threads inside each inner Laplace solve.}

\item{verbose}{Forwarded to \code{\link[=imh_laplace]{imh_laplace()}}.}

\item{debug_gradient_check}{If \code{TRUE}, runs a one-shot numerical-gradient
sanity check at the initial \eqn{q} and prints the max relative error
across all \code{D = p + n_lat + theta_dim} components.}

\item{seed}{Integer seed for the C++ RNG. Default uses \code{sample.int(.Machine$integer.max, 1L)}.}
}
\value{
A list with class \code{c("tulpa_tgmrf_nuts_joint", "tulpa_fit")}:
\itemize{
\item \code{draws_beta}, \code{draws_z}, \code{draws_theta} -- post-warmup draws.
\item \code{means_beta}, \code{means_z}, \code{means_theta},
\code{sds_beta}, \code{sds_z}, \code{sds_theta} -- column-wise moments.
\item \code{mean_accept}, \code{tree_depth}, \code{divergent}, \code{epsilon} --
sampler diagnostics.
\item \code{pilot}, \code{mode_beta}, \code{mode_z}, \code{mode_theta}, \code{mass_diag} --
pilot Laplace outputs used for init / mass matrix.
\item \code{inference_mode = "exact"}, \code{inference_tier = 1L},
\code{backend = "tgmrf_nuts_joint"}.
}
}
\description{
No-U-Turn Sampler (Hoffman & Gelman 2014) running directly on the full
joint vector \eqn{(\beta, z, \theta)} in C++. Unlike
\code{\link[=tulpa_tgmrf_nuts]{tulpa_tgmrf_nuts()}}, which targets the marginal posterior
\eqn{p(\theta\mid y)} via a finite-difference gradient on the inner
Laplace log-marginal, this sampler integrates the joint posterior in one
Hamiltonian system using closed-form gradients for \eqn{(\beta, z)} and a
central finite-difference gradient on \eqn{(Q, \mu, \log p(\theta))} with
respect to \eqn{\theta}.

Scope: \strong{C++-backend tgmrf blocks only} (\code{block$backend == "cpp"},
created via \code{\link[=tgmrf_cpp]{tgmrf_cpp()}}). Calling R for \code{Q(theta)} at every leapfrog
step would dwarf any compute-time advantage the joint sampler has over
the marginal-theta path. Users with R-closure blocks already have
\code{\link[=tulpa_tgmrf_imh]{tulpa_tgmrf_imh()}} (independence-MH over theta) and
\code{\link[=tulpa_tgmrf_nuts]{tulpa_tgmrf_nuts()}} (outer-theta NUTS) as exact-MCMC options.
}
\details{
Math (mirroring \code{src/tgmrf_nuts.cpp}):

\deqn{\log p(\beta, z, \theta \mid y) = \sum_i \log p(y_i \mid \eta_i)
  + \tfrac{1}{2}\log\det Q(\theta) - \tfrac{1}{2}(z - \mu)^\top Q(\theta)(z - \mu)
  - \tfrac{n_{\mathrm{lat}}}{2}\log(2\pi) + \log p(\theta).}

Gradients used by the leapfrog:
\itemize{
\item \eqn{\partial / \partial \beta_k = \sum_i X_{ik} s_i} with
\eqn{s_i = d\log p(y_i\mid\eta_i)/d\eta_i};
\item \eqn{\partial / \partial z_j = s_{i(j)} - (Q(z-\mu))_j};
\item \eqn{\partial / \partial \theta_m = \tfrac{1}{2}\mathrm{tr}(Q^{-1}\partial_mQ)
    - \tfrac{1}{2}(z-\mu)^\top \partial_m Q (z-\mu) + (\partial_m \mu)^\top Q(z-\mu)
    + d\log p(\theta)/d\theta_m}.
}

\eqn{\partial_m Q}, \eqn{\partial_m \mu}, \eqn{d\log p(\theta)/d\theta_m} are
obtained by central finite differences on the registered C++ kernels --
\eqn{2\,\mathrm{dim}(\theta)} extra \code{Q} evaluations per leapfrog step.
For typical \eqn{\dim(\theta) \le 5} this is cheap. AD-on-theta (via the
reserved arena/forward instantiation slots in \code{TgmrfSpec}) is a planned
upgrade; the wire format already carries the slots.

Mass matrix: diagonal, initialised from the inverse-variance of the
pilot Laplace posterior. Step size: dual-averaging warmup targeting
\code{target_accept = 0.8} (higher than the default 0.65 for plain NUTS
because \eqn{(\beta, z, \theta)} typically has dimension \eqn{\ge 50}).
}
\references{
Hoffman & Gelman (2014). The No-U-Turn Sampler. JMLR
15:1593-1623.
}
\seealso{
\code{\link[=tulpa_tgmrf_nuts]{tulpa_tgmrf_nuts()}} for the marginal-theta NUTS;
\code{\link[=tulpa_tgmrf_imh]{tulpa_tgmrf_imh()}} for the IMH composition; \code{\link[=tgmrf_cpp]{tgmrf_cpp()}} for the
C++-backend block constructor.
}
