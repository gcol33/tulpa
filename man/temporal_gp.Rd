% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_gp.R
\name{temporal_gp}
\alias{temporal_gp}
\title{Gaussian Process temporal structure}
\usage{
temporal_gp(
  time_var,
  cov = c("exponential", "matern", "gaussian", "periodic"),
  nu = 1.5,
  period = NULL,
  group_var = NULL,
  shared = NULL,
  scale_coords = TRUE,
  parameterization = c("noncentered", "centered")
)
}
\arguments{
\item{time_var}{Name of the time variable in data. Can be a formula
(e.g., \code{~ year}) or a character string (e.g., \code{"year"}). Should be
numeric (continuous time) or convertible to numeric.}

\item{cov}{Covariance function: \code{"exponential"} (default, rough),
\code{"matern"} (tunable smoothness), \code{"gaussian"} (very smooth), or
\code{"periodic"} (for seasonal patterns).}

\item{nu}{Smoothness parameter for Matern covariance. Common values:
\itemize{
\item 0.5: Equivalent to exponential (rough)
\item 1.5: Once differentiable (moderate smoothness)
\item 2.5: Twice differentiable (smooth)
Ignored for non-Matern covariance functions.
}}

\item{period}{Period for periodic covariance (e.g., 12 for monthly, 365 for
daily data with annual cycle). Only used when \code{cov = "periodic"}.}

\item{group_var}{Optional name of grouping variable for panel data.
If provided, separate GPs are estimated for each group.}

\item{shared}{Logical; if TRUE (default), temporal effect enters both
all processes.}

\item{scale_coords}{Logical; if TRUE (default), time values are scaled to
unit variance before computing distances.}

\item{parameterization}{Parameterization for GP effects:
\code{"noncentered"} (default) stores z ~ N(0,1) and scales by covariance
(better for weakly-informed effects);
\code{"centered"} stores effects directly (better for strongly-informed effects).}
}
\value{
A \code{tulpa_temporal_gp} object
}
\description{
Specify a Gaussian Process (GP) temporal random effect for irregularly-spaced
or continuous time points. Unlike RW1/RW2/AR1 which assume equally-spaced
observations, GP temporal effects model correlation as a function of time
distance.

This is particularly useful for:
\itemize{
\item Irregularly-spaced time series
\item Continuous time (e.g., exact timestamps)
\item Smooth temporal trends with uncertainty
}
}
\details{
The GP temporal model adds a time-correlated random effect:

\deqn{\eta(t) = X\beta + f(t)}

where \eqn{f(t)} follows a Gaussian process:
\deqn{f(t) \sim GP(0, \sigma^2 C(|t - t'|; \phi))}

The correlation function \eqn{C(d; \phi)} depends on time distance \eqn{d}:
\itemize{
\item \strong{Exponential}: \eqn{C(d) = \exp(-d/\phi)} - continuous but not differentiable
\item \strong{Matern}: Smooth with tunable roughness via \eqn{\nu}
\item \strong{Gaussian}: \eqn{C(d) = \exp(-(d/\phi)^2)} - infinitely differentiable
\item \strong{Periodic}: \eqn{C(d) = \exp(-2\sin^2(\pi d/p)/\phi^2)} - for seasonal data
}

\strong{Implementation}: Uses a state-space representation for O(n) computational
complexity when possible (exponential, Matern with half-integer nu).
}
\examples{
# Create GP temporal specification
temporal_gp("timestamp")
temporal_gp("day", cov = "matern", nu = 1.5)
temporal_gp("month", cov = "periodic", period = 12)

\dontrun{
# Irregularly-spaced time series (not run - GP temporal experimental)
set.seed(140)
times <- sort(runif(30, 0, 100))
df <- data.frame(
  time = times,
  x = rnorm(30),
  count = rpois(30, lambda = 20),
  effort = rgamma(30, shape = 4, rate = 1)
)

fit <- tulpa(
  count | effort ~ x,
  data = df,
  family = tulpaRatio::tulpa_poisson_gamma(),
  temporal = temporal_gp("time"),
  iter = 200, warmup = 100, chains = 1
)
}

}
\seealso{
\code{\link[=temporal_rw1]{temporal_rw1()}}, \code{\link[=temporal_ar1]{temporal_ar1()}} for equally-spaced temporal effects,
\code{\link[=spatial_gp]{spatial_gp()}} for spatial GP effects
}
