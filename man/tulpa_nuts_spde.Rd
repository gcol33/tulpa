% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_spde_nuts.R
\name{tulpa_nuts_spde}
\alias{tulpa_nuts_spde}
\title{Sample an SPDE GLM via NUTS, optionally jointly over Matern hypers}
\usage{
tulpa_nuts_spde(
  y,
  X,
  spatial,
  family = c("gaussian", "poisson", "binomial", "gamma", "neg_binomial_2", "beta"),
  n_trials = NULL,
  joint = FALSE,
  range = NULL,
  sigma = NULL,
  prior_range = NULL,
  prior_sigma = NULL,
  log_kappa_init = NULL,
  log_tau_init = NULL,
  sigma_beta = 10,
  log_phi_prior_sd = 3,
  log_phi_init = 0,
  noncenter = TRUE,
  mass_matrix = c("auto", "diag", "dense", "block_diag"),
  n_iter = 2000L,
  n_warmup = 1000L,
  max_treedepth = 10L,
  adapt_delta = 0.8,
  seed = 42L,
  verbose = FALSE
)
}
\arguments{
\item{y}{Response vector. Family-specific:
\itemize{
\item \code{gaussian}: any real
\item \code{poisson}, \code{neg_binomial_2}: non-negative integers
\item \code{binomial}: non-negative integers in \verb{[0, n_trials]}
\item \code{gamma}: strictly positive reals
\item \code{beta}: strictly in \verb{(0, 1)}
}}

\item{X}{Fixed-effects design matrix.}

\item{spatial}{A \code{tulpa_spatial} object from \code{\link[=spatial_spde]{spatial_spde()}} /
\code{\link[=spatial_spde_custom]{spatial_spde_custom()}} -- supplies the FEM matrices (C0, G1) and
projection (A) plus the smoothness \code{nu}.}

\item{family}{One of \code{"gaussian"}, \code{"poisson"}, \code{"binomial"},
\code{"gamma"}, \code{"neg_binomial_2"}, \code{"beta"}.}

\item{n_trials}{Integer vector for \code{family = "binomial"} (else ignored).}

\item{joint}{Logical. \code{FALSE} (default) conditions on fixed
\verb{(range, sigma)}. \code{TRUE} activates joint sampling of
\verb{(log_kappa, log_tau)} with the PC prior from \code{prior_range}, \code{prior_sigma}.}

\item{range, sigma}{Fixed-hyper mode only. Matern range and marginal SD
on the field. Default to the SPDE prior medians
(\code{spatial$prior_range[1]}, \code{spatial$prior_sigma[1]}).}

\item{prior_range, prior_sigma}{Joint mode only. PC prior anchors as
\code{c(value, alpha)} pairs:
\itemize{
\item \code{prior_range = c(r0, a_r)} encodes \code{P(range < r0) = a_r}
\item \code{prior_sigma = c(s0, a_s)} encodes \code{P(sigma > s0) = a_s}
Default to the spec's anchors (\code{spatial$prior_range},
\code{spatial$prior_sigma}) when those are length-2 PC anchors.
}}

\item{log_kappa_init, log_tau_init}{Joint mode only. Initial values for
the hyper slots. Default to the value implied by the PC anchor's
\verb{(r0, s0)} pair via \verb{kappa = sqrt(8 nu) / r0},
\verb{tau = 1 / (sqrt(4 pi) * kappa * s0)}.}

\item{sigma_beta}{Prior SD on each fixed-effect coefficient.}

\item{log_phi_prior_sd}{Prior SD on \code{log(phi)}. Role of \code{phi} is
family-specific:
\itemize{
\item \code{gaussian}: \code{phi} is the residual SD (sampled jointly)
\item \code{gamma}: \code{phi} is the Gamma shape (sampled jointly)
\item \code{neg_binomial_2}: \code{phi} is the NB size \code{r} (sampled jointly)
\item \code{beta}: \code{phi} is the Beta precision (sampled jointly)
\item \code{poisson}, \code{binomial}: \code{log_phi} is held tight and ignored downstream.
}}

\item{log_phi_init}{Starting value for \code{log(phi)}.}

\item{noncenter}{Fixed-hyper mode only (\code{joint = FALSE}). When \code{TRUE}
(default) the mesh field is sampled in a non-centered parameterisation:
the field block is a white-noise auxiliary \code{z} and the field is
\verb{v = L^\{-T\} z} with \verb{L L' = Q} at the fixed \verb{(range, sigma)}. This is the
same target density as the centered field (the \code{z -> v} Jacobian and
\verb{log|Q|/2} are constant under fixed hypers and cancel) and isotropises the
field block a priori, which matches the parameterisation the joint path
already uses. Set \code{FALSE} to sample the field directly (centered). Ignored
when \code{joint = TRUE} (the joint path is always non-centered).}

\item{mass_matrix}{NUTS metric. \code{"auto"} (default) picks a dense metric when
the parameter count is small enough (\verb{<= 200}) and a diagonal metric
otherwise. A dense metric captures the fixed-effect / field cross-curvature
a diagonal metric misses; this is what corrects the intercept marginal,
which a diagonal metric under-disperses for an ill-conditioned latent block
such as the rational fractional-nu precision (its near-null direction
confounds the intercept with the field,). \code{"diag"},
\code{"dense"}, \code{"block_diag"} force the choice.}

\item{n_iter, n_warmup, max_treedepth, adapt_delta, seed, verbose}{Standard
NUTS controls.}
}
\value{
A list with \code{draws} (matrix \verb{n_samples x n_params}), \code{means},
\code{phi_summary} (where applicable), \code{accept_prob}, \code{divergent},
\code{treedepth}, \code{epsilon}, \code{joint_hypers}, plus the supplied \code{spatial}
spec. In \code{joint = TRUE} mode additionally:
\code{w_draws} (transformed \code{z -> w} per draw), \code{range_draws},
\code{sigma_draws}, \code{kappa_draws}, \code{tau_draws}, and \code{range_summary},
\code{sigma_summary} (mean/median/5\%/95\% quantiles).
}
\description{
Bayesian counterpart to \code{\link[=fit_spde]{fit_spde()}} / \code{\link[=laplace_spde_at]{laplace_spde_at()}}. Routes the
SPDE-augmented GLM through tulpa's full NUTS backend via the generic
\code{LikelihoodSpec} interface. Two modes:
\itemize{
\item \strong{Legacy / fixed-hyper} (\code{joint = FALSE}, the default): conditions on
the supplied \verb{(range, sigma)}. Samples the latent block
\verb{(beta, w_mesh, log_phi)} jointly. Useful as the inner step of an outer
CCD / nested-Laplace integration over hypers.
\item \strong{Joint hypers} (\code{joint = TRUE}): samples \verb{(beta, z_mesh, log_kappa, log_tau, log_phi)} jointly via the non-centered transform
\verb{w = L(theta)^\{-T\} z}. A PC prior (Fuglstad et al. 2019 JASA) on
\verb{(range, sigma)} is taken from \code{prior_range}, \code{prior_sigma}. Returns
additional \code{w_draws}, \code{range_draws}, \code{sigma_draws}, \code{kappa_draws},
\code{tau_draws} columns/vectors on top of the raw parameter draws.
}
}
\seealso{
\code{\link[=fit_spde]{fit_spde()}} for the Laplace counterpart and the nested-Laplace
path over (range, sigma).
}
