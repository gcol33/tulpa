% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorical_accessors.R
\name{categorical_accessors}
\alias{categorical_accessors}
\alias{fitted.tulpa_categorical}
\alias{residuals.tulpa_categorical}
\alias{predict.tulpa_categorical}
\alias{posterior_predict.tulpa_categorical}
\title{Observation-level accessors for categorical fits}
\usage{
\method{fitted}{tulpa_categorical}(object, ...)

\method{residuals}{tulpa_categorical}(object, type = c("pearson", "response"), ...)

\method{predict}{tulpa_categorical}(
  object,
  newdata = NULL,
  type = c("link", "response"),
  se.fit = FALSE,
  level = 0.95,
  ...
)

\method{posterior_predict}{tulpa_categorical}(object, newdata = NULL, ndraws = NULL, seed = NULL, ...)
}
\arguments{
\item{object}{A \code{tulpa_multinomial} or \code{tulpa_ordinal} fit.}

\item{...}{Ignored.}

\item{type}{For \code{residuals()}, \code{"pearson"} (default) or \code{"response"}. For
\code{predict()}, \code{"link"} (default) or \code{"response"}.}

\item{newdata}{Optional data frame of covariates; \code{NULL} uses the training
design.}

\item{se.fit}{For \code{predict()}, also return standard errors and credible
bounds. On the link scale the standard error is exact for the Gaussian
(Laplace) posterior, \verb{sqrt(a' V a)}, with Gaussian bounds; on the response
scale the standard error and the bounds are the posterior SD and quantiles
of each class probability over the fit's posterior draws.}

\item{level}{Credible-interval level (default 0.95).}

\item{ndraws}{Number of posterior draws for \code{posterior_predict()}; defaults
to all stored draws.}

\item{seed}{Optional integer seed (RNG state is restored on exit).}
}
\value{
\code{fitted()}: an N x K probability matrix. \code{residuals()}: an N x K
matrix. \code{predict()}: a matrix (or, for the ordinal link scale, a vector);
with \code{se.fit = TRUE} a list of \code{fit}, \code{se.fit}, \code{lower}, \code{upper} of that
shape. \code{posterior_predict()}: an \verb{ndraws x N} integer matrix of class
indices with attributes \code{levels} and \code{ordered}.
}
\description{
\code{\link[=fitted]{fitted()}}, \code{\link[=residuals]{residuals()}}, \code{\link[=predict]{predict()}} and \code{\link[=posterior_predict]{posterior_predict()}} for a
categorical response: a \code{\link[=tulpa_multinomial]{tulpa_multinomial()}} fit (\code{family = "multinomial"})
or a \code{\link[=tulpa_ordinal]{tulpa_ordinal()}} fit (\code{family = "ordinal"}). The response is one of K
classes, so the response-scale quantities are N x K matrices with one column
per class.
\itemize{
\item \code{fitted()} returns the class probabilities at the posterior mode.
\item \code{residuals()} returns, per class, the indicator of the observed class minus
its fitted probability (\code{"response"}), or that difference divided by the
indicator's standard deviation \code{sqrt(p (1 - p))} (\code{"pearson"}). A
categorical response has no single scalar residual; each class column is
the Bernoulli residual of that class's indicator.
\item \code{predict()} returns the link scale -- the K - 1 baseline-category logits of
the multinomial model (N x (K - 1)), or the ordinal location predictor
\verb{x' beta} (length N) -- or the class probabilities (\code{type = "response"}).
\item \code{posterior_predict()} draws one class per observation for each posterior
draw and returns the class indices, with the response levels attached;
\code{\link[=simulate.tulpa_fit]{simulate.tulpa_fit()}} turns them into factor columns.
}

The pointwise log-likelihood behind \code{\link[=cpo]{cpo()}}, \code{\link[=dic]{dic()}}, \code{loo::waic()} and
\code{loo::loo()} is the log probability of each observed class at each stored
posterior draw; \code{\link[=dic]{dic()}} evaluates it at the posterior-mean parameters.
}
\seealso{
\code{\link[=tulpa_multinomial]{tulpa_multinomial()}}, \code{\link[=tulpa_ordinal]{tulpa_ordinal()}}
}
