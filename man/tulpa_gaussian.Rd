% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_gaussian.R
\name{tulpa_gaussian}
\alias{tulpa_gaussian}
\title{Fit a Gaussian linear model via tulpa's generic engine}
\usage{
tulpa_gaussian(
  formula,
  data,
  beta_prior = .tulpa_default_beta_prior("gaussian"),
  control = list()
)
}
\arguments{
\item{formula}{A formula (e.g., y ~ x1 + x2)}

\item{data}{A data frame}

\item{beta_prior}{Fixed-effect prior as \code{list(mean, sd)}: a mean-zero
(\code{mean = 0}) Gaussian on every coefficient with SD \code{sd}
(default the engine default, \code{prior_normal(0, 2.5)}).}

\item{control}{List of sampler knobs: \code{iter} (total iterations, default
2000), \code{warmup} (default 1000), \code{max_treedepth} (default 10),
\code{adapt_delta} (target acceptance statistic, default 0.8), \code{seed} (\code{NULL}
draws from the session RNG).}
}
\value{
A \code{tulpa_fit} list with \code{draws} (post-warmup draws of \code{beta[j]} and
\code{log_sigma}), \code{means}, \code{sigma} (\code{exp()} of the posterior mean of
\code{log_sigma}), \code{accept_rate} (the mean NUTS acceptance statistic over the
post-warmup iterations), the per-iteration \code{accept_prob}, \code{divergent},
\code{treedepth} and \code{log_prob}, the adapted step size \code{epsilon}, and
\code{formula}, \code{N}, \code{p}.
}
\description{
Proof-of-concept function demonstrating the tulpa generic interface: the
Gaussian likelihood is supplied as a \code{LikelihoodSpec} and fitted by the
production NUTS sampler every other ModelData backend runs (dual-averaged
step size, adapted diagonal mass matrix, reverse-mode AD gradient checked
against finite differences before sampling). Fits
\code{y ~ Normal(X * beta, sigma)}.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(x = rnorm(40))
d$y <- 1 + 2 * d$x + rnorm(40, 0, 0.6)
fit <- tulpa_gaussian(y ~ x, d, control = list(iter = 1000, warmup = 500,
                                              seed = 1))
fit$means
fit$accept_rate
}
}
