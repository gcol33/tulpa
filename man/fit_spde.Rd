% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_spde.R
\name{fit_spde}
\alias{fit_spde}
\title{Fit a Spatial Model using SPDE Laplace Approximation}
\usage{
fit_spde(
  y,
  X,
  spatial,
  family = "binomial",
  n_trials = NULL,
  range = NULL,
  sigma = NULL,
  nested_laplace = is.null(range) || is.null(sigma),
  phi = 1,
  offset = NULL,
  re_idx = NULL,
  n_re_groups = 0L,
  sigma_re = 1,
  mode = c("laplace", "nuts"),
  control = list()
)
}
\arguments{
\item{y}{Integer response vector.}

\item{X}{Design matrix.}

\item{spatial}{A \code{tulpa_spatial} object from \code{\link[=spatial_spde]{spatial_spde()}} or
\code{\link[=spatial_spde_custom]{spatial_spde_custom()}}.}

\item{family}{Distribution family: \code{"binomial"}, \code{"poisson"},
\code{"neg_binomial_2"}, or \code{"gaussian"} (continuous-field geostatistics, with
\code{phi} the observation-noise standard deviation).}

\item{n_trials}{Integer vector of trial sizes (binomial only).}

\item{range}{Spatial range parameter. If NULL, uses nested Laplace to
integrate over range and sigma.}

\item{sigma}{Marginal standard deviation. If NULL, uses nested Laplace.}

\item{nested_laplace}{Logical. If TRUE (default when range/sigma are NULL),
use nested Laplace approximation over hyperparameters.}

\item{phi}{Dispersion passed to the family, held fixed. One convention at
every door: for \code{gaussian} / \code{lognormal} this is the residual VARIANCE
(the SD is \code{sqrt(phi)}), for \code{neg_binomial_2} the size, \code{gamma} the
shape, \code{beta} the precision, \code{t} the scale; \code{binomial} and \code{poisson}
ignore it. The compiled kernels parameterize the two variance families by
the residual SD and are handed \code{sqrt(phi)} at the boundary.}

\item{offset}{Optional fixed additive term on the linear predictor
(\verb{eta = offset + X beta + A w}), length \code{length(y)}; \code{NULL} -> no offset.}

\item{re_idx, n_re_groups, sigma_re}{Optional single iid random-intercept
\code{(1 | g)} term alongside the Matern field: \code{re_idx} is a length-\code{length(y)}
1-based group index, \code{n_re_groups} the number of groups, and \code{sigma_re} the
(conditioned) random-effect SD. The field and the RE block are Laplace-
marginalised jointly. \code{n_re_groups = 0} (default) is no RE term. Not
supported for a fractional-nu field.}

\item{mode}{Inference method (the method is an argument, not a parallel
verb): \code{"laplace"} (default) is the nested-Laplace integration over
\verb{(range, sigma)} documented here; \code{"nuts"} delegates to \code{\link[=tulpa_nuts_spde]{tulpa_nuts_spde()}}
for exact HMC over the field (and, unless both \code{range} and \code{sigma} are
fixed, the Matern hyperparameters). \code{mode = "nuts"} does not support an
\code{offset} or a random-effect term, and its sampler knobs pass via \code{control}
(see \code{\link[=tulpa_nuts_spde]{tulpa_nuts_spde()}}); it returns that sampler's draws object.}

\item{control}{A named list of numerical / tuning knobs (statistical
arguments stay in the signature above). Recognized entries:
\itemize{
\item \code{method}: hyperparameter integration backend when nested Laplace is
active. \code{"ccd"} (default) uses a central composite design centered on
the joint posterior mode of \verb{(range, sigma)}, oriented by the local
Hessian (9 design points instead of \code{n_grid^2}), folding the PC priors
from \code{spatial$prior_range} / \code{spatial$prior_sigma} into the integrated
marginal and falling back to \code{"grid"} if the surface is too flat for a
Hessian-based design. \code{"grid"} uses a rectangular grid in
\verb{log(range) x log(sigma)} around the prior modes.
\item \code{n_grid}: grid points per hyperparameter dimension for
\code{method = "grid"} (ignored under \code{"ccd"}). Default 5.
\item \code{diagnose_k}: if TRUE (default), compute the outer
Pareto-\eqn{\hat{k}} accuracy diagnostic (\verb{$pareto_k}) by importance
sampling the joint \verb{(range, sigma)} posterior on the log scale against
the Gaussian proposal that orients the integration. See \code{\link[=tulpa_psis]{tulpa_psis()}}.
\item \code{k_samples}: importance draws for \code{diagnose_k}. Default 500, each
one extra batched SPDE marginal evaluation. It is a precision knob:
the GPD tail size is held at the fraction the default budget implies,
so a larger budget sharpens the same k-hat rather than moving it to a
deeper quantile of the weight distribution (gcol33/tulpa#631).
\item \code{k_tail_points}: expert override for that tail size, in upper-tail
order statistics. Silently capped at 20\% of the draws, beyond which
body ratios enter the tail and bias the shape.
\item \code{mode_find}: tuning for the outer \verb{(range, sigma)} mode-find under
\code{method = "ccd"}, as \code{list(factr =, ndeps =, maxit =)}; supply any
subset. \code{ndeps} is the central-difference step for \code{optim()}'s
numerical gradient on the log scale (default 1e-2): it must clear the
inner solver's convergence tolerance, and a step wide enough that its
truncation error exceeds the reduction the line search chases near a
flat optimum leaves L-BFGS-B aborting at the mode it just reached, in
which case the CCD design declines to the rectangular grid. \code{factr} is
the relative-reduction stop in units of \code{.Machine$double.eps} (default
1e5); \code{maxit} the iteration cap (default 300).
\item \code{max_iter}: maximum Newton iterations. Default 100.
\item \code{tol}: Newton convergence tolerance. Default 1e-6.
\item \code{n_threads}: OpenMP threads. Default 1.
\item \code{checkpoint}: grid-cell checkpoint/resume spec
\code{list(path =, resume =)}. Each solved \verb{(range, sigma)} cell is appended
to \code{path}; a \code{resume = TRUE} run loads the finished cells and re-solves
only the rest, so a killed or rebooted fit resumes instead of
restarting. \code{resume = FALSE} starts a fresh file. Default \code{NULL} (off).
}}
}
\value{
A list with:
\itemize{
\item \code{mode}: mode of the latent field (beta + mesh node effects)
\item \code{log_marginal}: log marginal likelihood
\item \code{converged}: convergence flag
\item \code{spatial}: the spatial specification (for prediction)
\item \code{pareto_k}, \code{pareto_k_is_ess}: outer Pareto-\eqn{\hat{k}} and its
importance-sampling ESS (\code{NA} when \code{diagnose_k = FALSE})
\item \code{pareto_k_proposal_source}, \code{pareto_k_first_pass}: which proposal
family the reported k-hat came from (several candidates are scored and
the best kept), and the k-hat of the first pass -- the proposal exactly
as placed, before refinement. A large gap says the placement is poor
even where the verdict is fine.
\item \code{nested}: nested Laplace results (if used)
}
}
\description{
Fits a GLM with a Matern spatial field via the SPDE approach.
Uses CHOLMOD sparse solver with optional nested Laplace for
hyperparameter integration.
}
\examples{
\donttest{
if (requireNamespace("fmesher", quietly = TRUE)) {
  set.seed(1)
  n <- 200L
  coords <- cbind(runif(n), runif(n))
  mesh <- fmesher::fm_mesh_2d(loc = coords, max.edge = c(0.15, 0.4), cutoff = 0.05)
  fem  <- fmesher::fm_fem(mesh)
  A    <- as(fmesher::fm_basis(mesh, loc = coords), "CsparseMatrix")
  spec <- spatial_spde_custom(C = fem$c0, G = fem$g1, A = A, nu = 1,
                              prior_range = c(0.3, 0.5), prior_sigma = c(0.6, 0.05))
  w <- as.numeric(rnorm(spec$n_mesh, 0, 0.6)); w <- w - mean(w)
  x <- rnorm(n)
  y <- rpois(n, exp(2.0 + 0.5 * x + as.numeric(spec$A \%*\% w)))
  fit <- fit_spde(y = y, X = cbind(1, x), spatial = spec, family = "poisson")
  fit$nested$range_mean
}
}
}
\references{
Lindgren, Rue & Lindstrom (2011). An explicit link between Gaussian fields
and Gaussian Markov random fields: the stochastic partial differential
equation approach. \emph{JRSS-B} 73(4):423-498.
Rue, Martino & Chopin (2009). Approximate Bayesian inference for latent
Gaussian models by using integrated nested Laplace approximations.
\emph{JRSS-B} 71(2):319-392.
}
