% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_spde.R
\name{fit_spde}
\alias{fit_spde}
\title{Fit a Spatial Model using SPDE Laplace Approximation}
\usage{
fit_spde(
  y,
  X,
  spatial,
  family = "binomial",
  n_trials = NULL,
  range = NULL,
  sigma = NULL,
  nested_laplace = is.null(range) || is.null(sigma),
  n_grid = 5L,
  phi = 1,
  max_iter = 100L,
  tol = 1e-06,
  n_threads = 1L
)
}
\arguments{
\item{y}{Integer response vector.}

\item{X}{Design matrix.}

\item{spatial}{A \code{tulpa_spatial} object from \code{\link[=spatial_spde]{spatial_spde()}} or
\code{\link[=spatial_spde_custom]{spatial_spde_custom()}}.}

\item{family}{Distribution family: \code{"binomial"}, \code{"poisson"}, or \code{"neg_binomial_2"}.}

\item{n_trials}{Integer vector of trial sizes (binomial only).}

\item{range}{Spatial range parameter. If NULL, uses nested Laplace to
integrate over range and sigma.}

\item{sigma}{Marginal standard deviation. If NULL, uses nested Laplace.}

\item{nested_laplace}{Logical. If TRUE (default when range/sigma are NULL),
use nested Laplace approximation over hyperparameters.}

\item{n_grid}{Number of grid points per hyperparameter dimension for
nested Laplace. Default 5.}

\item{phi}{Dispersion parameter (negbin only).}

\item{max_iter}{Maximum Newton iterations. Default 100.}

\item{tol}{Newton convergence tolerance. Default 1e-6.}

\item{n_threads}{OpenMP threads. Default 1.}
}
\value{
A list with:
\itemize{
\item \code{mode}: mode of the latent field (beta + mesh node effects)
\item \code{log_marginal}: log marginal likelihood
\item \code{converged}: convergence flag
\item \code{spatial}: the spatial specification (for prediction)
\item \code{nested}: nested Laplace results (if used)
}
}
\description{
Fits a GLM with a Matérn spatial field via the SPDE approach.
Uses CHOLMOD sparse solver with optional nested Laplace for
hyperparameter integration.
}
