% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_gp.R
\name{temporal_multiscale}
\alias{temporal_multiscale}
\title{Multi-scale temporal structure}
\usage{
temporal_multiscale(
  time_var,
  trend = c("rw2", "rw1", "none"),
  seasonal = NULL,
  short_term = c("ar1", "iid", "none"),
  group_var = NULL,
  shared = NULL
)
}
\arguments{
\item{time_var}{Single character string naming the time variable in the data.}

\item{trend}{Prior for the smooth long-term trend. One of \code{"rw2"}, \code{"rw1"},
or \code{"none"}.}

\item{seasonal}{Optional integer period (\verb{>= 2}) of a seasonal cycle, e.g.
\code{12} for monthly data with an annual cycle. \code{NULL} (default) omits the
seasonal component.}

\item{short_term}{Prior for the short-term component. One of \code{"ar1"},
\code{"iid"}, or \code{"none"}.}

\item{group_var}{Optional character string naming a grouping variable for
group-specific temporal effects.}

\item{shared}{Whether the effect is shared across processes in a
multi-process model. \code{NULL} (default) shares it; \code{FALSE} fits
process-specific effects and emits a warning.}
}
\value{
A \code{tulpa_temporal_multiscale} object.
}
\description{
Specify a temporal random effect that decomposes variation into separate
scales: a smooth trend, an optional seasonal cycle, and a short-term
component. Each scale uses its own prior, letting slow and fast dynamics be
modelled jointly.
}
\details{
At least one of \code{trend}, \code{seasonal}, or \code{short_term} must be active.
}
\examples{
# Trend + annual seasonal cycle + AR1 short-term component on monthly data
temporal_multiscale("month", trend = "rw2", seasonal = 12, short_term = "ar1")

}
\seealso{
\code{\link[=temporal_rw1]{temporal_rw1()}}, \code{\link[=temporal_rw2]{temporal_rw2()}}, \code{\link[=temporal_ar1]{temporal_ar1()}} for
single-scale temporal priors.
}
