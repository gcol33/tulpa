% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_beta_nuts.R
\name{tulpa_nuts_beta}
\alias{tulpa_nuts_beta}
\title{Fit a beta-regression model via NUTS (joint sampling of beta + log_phi)}
\usage{
tulpa_nuts_beta(
  y,
  X,
  sigma_beta = 10,
  log_phi_prior_sd = 3,
  log_phi_init = 0,
  n_iter = 2000L,
  n_warmup = 1000L,
  max_treedepth = 10L,
  adapt_delta = 0.8,
  seed = NULL,
  verbose = FALSE
)
}
\arguments{
\item{y}{Response vector, strictly in \verb{(0, 1)}.}

\item{X}{Fixed-effects design matrix.}

\item{sigma_beta}{Prior SD on each fixed-effect coefficient
(\code{beta_j ~ N(0, sigma_beta)}). Default \code{10}.}

\item{log_phi_prior_sd}{Prior SD on \code{log(phi)}
(\code{log_phi ~ N(0, log_phi_prior_sd)}). Default \code{3} (very weak;
covers \code{phi} from ~0.001 to ~1000 within +-2 SD).}

\item{log_phi_init}{Starting value for \code{log(phi)}. Default \code{0}
(i.e. \code{phi = 1}); a method-of-moments warm start can speed warmup
in highly concentrated regimes.}

\item{n_iter}{Total NUTS iterations including warmup. Default \code{2000}.}

\item{n_warmup}{Warmup iterations. Default \code{1000}.}

\item{max_treedepth}{NUTS max tree depth. Default \code{10}.}

\item{adapt_delta}{Target acceptance rate for dual averaging.
Default \code{0.8}.}

\item{seed}{RNG seed; \code{NULL} (default) draws one from the session RNG so
\code{set.seed()} controls the fit (two default calls are then independent).}

\item{verbose}{Print sampler progress.}
}
\value{
A list with:
\itemize{
\item \code{draws} -- \verb{n_samples x (p + 1)} matrix of post-warmup draws,
columns \verb{beta[1] ... beta[p], log_phi}.
\item \code{means} -- posterior means.
\item \code{phi_summary} -- posterior mean / median / quantiles of
\code{phi = exp(log_phi)}.
\item \code{accept_prob}, \code{divergent}, \code{treedepth}, \code{epsilon} -- NUTS
diagnostics from the underlying chain.
}
}
\description{
Bayesian counterpart to \code{\link[=tulpa_laplace_beta]{tulpa_laplace_beta()}}. Routes the Beta GLM
through tulpa's full NUTS backend via the generic \code{LikelihoodSpec}
interface, sampling the precision \code{phi} jointly with the regression
coefficients on the log scale. NUTS performs exact marginalisation
over \code{phi}, replacing the Brent outer-opt in \code{\link[=tulpa_laplace_beta]{tulpa_laplace_beta()}}
and the deterministic nested-Laplace grid that would otherwise
integrate over the Beta dispersion.

Mean-precision parameterisation, default logit link:
\code{y_i ~ Beta(mu_i * phi, (1 - mu_i) * phi)} with
\code{mu_i = 1 / (1 + exp(-eta_i))} and \code{eta_i = X_i \%*\% beta}.
}
\examples{
set.seed(1)
n <- 150L
X <- cbind(1, rnorm(n))
mu <- plogis(X \%*\% c(0.2, 0.7)); phi <- 8
y <- rbeta(n, mu * phi, (1 - mu) * phi)
\donttest{
fit <- tulpa_nuts_beta(y, X, n_iter = 500L, n_warmup = 250L)
colMeans(fit$draws)
}
}
\seealso{
\code{\link[=tulpa_laplace_beta]{tulpa_laplace_beta()}} for the Laplace + Brent point
estimate; \code{\link[=tulpa_laplace]{tulpa_laplace()}} for the underlying Laplace engine.
}
