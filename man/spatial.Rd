% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_field.R
\name{spatial}
\alias{spatial}
\title{Areal spatially varying coefficient field}
\usage{
spatial(graph, formula, proper = FALSE, shared = NULL, by = NULL)
}
\arguments{
\item{graph}{Symmetric adjacency matrix of the spatial graph
(\verb{[n_node x n_node]}, dense or sparse). One CAR field is defined over its
nodes per coefficient.}

\item{formula}{One-sided formula carrying a grouping bar, e.g.
\code{~ 1 + time || cell}. The left-hand side is expanded with
\code{\link[stats:model.matrix]{stats::model.matrix()}} into one CAR field per column (\code{1} = intercept
field; a covariate = spatially varying slope on it; \verb{0 +} drops the
intercept). The right-hand side must be a single bare column naming the
graph node. Only the double bar \code{||} (independent fields) is supported;
the single bar \code{|} would request correlated fields (a multivariate CAR)
and is not yet implemented.}

\item{proper}{Logical; \code{FALSE} (default) builds intrinsic CAR (ICAR /
Besag) fields with the sum-to-zero constraint. \code{TRUE} (proper CAR with an
estimated spatial autocorrelation) is reserved for a future release.}

\item{shared}{Optional shared-effect handle, passed through to the field
blocks (see the model docs). Default \code{NULL} (shared).}

\item{by}{Reserved. A future replicated-CAR argument that would build an
independent copy of the whole field for each level of a factor. Not yet
implemented; passing it errors.}
}
\value{
A \code{tulpa_spatial_field} object describing the field(s). It is
expanded into one CAR block per design column at fit time, when the data
is available.
}
\description{
Declare one or more areal (CAR / Besag) fields over a graph from an
\pkg{lme4}-style bar formula. The bar's left-hand side lists the
coefficients that vary smoothly over the graph; the right-hand side names
the graph-node index. Each coefficient becomes an independent CAR field,
entering the linear predictor scaled by that coefficient's per-observation
design value:

\deqn{\eta_i = \ldots + \sum_c X_{ic}\, z^{(c)}_{g_i},}

where \eqn{g_i} is the graph node of observation \eqn{i}, \eqn{z^{(c)}} is
the CAR field for design column \eqn{c}, and \eqn{X_{ic}} is that column's
value at observation \eqn{i}. The intercept column is all ones, so
\code{~ 1 || cell} is the ordinary spatial intercept field; a covariate column
(e.g. \code{time}) gives a spatially varying slope on that covariate (a
per-region trend).

Use it inline in a \code{\link[=tulpa]{tulpa()}} model formula, the same way a random-effect
bar is written:

\preformatted{
y ~ time + spatial(graph = adj, formula = ~ 1 + time || cell) + (1 | site)
}
}
\details{
Each field is independent (its own precision), matching \code{INLA}'s two
separate \code{f(cell, model = "besag")} and \code{f(cell.slope, time, model = "besag")} fields. Nesting (\code{a / b}), interaction, or expression
grouping is rejected: the grouping must be a single graph node. Add
ordinary nested random effects, e.g. \code{(1 | site)}, as separate terms.
}
\examples{
# Chain graph over 10 cells
adj <- matrix(0, 10, 10)
for (i in 1:9) adj[i, i + 1] <- adj[i + 1, i] <- 1

# Spatial intercept plus a spatially varying time slope
f <- spatial(graph = adj, formula = ~ 1 + time || cell)
print(f)

}
\seealso{
\code{\link[=spatial_car]{spatial_car()}} for a single areal field passed via the
\verb{spatial =} argument, \code{\link[=spatial_svc]{spatial_svc()}} for the coordinate-based
(Gaussian-process) spatially varying coefficient.
}
