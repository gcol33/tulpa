% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_diagnostics.R
\name{plot_ess}
\alias{plot_ess}
\title{Plot Effective Sample Size Diagnostic}
\usage{
plot_ess(fit, type = c("bulk", "tail"), threshold = 400, pars = NULL)
}
\arguments{
\item{fit}{A \code{tulpa_fit} object.}

\item{type}{Type of ESS: "bulk" (default) or "tail".}

\item{threshold}{Minimum acceptable ESS (default: 400).}

\item{pars}{Character vector of parameter names to include.}
}
\value{
A ggplot object (if ggplot2 available) or base R plot (invisible).
}
\description{
Creates a visual display of effective sample size (ESS) for all parameters,
expressed as a ratio of ESS to total samples. Low ESS indicates high
autocorrelation.
}
\details{
ESS/iter ratio interpretation:
\itemize{
\item Green: ESS >= threshold
\item Yellow: threshold/2 <= ESS < threshold
\item Red: ESS < threshold/2
}
}
\examples{
\donttest{
set.seed(123)
df <- data.frame(x = rnorm(60))
df$y <- rpois(60, exp(0.5 + 0.3 * df$x))
fit <- tulpa(y ~ x, data = df, family = "poisson", mode = "hmc",
             control = list(n_iter = 500L, warmup = 250L, n_chains = 2L,
                            seed = 1L))
plot_ess(fit)
plot_ess(fit, type = "tail")
}

}
\seealso{
\code{\link[=plot_rhat]{plot_rhat()}}, \code{\link[=diagnostic_summary]{diagnostic_summary()}}, \code{\link[=diagnostics]{diagnostics()}}
}
