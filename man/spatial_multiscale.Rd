% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_gp.R
\name{spatial_multiscale}
\alias{spatial_multiscale}
\title{Multi-Scale Gaussian Process spatial structure}
\usage{
spatial_multiscale(
  coords,
  scales = c("local", "regional"),
  approx = c("nngp", "hsgp"),
  m = 6L,
  c_boundary = 1.5,
  range_local = c(0.01, 1),
  range_regional = c(1, 10),
  cov = c("exponential", "matern"),
  nu = 1.5,
  nn_local = 10,
  nn_regional = 30,
  shared = NULL,
  scale_coords = TRUE,
  sampler = c("auto", "noncentered", "centered", "interweaved")
)
}
\arguments{
\item{coords}{A one-sided formula specifying coordinate columns (e.g.,
\code{~ lon + lat}), or a character vector of length 2 with column names.}

\item{scales}{Character vector specifying scale names. Default: \code{c("local", "regional")}.}

\item{approx}{Approximation method: \code{"nngp"} (default) for Nearest Neighbor
GP; \code{"hsgp"} for Hilbert Space GP (faster for smooth fields).}

\item{m}{Number of HSGP basis functions per dimension (default 6). Only
used when \code{approx = "hsgp"}. Total basis functions will be m^2.}

\item{c_boundary}{Boundary factor for HSGP domain extension (default 1.5).
Only used when \code{approx = "hsgp"}.}

\item{range_local}{Plausible range interval for the local scale as
\code{c(lower, upper)} in coordinate units. Default: \code{c(0.01, 1)} (after
scaling). Under exact NUTS this is not a hard box: \code{lower} anchors a PC
prior on that scale's range (\code{P(range < lower) = 0.05}, the same prior
\code{\link[=spatial_gp]{spatial_gp()}} uses), and the pair places the sampler's starting range at
their geometric mean. The range itself is free on \verb{(0, Inf)}.}

\item{range_regional}{Plausible range interval for the regional scale, read
the same way. Default: \code{c(1, 10)} (after scaling). Keeping the two
intervals separated is what identifies the scales against each other.}

\item{cov}{Covariance function: \code{"exponential"} (default) or \code{"matern"}.}

\item{nu}{Smoothness parameter for Matern covariance, one of \code{1.5} or \code{2.5}.}

\item{nn_local}{Number of nearest neighbors for local scale. Default 10.}

\item{nn_regional}{Number of nearest neighbors for regional scale. Default 30.}

\item{shared}{Logical; if TRUE (default), spatial effects enter both
all processes.}

\item{scale_coords}{Logical; if TRUE (default), coordinates are scaled to
unit variance before computing distances.}

\item{sampler}{Latent parameterization for the exact-NUTS field. \code{"auto"}
(default) and \code{"noncentered"} sample \code{z ~ N(0, I)} per scale and
reconstruct each field as \code{w = f(z, sigma2, phi)}, avoiding the
field/hyperparameter funnel; \code{"centered"} places the NNGP density on each
field directly. \code{"interweaved"} alternates between parameterizations and
is not implemented on the exact-NUTS path.}
}
\value{
A \code{tulpa_multiscale} object
}
\description{
Specify a multi-scale spatial random effect that decomposes spatial
variation into local (fine-scale) and regional (broad-scale) components.
Each scale has its own range and variance parameters.

This is particularly useful for large datasets (>100k observations) where
spatial patterns exist at multiple scales.
}
\details{
The multi-scale model decomposes spatial variation additively:

\deqn{\eta(s) = X\beta + w_{local}(s) + w_{regional}(s)}

where each component follows an independent Gaussian process:
\deqn{w_{local}(s) \sim GP(0, \sigma^2_{local} C(\phi_{local}))}
\deqn{w_{regional}(s) \sim GP(0, \sigma^2_{regional} C(\phi_{regional}))}

\strong{Identifiability}: With sufficient data (>500 locations), the two scales
are typically well-identified when prior ranges are non-overlapping.
PC priors on variance components help prevent overfitting.

\strong{Computational cost}: Approximately 1.5-2x the cost of single-scale GP,
as two NNGP likelihoods must be evaluated.
}
\examples{
# Create multi-scale spatial structure
ms <- spatial_multiscale(
  ~ lon + lat,
  range_local = c(0.1, 0.5),
  range_regional = c(1, 5)
)
print(ms)

\dontrun{
set.seed(101)
n <- 60
df <- data.frame(
  lon = runif(n, 0, 10),
  lat = runif(n, 0, 10),
  depth = rnorm(n),
  temp = rnorm(n)
)
df$count <- rpois(n, exp(1 + 0.2 * df$depth))

# Both scales are sampled by exact NUTS (mode = "exact"); the field is
# returned as gp_local[i] / gp_regional[i] draws.
fit <- tulpa(
  count ~ depth + temp,
  data = df,
  family = "poisson",
  spatial = spatial_multiscale(
    ~ lon + lat,
    range_local = c(0.1, 0.5),
    range_regional = c(1, 5)
  ),
  mode = "exact",
  control = list(n_iter = 200L, n_warmup = 100L)
)
summary(fit)
}

}
\seealso{
\code{\link[=spatial_gp]{spatial_gp()}} for single-scale GP, \code{\link[=temporal_multiscale]{temporal_multiscale()}} for
multi-scale temporal effects
}
