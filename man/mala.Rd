% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mala.R
\name{mala}
\alias{mala}
\title{Metropolis-Adjusted Langevin Algorithm (MALA)}
\usage{
mala(
  log_posterior,
  grad_log_posterior,
  init,
  n_iter = 2000L,
  warmup = n_iter\%/\%2L,
  epsilon = 0.1,
  target_accept = 0.574,
  mass_diag = NULL,
  thin = 1L,
  verbose = FALSE
)
}
\arguments{
\item{log_posterior}{Function \verb{function(theta) -> numeric} returning
the \emph{unnormalized} log posterior.}

\item{grad_log_posterior}{Function \verb{function(theta) -> numeric vector}
returning the gradient at \code{theta}.}

\item{init}{Numeric vector: initial state (must give finite
log_posterior).}

\item{n_iter}{Total iterations including warmup (default 2000).}

\item{warmup}{Warmup iterations (step-size adaptation + discarded;
default \code{n_iter / 2}).}

\item{epsilon}{Initial step size (default 0.1). Adapted during warmup.}

\item{target_accept}{Target acceptance during warmup adaptation
(default 0.574, the Roberts & Rosenthal 1998 optimum).}

\item{mass_diag}{Optional preconditioner: a length-\code{d} vector of
per-dimension scales, equivalent to using \code{diag(mass_diag)} as
a metric. Default \code{rep(1, d)}. For posteriors with very different
scales across dimensions, set this to (an estimate of) the
posterior SDs.}

\item{thin}{Keep every \code{thin}-th post-warmup sample (default 1).}

\item{verbose}{Print acceptance + step-size summary at end (default
FALSE).}
}
\value{
A list with class \code{tulpa_fit} carrying:
\itemize{
\item \code{draws}: post-warmup draws.
\item \code{means}, \code{n_params}, \code{n_samples}, \code{log_prob}.
\item \code{accept_prob}: per-iteration accepted indicators (post-warmup).
\item \code{mean_accept}: post-warmup acceptance rate.
\item \code{epsilon}: final adapted step size.
\item \code{inference_mode}: \code{"exact"}.
\item \code{inference_tier}: \code{1L}.
\item \code{backend}: \code{"mala"}.
}
}
\description{
Sample from a posterior using MALA: a Langevin proposal driven by
the gradient of the log posterior, corrected by a Metropolis-
Hastings accept/reject step. Each iteration is one log-posterior +
one gradient evaluation. The natural stepping stone between
random-walk MH and HMC: cheaper per iteration than HMC (no leapfrog
integration), better-mixing than RWMH because the drift term moves
toward higher density.

Step size \code{epsilon} is adapted via dual averaging during warmup to
target an acceptance rate of 0.574 (Roberts & Rosenthal 1998 optimal
for high-dimensional Gaussians).
}
\section{Tier}{

Tier 1 (Exact). The MH step makes the chain asymptotically correct.
Mixing depends on whether the gradient gives useful local geometry —
poor for posteriors with very different scales across dimensions
(use a preconditioner or HMC instead).
}

\examples{
\dontrun{
  log_post <- function(t) -0.5 * sum((t - c(1, 2))^2)
  grad <- function(t) -(t - c(1, 2))
  fit <- mala(log_post, grad, init = c(0, 0), n_iter = 4000)
  colMeans(fit$draws)  # near c(1, 2)
  fit$mean_accept      # should adapt toward 0.574
}

}
\references{
Roberts, G. O., & Tweedie, R. L. (1996). Exponential convergence of
Langevin distributions and their discrete approximations.
\emph{Bernoulli}, 2(4), 341–363.

Roberts, G. O., & Rosenthal, J. S. (1998). Optimal scaling of
discrete approximations to Langevin diffusions. \emph{JRSS B}, 60(1),
255–268.
}
