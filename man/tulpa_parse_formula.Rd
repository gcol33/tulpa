% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/formula.R
\name{tulpa_parse_formula}
\alias{tulpa_parse_formula}
\title{Parse a mixed-model formula}
\usage{
tulpa_parse_formula(formula)
}
\arguments{
\item{formula}{A formula object (e.g., \code{y ~ x + (1 | group)})}
}
\value{
A list with:
\itemize{
\item \code{response}: character, the response variable label (\code{NULL} if none)
\item \code{response_expr}: language object, the LHS of \code{~} (\code{NULL} if none)
\item \code{fixed_formula}: formula, the fixed-effects-only formula
\item \code{random_effects}: list of parsed RE specifications
\item \code{n_re_terms}: integer, number of RE terms
\item \code{latent_blocks}: list of evaluated user-defined latent block objects
(typically \code{tgmrf} S3 objects), one per \code{latent(...)} term in the
original formula. Empty list when no \code{latent(...)} terms are present.
\item \code{n_latent_blocks}: integer, number of \code{latent(...)} terms
\item \code{original}: the original formula
}
}
\description{
Decomposes a formula into fixed effects and random effects by walking
the formula's abstract syntax tree. This is structural recursion with
pattern matching on bar terms — no string manipulation.
}
\examples{
pf <- tulpa_parse_formula(y ~ x1 + x2 + (1 | group) + (x1 || site))
pf$response        # "y"
pf$fixed_formula   # y ~ x1 + x2

}
