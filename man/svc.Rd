% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_svc.R
\name{svc}
\alias{svc}
\alias{svc.tulpa_fit}
\title{Extract spatially-varying coefficients from a fitted model}
\usage{
svc(object, terms = NULL, summary = FALSE, probs = c(0.025, 0.5, 0.975), ...)

\method{svc}{tulpa_fit}(object, terms = NULL, summary = FALSE, probs = c(0.025, 0.5, 0.975), ...)
}
\arguments{
\item{object}{A \code{tulpa_fit} object fitted with \code{svc} argument}

\item{terms}{Which SVC terms to extract. If NULL (default), extracts all.}

\item{summary}{Logical; if TRUE, return summary statistics instead of
full posterior draws.}

\item{probs}{Quantiles to compute if \code{summary = TRUE}.}

\item{...}{Ignored}
}
\value{
A \code{tulpa_svc_posterior} object containing:
\itemize{
\item \code{draws}: Array of posterior draws (draws x locations x terms)
\item \code{coords}: Coordinate matrix
\item \code{term_names}: Names of SVC terms
}
}
\description{
Extract posterior distributions of spatially-varying coefficients (SVCs)
from a fitted tulpa model with SVC specification.
}
\examples{
\donttest{
set.seed(303)
n <- 25L
df <- data.frame(lon = runif(n), lat = runif(n), x = rnorm(n))
bsurf <- 0.9 * sin(2.8 * df$lon) + 0.7 * cos(2.2 * df$lat)
df$count <- rpois(n, exp(0.2 + (0.8 + bsurf) * df$x))

# The varying slope on `x` is a spatial field; SVC is exact-mode only.
fit <- tulpa(
  count ~ x,
  data = df,
  family = "poisson",
  spatial = spatial_svc(~ lon + lat, terms = ~ x - 1, nn = 5L),
  mode = "exact",
  control = list(n_iter = 80L, n_warmup = 40L, seed = 1L)
)

svc_post <- svc(fit)
summary(svc_post)
}

}
\seealso{
\code{\link[=spatial_svc]{spatial_svc()}}, \code{\link[=plot.tulpa_svc_posterior]{plot.tulpa_svc_posterior()}}
}
