% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rational_spde.R
\name{.spde_assemble_at}
\alias{.spde_assemble_at}
\title{Assemble the fractional rSPDE precision and obs map at a given (range, sigma)}
\usage{
.spde_assemble_at(spatial, range, sigma, order = 2L)
}
\arguments{
\item{spatial}{A validated \code{spatial_spde} spec with fractional \code{nu}.}

\item{range, sigma}{Spatial range and marginal SD.}

\item{order}{Rational approximation order. Default 2 (the rSPDE convention),
which keeps the rational precision's condition number tractable.}
}
\value{
A list with \code{Q}, \code{Pr}, \code{A_eff}, \code{Pl} (all CSC, sized to the non-orphan
submesh), \code{keep} (1-based indices of the retained mesh nodes), \code{n_mesh_full}
(the full mesh size), \code{kappa}, \code{var_scale}, \code{l_max}, and \code{logdet_Q}.
}
\description{
Shared by every fractional-\code{nu} fit path (Laplace single-point, nested,
NUTS, marginal SEs). Maps \verb{(range, sigma)} to the SPDE hyperparameters
\verb{kappa = sqrt(8 nu) / range}, builds the latent precision \verb{Q = Pl' C^\{-1\} Pl}
and field shift \code{Pr} (field \verb{u = Pr x}) from the validated R oracle
\code{.spde_rational_assemble()}, then \strong{normalizes the field to marginal variance
\code{sigma^2}}. The rational construction is correct in spectral shape but its
overall scale carries kappa-dependent constants (the \code{l_max} normalization,
the per-factor conditioning rescalings); without the variance normalization
the implied field variance is not \code{sigma^2} and varies with the range, which
biases the nested \verb{(range, sigma)} integration. The normalization rescales
\code{Pr} (hence \verb{A_eff = A Pr}) by \verb{sigma / sqrt(mean marginal variance)}; \code{Q} is
untouched, so \code{logdet_Q} remains the correct prior normalizer for the
variance-normalized model.
}
\keyword{internal}
