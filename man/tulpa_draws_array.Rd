% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/convergence.R
\name{tulpa_draws_array}
\alias{tulpa_draws_array}
\title{Posterior draws as a 3D array}
\usage{
tulpa_draws_array(fit)
}
\arguments{
\item{fit}{A \code{tulpa_fit} (or subclass) carrying posterior \verb{$draws}.}
}
\value{
A numeric array with dimensions \verb{[n_iter, n_chain, n_param]} and the
parameter names on the third dimension, or \code{NULL} if the fit carries no
draws. Chains of unequal length are truncated to the shortest.
}
\description{
Assembles a fitted model's posterior draws into an \verb{[iteration, chain, parameter]} array -- the layout expected by \code{bayesplot} and analogous to
\code{posterior::as_draws_array()}. Multiple chains are recognised from a 3D
draws array, a \verb{$chain_id} row map, or an \verb{$n_chains} count over chain-major
rows; a single pooled chain yields a one-chain array.
}
\details{
As with \code{\link[=posterior_sample]{posterior_sample()}}, a fit that carries no draws returns \code{NULL} with
a message naming its backend and the representation it carries instead.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(x = rnorm(60))
d$y <- rpois(60, exp(0.5 + 0.3 * d$x))
fit <- tulpa(y ~ x, data = d, family = "poisson", mode = "hmc",
             control = list(n_iter = 400L, warmup = 200L, n_chains = 2L,
                            seed = 1L))
dim(tulpa_draws_array(fit))   # [iteration, chain, parameter]
}
}
\seealso{
\code{\link[=posterior_sample]{posterior_sample()}}, \code{\link[=tulpa_posterior_draws]{tulpa_posterior_draws()}}, \code{\link[=diagnostics]{diagnostics()}}
}
