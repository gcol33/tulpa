% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_generic.R
\name{logLik.tulpa_fit}
\alias{logLik.tulpa_fit}
\title{The fit's log-scale goodness quantity}
\usage{
\method{logLik}{tulpa_fit}(object, ...)
}
\arguments{
\item{object}{A \code{tulpa_fit} object.}

\item{...}{Ignored.}
}
\value{
A \code{logLik} object with \code{quantity} and \code{conditioned_on} attributes,
and a \code{declined} attribute naming the reason when no value could be read.
}
\description{
What this returns depends on what the fit computed, and the returned object
names it in a \code{quantity} attribute:
}
\details{
\describe{
\item{\code{"log_posterior_mean"}}{a sampler fit: the mean over the draws of
the log joint posterior density, prior included, in that backend's own
parameterization: the unconstrained coordinates with their Jacobians
for the ModelData samplers (\code{hmc}, \code{ess}, \code{sghmc}, \code{sgld}, \code{mclmc},
\code{smc}, \code{vi}), and the natural scale each quantity is sampled on for the
RE-covariance and Polya-Gamma Gibbs samplers. Values are therefore
comparable across fits of the same backend only.}
\item{\code{"log_evidence"}}{a deterministic fit that estimated no
hyperparameter from the data: the log marginal probability of the data
under the model as specified. A hyperparameter the fit integrated counts,
and so does one the caller supplied (a \code{phi}, a \code{sigma_re}, an outer grid
laid at one value), which is part of the model rather than an estimate. A
Laplace fit of a model with nothing to integrate reports its log marginal
likelihood here, which already is that quantity.}
\item{\code{"log_marginal_likelihood"}}{a deterministic fit that estimated some
hyperparameters by maximising over the same data (empirical Bayes, or
\code{estimate_phi = TRUE}): the log marginal likelihood at those estimates.
The \code{conditioned_on} attribute names them.}
\item{\code{"log_likelihood"}}{a fit that maximised over every parameter it
reports, with the random effects integrated out (\code{\link[=agq_fit]{agq_fit()}}): the
maximised log-likelihood, with \code{df} the number of maximised parameters.}
}

On a nested-Laplace fit the value is the log evidence of its outer grid,
\verb{log sum_k exp(log_marginal_k + log_cell_k)}, where \code{log_marginal} already
carries each axis's hyperprior density on its integration coordinate and
\code{log_cell_k} is the cell's absolute volume there. Every default outer axis
carries a proper prior (a PC prior on each scale and range, a uniform on each
bounded axis), so the value is the evidence under that prior and does not move
with where the nodes were laid or how many there are, provided the grid
covers the posterior. A fit carrying an axis with no proper prior (a flat
random-effect covariance prior, a dispersion whose family has no PC prior yet,
an axis on a block that does not carry its coordinates) reports \code{NA} with
\code{declined = "improper_hyperprior"} and names the axes in a \code{declined_axes}
attribute. A central-composite (CCD) design reproduces moments and carries no
cell volume, so a fit integrated on one reports \code{NA} with a \code{declined}
attribute.

A sampler fit whose producer kept no per-draw log posterior reports \code{NA}
with \code{quantity = "log_posterior_mean"} and
\code{declined = "no_log_posterior_recorded"}. The Polya-Gamma Gibbs routes that
recentre a proper field every sweep (the NNGP and multiscale-GP fields, and
the negative-binomial kernel's iid random-effect block) leave no written
density invariant and record none; a fit carrying neither draws
nor a log marginal declines with \code{"no_goodness_quantity_recorded"}. A value
of \code{NA} always carries a \code{declined} attribute.

Values with a different \code{quantity} or \code{conditioned_on} are not on one scale,
and \code{compare_models(criterion = "loglik")} refuses such a set. Only a
\code{"log_likelihood"} is what AIC and BIC penalise, so \code{\link[=AIC.tulpa_fit]{AIC.tulpa_fit()}} and
\code{\link[=BIC.tulpa_fit]{BIC.tulpa_fit()}} refuse every other quantity; compare those fits by their
evidence, or by \code{compare_models(criterion = "waic")} / \code{"loo"}, which score
the pointwise predictive density.
}
