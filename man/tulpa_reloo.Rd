% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kfold.R
\name{tulpa_reloo}
\alias{tulpa_reloo}
\title{Selective refit of high-Pareto-k observations (reloo)}
\usage{
tulpa_reloo(object, data, k_threshold = 0.7, n_trials = NULL, ndraws = NULL)
}
\arguments{
\item{object}{A \code{tulpa_fit} fitted through \code{\link[=tulpa]{tulpa()}} (must carry \verb{$call}).}

\item{data}{The data frame the model was fit to.}

\item{k_threshold}{Pareto k-hat above which an observation is refit
(default 0.7, the standard PSIS reliability gate).}

\item{n_trials}{Optional binomial denominators (length \code{nrow(data)});
defaults to the trials stored on the fit, else 1.}

\item{ndraws}{Number of posterior draws used for the PSIS-LOO baseline
(defaults to all stored draws, or 400 on the draw-free Laplace tier).}
}
\value{
A list with \code{elpd_loo} (corrected), \code{se_elpd_loo}, \code{looic},
\code{pointwise} (per-observation elpd, exact at the refit observations),
\code{reloo_idx} (indices refit), \code{pareto_k} (the original k-hat values), and
\code{k_threshold}.
}
\description{
PSIS-LOO with exact refits where the importance sampling is unreliable:
observations whose Pareto k-hat exceeds \code{k_threshold} are re-scored by
refitting the model without that observation (through the fit's stored
\code{\link[=tulpa]{tulpa()}} call, as in \code{\link[=tulpa_kfold]{tulpa_kfold()}}) and evaluating the exact held-out
log predictive density. All other observations keep their PSIS-LOO value,
so the cost is one refit per flagged observation rather than per fold.

The same restrictions as \code{\link[=tulpa_kfold]{tulpa_kfold()}} apply: fixed-effect / GLMM fits
only (subsetting breaks a spatial / temporal field), and held-out
random-effect groups contribute at their prior mean.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(x = rnorm(120))
d$y <- rpois(120, exp(0.4 + 0.6 * d$x))
fit <- tulpa(y ~ x, data = d, family = "poisson", mode = "laplace")
rl  <- tulpa_reloo(fit, data = d)
rl$elpd_loo
}
}
\seealso{
\code{\link[=tulpa_kfold]{tulpa_kfold()}} for the full refit-CV; \code{\link[=tulpa_criteria]{tulpa_criteria()}} for
PSIS-LOO / WAIC without refits.
}
