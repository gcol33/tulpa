% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/family_loglik.R
\name{.numeric_response}
\alias{.numeric_response}
\title{The response as the numeric vector the kernels read.}
\usage{
.numeric_response(family, y)
}
\arguments{
\item{family}{Family identifier, canonical or aliased.}

\item{y}{The response, or \code{NULL}.}
}
\description{
A logical response is the binary idiom \code{glm()} accepts for a binomial
family, \code{TRUE} a success; it is read as 0/1 there. Anywhere else, and for
any other non-numeric response (a factor, a character vector), the family
has no reading of it, so it is refused by name rather than left to fail
inside a backend in that backend's words -- \verb{is.numeric(y) is not TRUE} on
the Laplace door, a failed inner solve on EB, non-finite integration nodes
on the structured tier (gcol33/tulpa#880).
}
\keyword{internal}
