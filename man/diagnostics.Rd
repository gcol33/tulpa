% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{diagnostics}
\alias{diagnostics}
\alias{diagnostics.default}
\title{Posterior diagnostics for a fitted model}
\usage{
diagnostics(fit, ...)

\method{diagnostics}{default}(
  fit,
  pars = NULL,
  measures = c("rhat", "ess_bulk", "ess_tail"),
  probs = c(0.05, 0.95),
  ...
)
}
\arguments{
\item{fit}{A \code{tulpa_fit} (or subclass) carrying posterior \verb{$draws}. Multiple
chains are recognised from a 3D \verb{[iter, chain, param]} draws array, a
\verb{$chain_id} row map, or an \verb{$n_chains} count over chain-major rows.}

\item{...}{Passed to the method.}

\item{pars}{Optional character vector of parameter names to restrict to.}

\item{measures}{Character vector selecting which diagnostics to compute, in
output-column order. Available: \code{"rhat"}, \code{"rhat_bulk"}, \code{"rhat_fold"},
\code{"ess_bulk"}, \code{"ess_tail"}, \code{"ess_mean"}, \code{"ess_sd"}, \code{"mcse_mean"},
\code{"mcse_sd"}, \code{"ess_quantile"}, \code{"mcse_quantile"}. Defaults to the core set
\code{c("rhat", "ess_bulk", "ess_tail")}. Applies to chain fits; the
approximation-reliability table has a fixed set of columns.}

\item{probs}{Numeric probabilities for the quantile-based measures
(\code{"ess_quantile"}, \code{"mcse_quantile"}); each expands to one column named
e.g. \code{ess_q5}, \code{ess_q95}. Default \code{c(0.05, 0.95)}. Chain fits only.}
}
\value{
For a chain fit, a data frame with a \code{parameter} column followed by
one column per requested measure; entries are \code{NA} for parameters that are
constant or have too few draws. For an i.i.d. approximation fit, the
\code{laplace_diagnostics} table (see that function for its attributes). For a
point fit, \code{NULL}.
}
\description{
The diagnostic front door for any tulpa fit. What a fit's posterior draws
can be asked depends on how they were produced, so this reads the draws
provenance and returns the diagnostic that applies:

\describe{
\item{MCMC chain draws}{improved Rhat (the maximum of rank-normalized
split-Rhat and folded split-Rhat), bulk / tail / mean / sd / quantile
effective sample size, and Monte Carlo standard errors, following
Vehtari et al. (2021). Split-Rhat is defined for a single chain, so a
result is produced for any number of chains.}
\item{i.i.d. approximation draws}{the approximation-reliability table --
the PSIS tail-shape \code{pareto_k} scored against the exact inner-Laplace
marginal, the outer-grid quadrature effective sample size, and a
per-parameter posterior summary. See \code{\link[=laplace_diagnostics]{laplace_diagnostics()}} for the
full description of this table and its attributes.}
\item{point summaries}{no sample to diagnose; returns \code{NULL} with a
message naming the backend.}
}

Provenance is read from \verb{$draws_kind} (stamped by \code{tulpa_dispatch}), falling
back to the backend registry's \code{emits} property and then to an inner
\verb{$joint_fit}. A fit that predates the tag is treated as a chain, so an older
fit is never silently refused.
}
\section{Extending}{

\code{diagnostics()} is an S3 generic so a model package can answer for its own
fit class (\code{diagnostics.ratiod_fit()}, say) rather than shadowing this
export with a same-named function. The default method does the provenance
routing described above and is what a method should delegate to once it has
assembled a draws array.
}

\examples{
\donttest{
set.seed(1)
df <- data.frame(x = rnorm(60))
df$y <- rpois(60, exp(0.5 + 0.3 * df$x))

# chain fit -> Rhat / ESS
hmc <- tulpa(y ~ x, data = df, family = "poisson", mode = "hmc",
             control = list(n_iter = 500L, warmup = 250L, n_chains = 2L,
                            seed = 1L))
diagnostics(hmc)

# deterministic fit -> PSIS approximation reliability
smc <- tulpa(y ~ x, data = df, family = "poisson", mode = "smc")
diagnostics(smc)
}
}
\references{
Vehtari, Gelman, Simpson, Carpenter & Burkner (2021). Rank-normalization,
folding, and localization: an improved Rhat for assessing convergence of
MCMC. \emph{Bayesian Analysis} 16(2):667-718.

Vehtari, Simpson, Gelman, Yao & Gabry (2024). Pareto smoothed importance
sampling. \emph{JMLR} 25(72):1-58.
}
\seealso{
\code{\link[=laplace_diagnostics]{laplace_diagnostics()}} for the approximation-reliability table in
full, \code{\link[=tulpa_draws_array]{tulpa_draws_array()}}, \code{\link[=plot_rhat]{plot_rhat()}}, \code{\link[=plot_ess]{plot_ess()}},
\code{\link[=diagnostic_summary]{diagnostic_summary()}}, \code{\link[=check_diagnostics]{check_diagnostics()}}
}
