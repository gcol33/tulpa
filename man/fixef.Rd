% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_interop.R
\name{fixef}
\alias{fixef}
\alias{fixef.tulpa_fit}
\title{Fixed-effect coefficients (lme4-compatible)}
\usage{
fixef(object, ...)

\method{fixef}{tulpa_fit}(object, ...)
}
\arguments{
\item{object}{A \code{tulpa_fit} object.}

\item{...}{Ignored.}
}
\value{
Named numeric vector of fixed-effect estimates.
}
\description{
The fixed-effect point estimates, equivalent to \code{\link[=coef]{coef()}} on a \code{tulpa_fit}.
Provided under the \code{fixef} name for code written against the \code{lme4} / \code{nlme}
interface: \code{lme4::fixef(fit)} and \code{nlme::fixef(fit)} dispatch here too when
either package is installed, so a \code{tulpa_fit} can be dropped into an
\code{lme4}-shaped workflow.
}
\details{
Note the deliberate difference from \code{lme4::coef.merMod}, which returns
per-group sums of the fixed and random effects. On a \code{tulpa_fit}, \code{\link[=coef]{coef()}}
returns the fixed effects alone and \code{fixef()} is its synonym; the random
effects are \code{\link[=ranef]{ranef()}}.
}
\examples{
\donttest{
set.seed(1)
df <- data.frame(x = rnorm(80))
df$y <- rpois(80, exp(0.5 + 0.3 * df$x))
fit <- tulpa(y ~ x, data = df, family = "poisson")
fixef(fit)
}
}
\seealso{
\code{\link[=coef.tulpa_fit]{coef.tulpa_fit()}}, \code{\link[=ranef]{ranef()}}
}
