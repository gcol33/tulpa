% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rational_spde.R
\name{.spde_mean_marginal_var}
\alias{.spde_mean_marginal_var}
\title{Mean marginal variance of the rSPDE field u = Pr x, x ~ N(0, Q^{-1})}
\usage{
.spde_mean_marginal_var(Q, Pr, C0, n_probe = .SPDE_VARNORM_NPROBE)
}
\description{
Estimates \verb{mean_i [Pr Q^\{-1\} Pr']_ii = tr(Pr Q^\{-1\} Pr') / n} by Hutchinson
probing: for \code{z ~ N(0, I)}, \verb{a = Pr' z}, \verb{Q v = a}, then \verb{E[a' v] = tr(...)}.
The solve is against \code{Q} through the SAME sparse Cholesky the precomputed C++
fit uses, so the normalization is consistent with the fit even when the wide
rational spectrum makes \code{Q} ill-conditioned: a shared solver makes the implied
field covariance identical between the normalization and the likelihood, which
is what the nested \verb{(range, sigma)} integration needs (an inconsistent solver
breaks the cross-grid marginal). The probe matrix is fixed across calls for a
deterministic, grid-smooth normalization.
}
\keyword{internal}
