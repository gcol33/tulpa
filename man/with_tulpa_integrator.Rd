% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/integrator.R
\name{with_tulpa_integrator}
\alias{with_tulpa_integrator}
\title{Run an expression under a chosen symplectic integrator}
\usage{
with_tulpa_integrator(name, expr, mts_substeps = 4L)
}
\arguments{
\item{name}{Integrator name, as for \code{\link[=tulpa_integrator]{tulpa_integrator()}}.}

\item{expr}{Expression to evaluate. Evaluated in the caller's environment.}

\item{mts_substeps}{Inner prior-force substeps for \code{"mts"} (default 4).}
}
\value{
The value of \code{expr}.
}
\description{
Evaluate \code{expr} with the integrator set to \code{name}, then restore whatever was
selected before -- on an error as well as on success. The integrator
selection is process-global, so a bare \code{\link[=tulpa_integrator]{tulpa_integrator()}} call leaves every
later fit in the session on the new scheme, and an error between setting and
restoring leaves it there permanently.
}
\examples{
with_tulpa_integrator("yoshida4", tulpa_integrator())
tulpa_integrator()   # unchanged

}
\seealso{
\code{\link[=tulpa_integrator]{tulpa_integrator()}}
}
