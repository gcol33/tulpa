% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyper_quadrature.R
\name{tulpa_hyper_check_copy_slab}
\alias{tulpa_hyper_check_copy_slab}
\title{Validate/default a copy-scale slab measure choice}
\usage{
tulpa_hyper_check_copy_slab(x)
}
\arguments{
\item{x}{A \code{copy_slab} value: \code{NULL}, \code{"exponential"}, or \code{"flat"}.}
}
\value{
\code{x}, defaulted to \code{"exponential"} when \code{NULL}.
}
\description{
Validates a \code{copy_slab} argument -- \code{"exponential"} (the default) or
\code{"flat"}, the two continuum measures tulpa supports for a copy scale's
non-atom mass -- defaulting \code{NULL} to \code{"exponential"} and erroring on
anything else. Intended so a consumer package's own \code{copy_slab} argument
stays in sync with tulpa's own accepted choices rather than restating
them.
}
\examples{
tulpa_hyper_check_copy_slab("exponential")
try(tulpa_hyper_check_copy_slab("uniform"))
}
\seealso{
\code{\link[=tulpa_hyper_copy_slab_density]{tulpa_hyper_copy_slab_density()}}
}
