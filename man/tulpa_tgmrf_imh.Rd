% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tgmrf_imh.R
\name{tulpa_tgmrf_imh}
\alias{tulpa_tgmrf_imh}
\title{IMH-Laplace MCMC over a tgmrf block's hyperparameters}
\usage{
tulpa_tgmrf_imh(
  y,
  n_trials,
  X,
  block,
  family = "binomial",
  phi = 1,
  re_idx = NULL,
  n_re_groups = 0L,
  sigma_re = 1,
  n_iter = 2000L,
  warmup = n_iter\%/\%2L,
  thin = 1L,
  scale = 1,
  pilot_axis_points = 5L,
  fd_step = 0.05,
  max_iter = 50L,
  tol = 1e-07,
  n_threads = 1L,
  verbose = FALSE
)
}
\arguments{
\item{y, n_trials, X}{Response, trials per obs (1L vector for non-binomial),
and fixed-effects design matrix -- same arguments as
\code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}}.}

\item{block}{A \code{tgmrf} object.}

\item{family, phi}{Likelihood family and dispersion.}

\item{re_idx, n_re_groups, sigma_re}{Optional RE wiring (forwarded to
the inner Laplace).}

\item{n_iter, warmup, thin, scale}{MCMC arguments forwarded to
\code{\link[=imh_laplace]{imh_laplace()}}.}

\item{pilot_axis_points}{Per-axis grid resolution for the pilot fit
(default 5).}

\item{fd_step}{Step size for the finite-difference Hessian on
\code{log_marginal(theta)} (default 0.05; in the block's theta units).}

\item{max_iter, tol}{Inner-Newton budget per proposal.}

\item{n_threads}{OpenMP threads inside each inner Laplace solve.}

\item{verbose}{Forwarded to \code{\link[=imh_laplace]{imh_laplace()}}.}
}
\value{
A list with class \code{c("tulpa_tgmrf_imh", "tulpa_fit")}:
\itemize{
\item \code{draws}: posterior draws of \code{theta} (rows = post-warmup samples,
cols = \code{theta_dim}).
\item \code{means}, \code{sds}: posterior moments.
\item \code{mean_accept}: post-warmup acceptance rate.
\item \code{pilot}: the pilot \code{tulpa_nested_laplace} object (for diagnostics).
\item \code{mode_theta}, \code{hessian_theta}: pilot-derived mode + FD Hessian.
\item \code{inference_mode}: \code{"exact"}.
\item \code{inference_tier}: \code{1L} (MH-corrected).
\item \code{backend}: \code{"tgmrf_imh"}.
}
}
\description{
Composes the Laplace adapter (\code{tulpa_nested_laplace()}) with the
generic \code{\link[=imh_laplace]{imh_laplace()}} independence-Metropolis sampler to obtain
asymptotically-correct posterior draws for the tgmrf block's
hyperparameter vector \code{theta}.

The pipeline is the canonical "Laplace body + MH bias correction" used
throughout tulpa's debias philosophy:
\enumerate{
\item Pilot grid fit to get a coarse \code{log_marginal(theta_k)} landscape.
\item Localise the mode in theta-space (grid argmax) and estimate a
finite-difference Hessian around it.
\item Run IMH-Laplace with proposals
\verb{theta' ~ N(mode, scale^2 * H^\{-1\})} and exact MH accept/reject
against the target
\verb{log p(theta | y) = log_marginal(theta) + log p(theta)}
(the user prior is already embedded in \code{log_marginal} via the
\code{prior} closure attached to the block).
\item Each proposal \verb{theta'} is evaluated by a single-point inner
Laplace solve through \code{cpp_nested_laplace_multi} -- \code{n_iter}
inner solves total.
}

Because the body (Laplace approximation of \verb{(beta, z) | theta}) is
Gaussian and the bias is corrected by MH, the resulting draws are
exact in the limit. For posteriors that are nearly Laplace-Gaussian
the acceptance rate is high and the sampler is dramatically cheaper
than full NUTS over the joint \verb{(beta, z, theta)}.
}
\seealso{
\code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}} for the grid-only Laplace,
\code{\link[=imh_laplace]{imh_laplace()}} for the generic MH wrapper.
}
