% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/control_check.R
\name{tulpa_check_control}
\alias{tulpa_check_control}
\title{Validate a \code{control = list()} surface against its canonical key set}
\usage{
tulpa_check_control(control, allowed, where)
}
\arguments{
\item{control}{The \code{control} list to validate. \code{NULL} and empty lists pass.}

\item{allowed}{Character vector of accepted knob names.}

\item{where}{Name of the calling fitter, used in the error message.}
}
\value{
\code{invisible(NULL)}, called for the side effect of erroring on an
unknown or unnamed knob.
}
\description{
Front-door fitters across the \verb{tulpa*} ecosystem carry their perf /
numerical / tuning knobs in a single \code{control} list (design principle 6).
Without a name check a misspelled knob is a silent no-op --
\code{control$adaptve_grid} fits the default and reports nothing. This validates
the names a caller supplied against the whitelist of what the target fitter
actually reads, and errors listing the allowed set.
}
\details{
Consumer packages (\code{tulpaRatio}, \code{tulpaObs}) call this with their own key
registry rather than reimplementing the check.
}
\examples{
tulpa_check_control(list(max_iter = 50), c("max_iter", "tol"), "my_fit")
try(tulpa_check_control(list(max_itr = 50), c("max_iter", "tol"), "my_fit"))

}
