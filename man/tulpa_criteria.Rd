% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/criteria.R
\name{tulpa_criteria}
\alias{tulpa_criteria}
\title{Model criteria from a pointwise log-likelihood}
\usage{
tulpa_criteria(
  log_lik,
  criteria = c("waic", "loo", "cpo", "lpml", "dic"),
  loglik_at_mean = NULL,
  group = NULL,
  chunk_size = NULL,
  pointwise = FALSE
)
}
\arguments{
\item{log_lik}{An \verb{[n_draws x n_obs]} numeric matrix of pointwise
log-likelihoods, or a \code{\link[=tulpa_loglik]{tulpa_loglik()}} streaming wrapper.}

\item{criteria}{Which criteria to compute. Any of \code{"waic"}, \code{"loo"},
\code{"cpo"}, \code{"lpml"}, \code{"dic"}. \code{"loo"}, \code{"cpo"}, and \code{"lpml"} share the single
PSIS pass; \code{"dic"} additionally needs \code{loglik_at_mean}.}

\item{loglik_at_mean}{Optional length-\code{n_obs} vector of pointwise
log-likelihoods evaluated at the posterior mean of the parameters, supplied
by the caller (the model package knows the parameterization). Required for
DIC's plug-in deviance; without it the DIC fields are \code{NA}.}

\item{group}{Optional length-\code{n_obs} grouping (an integer / factor /
character vector). The LOO unit is \strong{one column of \code{log_lik}}: with
\code{group = NULL} (the default) every column is its own fold (leave-one-row-out,
e.g. per plot / per visit) and the result is byte-identical to the
ungrouped call. When supplied, the per-draw pointwise log-likelihoods are
summed within group to a \verb{[n_draws x n_groups]} matrix \strong{before} PSIS, so
each fold is a whole group (leave-one-group-out cross-validation, LOGO-CV).
Use it to switch the estimand from per-row to per-group LOO -- e.g. on a
cell-compressed hierarchical fit, leave out a whole cell rather than one of
its rows. WAIC's variance term, \code{lppd}, \code{elpd_loo}, \code{cpo} and \code{pareto_k}
are all computed on the grouped matrix. DIC is a plug-in deviance over all
observations and is unaffected by \code{group}.}

\item{chunk_size}{Number of observation columns to process per block. The
default streams the whole matrix at once when materialized, else picks a
block sized to a few million entries.}

\item{pointwise}{If \code{TRUE}, also return the per-observation vectors
(\code{elpd_waic}, \code{p_waic}, \code{elpd_loo}, \code{pareto_k}, \code{cpo}) for plotting /
stacking.}
}
\value{
A \code{tulpa_criteria} object: a list with the requested scalar scores
(each estimate paired with its standard error where defined),
\code{n_draws} / \code{n_obs}, the PSIS \code{pareto_k} summary, and -- when
\code{pointwise = TRUE} -- a \code{pointwise} data frame.
}
\description{
Turn an \verb{[n_draws x n_obs]} pointwise log-likelihood into the standard
Bayesian goodness-of-fit currency: WAIC, DIC, conditional predictive
ordinates (CPO) and their sum LPML, PSIS-LOO, all from one matrix. PSIS-LOO
reuses the native \code{\link[=tulpa_psis]{tulpa_psis()}} smoothing, so the CPO and LOO numbers are
the same computation (\code{CPO_i = exp(elpd_loo_i)}, \code{LPML = elpd_loo}). The
input may be a matrix or a streaming \code{\link[=tulpa_loglik]{tulpa_loglik()}} so EVA-scale fits are
processed in observation blocks.
}
\details{
\code{p_waic} is the well-known positively-biased variance estimator at low draw
counts; the result records \code{n_draws} and the count of observations with
\code{p_waic_i > 0.4} (the \code{loo} heuristic for an unreliable WAIC), and the
PSIS-LOO \code{elpd_loo} is the more stable figure to report when that count is
non-trivial.

The LOO unit is whatever \strong{one column of \code{log_lik}} holds. If the consumer
built the matrix with one column per row (plot / visit), the default is
per-row LOO; if a column already carries a whole group's compressed
likelihood, leaving it out drops that group and \code{pareto_k} can blow up by
construction. The \code{group} argument makes the unit explicit: supply it to
aggregate columns into folds and report leave-one-group-out CV (LOGO-CV)
instead, a different and deliberate estimand.
}
\references{
Vehtari, Gelman & Gabry (2017). Practical Bayesian model evaluation using
leave-one-out cross-validation and WAIC. \emph{Statistics and Computing}
27(5):1413-1432. Watanabe (2010). Spiegelhalter et al. (2002). Geisser &
Eddy (1979).
}
\seealso{
\code{\link[=tulpa_psis]{tulpa_psis()}} for the smoothing core, \code{\link[=tulpa_pit]{tulpa_pit()}} for the
probability-integral-transform companion, \code{\link[=compare_models]{compare_models()}} for
model comparison.
}
