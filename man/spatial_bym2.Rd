% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_car.R
\name{spatial_bym2}
\alias{spatial_bym2}
\title{BYM2 spatial structure}
\usage{
spatial_bym2(
  adjacency,
  level = c("group", "obs"),
  group_var = NULL,
  shared = NULL,
  scale_factor = NULL,
  parameterization = c("standard", "collapsed")
)
}
\arguments{
\item{adjacency}{Symmetric adjacency matrix (\verb{[n_units x n_units]}).}

\item{level}{Either \code{"group"} (one effect per level of \code{group_var}) or
\code{"obs"} (one effect per row of the data; \code{nrow(data)} must equal
\code{nrow(adjacency)}).}

\item{group_var}{Name of the grouping variable in the data; required
when \code{level = "group"}.}

\item{shared}{Optional shared-effect handle (see model docs).}

\item{scale_factor}{Scaling factor for the ICAR component. If NULL
(default), computed from the adjacency matrix following Riebler et al.}

\item{parameterization}{\code{"standard"} (default) or \code{"collapsed"} (deprecated).}
}
\value{
A \code{tulpa_spatial} object
}
\description{
Specify a Besag-York-Mollie 2 (BYM2) spatial random effect.
BYM2 decomposes the spatial effect into a structured (ICAR) component
and an unstructured (IID) component, with a mixing parameter controlling
the proportion of variance attributable to spatial structure.

BYM2 is preferred over plain CAR when you want to:
\itemize{
\item Distinguish structured vs unstructured spatial variation
\item Have an interpretable spatial fraction parameter
\item Use the scaling from Riebler et al. (2016)
}
}
\examples{
# Create adjacency matrix for 10 regions (chain structure)
adj <- matrix(0, 10, 10)
for (i in 1:9) {
  adj[i, i+1] <- adj[i+1, i] <- 1
}

# Create BYM2 spatial structure
bym2 <- spatial_bym2(adj, level = "group", group_var = "region")
print(bym2)

\dontrun{
# Generate synthetic epidemiological data (not run - requires laplace backend)
set.seed(456)
n_regions <- 10
epi_data <- data.frame(
  region = factor(1:n_regions),
  age = rnorm(n_regions, 50, 10),
  cases = rbinom(n_regions, size = 100, prob = 0.15),
  population = rep(100L, n_regions)
)

# Disease mapping with BYM2 spatial smoothing
fit <- tulpa(
  cases | population ~ age,
  spatial = spatial_bym2(adj, level = "group", group_var = "region"),
  data = epi_data,
  family = tulpa_binomial(),
  backend = "laplace"
)
summary(fit)
}

}
\references{
Riebler, A., Sorbye, S. H., Simpson, D., & Rue, H. (2016). An intuitive
Bayesian spatial model for disease mapping that accounts for scaling.
Statistical Methods in Medical Research, 25(4), 1145-1165.
}
