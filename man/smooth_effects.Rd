% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoother.R
\name{smooth_effects}
\alias{smooth_effects}
\title{Extract fitted covariate smooths}
\usage{
smooth_effects(object, term = 1L)
}
\arguments{
\item{object}{A \code{tulpa_fit} from \code{\link[=tulpa]{tulpa()}} with \code{s(...)} term(s) in the
formula.}

\item{term}{Which smoother: index or covariate name. Default 1.}
}
\value{
A data frame with columns \code{x} (node location) and \code{estimate} (the
posterior-mean smooth at that node), with the covariate name as an
attribute \code{"var"}.
}
\description{
The grid-marginalized posterior mean of each \code{s(x)} term's latent values,
one estimate per node (bin midpoint or unique covariate value). The latent
block values are read from the fit's per-grid modes, weighted by the
hyperparameter grid weights.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(x = runif(300, -2, 2))
d$y <- rpois(300, exp(0.3 + sin(2 * d$x)))
fit <- tulpa(y ~ s(x), data = d, family = "poisson")
sm <- smooth_effects(fit)
plot(sm$x, sm$estimate, type = "l")
}
}
