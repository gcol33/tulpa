% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_core.R
\name{temporal_ar1}
\alias{temporal_ar1}
\title{AR1 temporal structure (First-order Autoregressive)}
\usage{
temporal_ar1(time_var, group_var = NULL, shared = NULL, rho_prior = NULL)
}
\arguments{
\item{time_var}{A formula (\code{~ time}) or single character string naming the
time variable in the data.}

\item{group_var}{Optional formula (\code{~ g}) or character string naming a
grouping variable. When supplied, a separate random walk is fit per group;
\code{NULL} (default) fits a single walk shared across all observations.}

\item{shared}{Whether the temporal effect is shared across processes in a
multi-process model. \code{NULL} (default) shares the effect; \code{FALSE} fits
process-specific effects and emits a warning about unshared confounding.}

\item{rho_prior}{Prior for the autocorrelation parameter. Default is
\code{NULL} which uses a Uniform(-1, 1) prior. Can specify a Beta prior
on (rho+1)/2 for more informative priors.}
}
\value{
A \code{tulpa_temporal} object
}
\description{
Specify a first-order autoregressive temporal random effect.
AR1 models temporal correlation where each time point depends on
the previous one: \code{phi[t] = rho * phi[t-1] + epsilon[t]}.

Unlike RW1/RW2, AR1 is a proper (stationary) model with an estimated
autocorrelation parameter rho.
}
\details{
The AR1 process has marginal variance sigma^2 / (1 - rho^2) and
correlation between time points t and s of rho^|t-s|.

The precision matrix is tridiagonal and full rank, so no constraints
are needed.
}
\examples{
# Create temporal AR1 specification
temporal_ar1("year")
temporal_ar1("year", group = "site")

\donttest{
# AR1 temporal correlation
set.seed(127)
df <- data.frame(
  year = rep(2005:2024, each = 3),
  x = rnorm(60),
  count = rpois(60, lambda = 18),
  effort = rgamma(60, shape = 3.5, rate = 1)
)

fit <- tulpa(
  count | effort ~ x,
  data = df,
  family = tulpa_poisson_gamma(),
  temporal = temporal_ar1("year"),
  backend = "hmc",
  iter = 200,
  warmup = 100,
  chains = 1
)

# Panel data with group-specific AR1
set.seed(128)
df_panel <- data.frame(
  year = rep(2012:2027, each = 3),
  site = rep(paste0("site", 1:3), times = 16),
  x = rnorm(48),
  count = rpois(48, lambda = 12),
  effort = rgamma(48, shape = 2.5, rate = 1)
)

fit2 <- tulpa(
  count | effort ~ x,
  data = df_panel,
  family = tulpa_poisson_gamma(),
  temporal = temporal_ar1("year", group_var = "site"),
  backend = "hmc",
  iter = 200,
  warmup = 100,
  chains = 1
)
}

}
