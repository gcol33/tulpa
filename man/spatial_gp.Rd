% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{spatial_gp}
\alias{spatial_gp}
\title{Gaussian Process spatial structure}
\usage{
spatial_gp(
  coords,
  cov = c("exponential", "matern", "gaussian", "spherical"),
  nu = 1.5,
  nn = 15,
  solver = c("auto", "cholesky", "cg", "pcg", "gpu"),
  cg_tol = 1e-06,
  cg_maxiter = 100,
  shared = NULL,
  scale_coords = TRUE,
  parameterization = c("centered", "noncentered", "collapsed")
)
}
\arguments{
\item{coords}{A one-sided formula specifying coordinate columns (e.g.,
\code{~ lon + lat}), or a character vector of length 2 with column names.}

\item{cov}{Covariance function: \code{"exponential"} (default), \code{"matern"},
\code{"gaussian"}, or \code{"spherical"}.}

\item{nu}{Smoothness parameter for Matern covariance. Common values:
\itemize{
\item 0.5: Equivalent to exponential (rough, once mean-square differentiable)
\item 1.5: Once differentiable (moderate smoothness)
\item 2.5: Twice differentiable (smooth)
Ignored for non-Matern covariance functions.
}}

\item{nn}{Number of nearest neighbors for NNGP approximation. Default 15.
Larger values give better approximation but slower computation.}

\item{solver}{Linear algebra solver for GP computations:
\itemize{
\item \code{"auto"} (default): Automatically select based on problem size.
Uses Cholesky for N < 2000, CG for larger problems. Uses GPU if
available and N > 5000.
\item \code{"cholesky"}: Direct Cholesky decomposition. Exact but O(N*k^3).
Best for smaller datasets or when high precision is critical.
\item \code{"cg"}: Conjugate Gradient iterative solver. O(N\emph{k^2}iter).
Better for large N (> 2000) with good preconditioning.
\item \code{"pcg"}: Preconditioned CG with diagonal preconditioner.
Faster convergence than CG for ill-conditioned systems.
\item \code{"gpu"}: GPU-accelerated batched Cholesky using CUDA (if available).
Requires tulpa to be compiled with GPU support. Falls back to PCG
if GPU is unavailable. Best for large datasets (N > 5000) with
CUDA-capable GPU. Use \code{\link[=gpu_available]{gpu_available()}} to check support.
}}

\item{cg_tol}{Convergence tolerance for CG/PCG solvers. Default 1e-6.
Smaller values give more accurate solutions but slower convergence.}

\item{cg_maxiter}{Maximum iterations for CG/PCG. Default 100.}

\item{shared}{Logical; if TRUE (default), spatial effect enters both
all processes. Set to FALSE for process-specific spatial
effects (triggers warning about potential confounding).}

\item{scale_coords}{Logical; if TRUE (default), coordinates are scaled to
unit variance before computing distances.}

\item{parameterization}{GP parameterization: \code{"centered"} (default) samples
GP effects directly; \code{"noncentered"} uses z ~ N(0,1) with scaling;
\code{"collapsed"} (deprecated) marginalizes via inner Laplace.}
}
\value{
A \code{tulpa_gp} object
}
\description{
Specify a Gaussian Process (GP) spatial random effect using coordinate-based
distances. Uses Nearest Neighbor Gaussian Process (NNGP) approximation for
computational efficiency with large datasets (scales to millions of observations).

This provides a continuous spatial effect that captures smooth spatial
variation, unlike CAR/BYM2 which require discrete areal units.
}
\details{
The GP spatial model adds a spatially-correlated random effect to the
linear predictor:

\deqn{\eta(s) = X\beta + w(s)}

where \eqn{w(s)} follows a Gaussian process:
\deqn{w(s) \sim GP(0, \sigma^2 C(d; \phi))}

The correlation function \eqn{C(d; \phi)} depends on distance \eqn{d} and
range parameter \eqn{\phi}:
\itemize{
\item \strong{Exponential}: \eqn{C(d) = \exp(-d/\phi)}
\item \strong{Matern}: \eqn{C(d) = \frac{2^{1-\nu}}{\Gamma(\nu)} (\sqrt{2\nu} d/\phi)^\nu K_\nu(\sqrt{2\nu} d/\phi)}
\item \strong{Gaussian}: \eqn{C(d) = \exp(-(d/\phi)^2)}
\item \strong{Spherical}: \eqn{C(d) = 1 - 1.5(d/\phi) + 0.5(d/\phi)^3} for \eqn{d < \phi}
}

\strong{NNGP approximation}: For computational tractability, we use the
Nearest Neighbor Gaussian Process (Datta et al., 2016), which conditions
each location on its k nearest neighbors. This reduces complexity from
O(n^3) to O(n*k^2), enabling models with millions of observations.
}
\examples{
# Create GP spatial structure
gp <- spatial_gp(~ lon + lat)
print(gp)

\donttest{
# Generate synthetic spatial data
set.seed(789)
n <- 50
df <- data.frame(
  lon = runif(n, 0, 10),
  lat = runif(n, 0, 10),
  depth = rnorm(n),
  temp = rnorm(n),
  count = rpois(n, 25),
  effort = rgamma(n, shape = 4, rate = 1)
)

# Continuous spatial effect with exponential covariance
fit <- tulpa(
  count | effort ~ depth + temp,
  data = df,
  family = tulpa_poisson_gamma(),
  spatial = spatial_gp(~ lon + lat),
  backend = "hmc",
  iter = 200,
  warmup = 100,
  chains = 1
)
summary(fit)

# Smoother spatial field with Matern covariance
fit2 <- tulpa(
  count | effort ~ depth + temp,
  data = df,
  family = tulpa_poisson_gamma(),
  spatial = spatial_gp(~ lon + lat, cov = "matern", nu = 1.5),
  backend = "hmc",
  iter = 200,
  warmup = 100,
  chains = 1
)
}

}
\references{
Datta, A., Banerjee, S., Finley, A. O., & Gelfand, A. E. (2016).
Hierarchical nearest-neighbor Gaussian process models for large
geostatistical datasets. Journal of the American Statistical Association,
111(514), 800-812.
}
\seealso{
\code{\link[=spatial_car]{spatial_car()}}, \code{\link[=spatial_bym2]{spatial_bym2()}} for areal spatial effects,
\code{\link[=spatial_svc]{spatial_svc()}} for spatially-varying coefficients,
\code{\link[=spatial_multiscale]{spatial_multiscale()}} for multi-scale spatial effects
}
