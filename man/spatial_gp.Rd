% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_gp.R
\name{spatial_gp}
\alias{spatial_gp}
\title{Gaussian process spatial structure (NNGP)}
\usage{
spatial_gp(
  coords,
  approx = c("nngp", "hsgp"),
  cov = c("exponential", "matern"),
  nu = 1.5,
  nn = 15,
  m = 6,
  c = 1.5,
  solver = c("auto", "cholesky", "cg", "pcg", "gpu"),
  cg_tol = 1e-06,
  cg_maxiter = 100,
  shared = NULL,
  scale_coords = TRUE,
  parameterization = c("centered", "noncentered", "collapsed")
)
}
\arguments{
\item{coords}{A formula (\code{~ lon + lat}) or character vector of length 2
naming the two coordinate variables in the data.}

\item{approx}{GP approximation: \code{"nngp"} (default, a nearest-neighbour GP with
the \code{cov} / \code{nu} / \code{nn} / \code{solver} arguments) or \code{"hsgp"} (a Hilbert-space
basis GP with \code{m} functions per dimension and boundary factor \code{c}).}

\item{cov}{Covariance function (NNGP only). One of \code{"exponential"} or
\code{"matern"}.}

\item{nu}{Matern smoothness parameter, one of \code{1.5} or \code{2.5}. Used only when
\code{cov = "matern"} (\code{nu = 0.5} is \code{cov = "exponential"}).}

\item{nn}{Number of nearest neighbours used in the NNGP approximation.}

\item{m}{Number of HSGP basis functions per dimension (\code{approx = "hsgp"}).}

\item{c}{HSGP boundary factor, \verb{>= 1} (\code{approx = "hsgp"}).}

\item{solver}{Linear solver for the GP. One of \code{"auto"}, \code{"cholesky"},
\code{"cg"}, \code{"pcg"}, or \code{"gpu"}. \code{"gpu"} falls back to \code{"pcg"} when CUDA
support is unavailable.}

\item{cg_tol}{Convergence tolerance for the (preconditioned) CG solver.}

\item{cg_maxiter}{Maximum number of (preconditioned) CG iterations.}

\item{shared}{Whether the spatial effect is shared across processes in a
multi-process model. \code{NULL} (default) shares the effect; \code{FALSE} fits
process-specific effects and emits a warning.}

\item{scale_coords}{Logical. Standardize coordinates before fitting
(default \code{TRUE}).}

\item{parameterization}{Latent parameterization. One of \code{"centered"},
\code{"noncentered"}, or \code{"collapsed"} (the last is deprecated).}
}
\value{
A \code{tulpa_gp} object (also of class \code{tulpa_spatial}).
}
\description{
Specify a Gaussian-process spatial random effect, approximated with a
nearest-neighbour GP (NNGP) for scalability. Captures smooth spatial
variation from point-referenced coordinates.
}
\examples{
# GP spatial specification from coordinate columns
spatial_gp(~ lon + lat)
spatial_gp(~ lon + lat, cov = "matern", nu = 1.5)

}
\seealso{
\code{\link[=spatial_car]{spatial_car()}}, \code{\link[=spatial_bym2]{spatial_bym2()}} for areal spatial effects.
}
