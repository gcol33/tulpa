% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/hyper_quadrature.R
\name{tulpa_grid_log_quad}
\alias{tulpa_grid_log_quad}
\title{Per-cell log quadrature weight of an outer grid}
\usage{
tulpa_grid_log_quad(
  theta_grid,
  specs = NULL,
  copy_slab = "exponential",
  close_domain = TRUE,
  folded_axes = NULL,
  refining = NULL
)
}
\arguments{
\item{theta_grid}{A named \verb{[n_cells x n_axes]} matrix (see
\code{\link[=tulpa_theta_matrix]{tulpa_theta_matrix()}}).}

\item{specs}{Optional pre-built per-axis spec list; \code{NULL} rebuilds it
from \code{theta_grid}'s columns via \code{\link[=tulpa_joint_axis_specs_from_grid]{tulpa_joint_axis_specs_from_grid()}}.}

\item{copy_slab}{\code{"exponential"} or \code{"flat"}; the copy-scale axis's
continuum measure (see \code{?tulpa_joint_axis_specs_from_grid}).}

\item{close_domain}{Whether an unbounded axis's outer cells are closed at
the grid's own edge rather than left open to infinity.}

\item{folded_axes}{Optional names of axes folded onto \verb{[0, Inf)} (e.g. a
correlation axis reflected at 0).}

\item{refining}{Optional refinement-slice tag vector (see
\code{\link[=tulpa_hyper_slice_home]{tulpa_hyper_slice_home()}}); when supplied, specs are rebuilt from the
grid's base (non-slice) cells only.}
}
\value{
Numeric vector of per-cell log quadrature weights, length
\code{nrow(theta_grid)}, or \code{NULL} if \code{theta_grid} has no axis names.
}
\description{
The per-cell log quadrature weight (prior mass) of a nested-Laplace outer
grid: every path that turns a fit's \code{log_marginal} into posterior cell
weights goes through this one rule, so the prior mass a cell carries is
decided in one place. Rebuilds axis specs from the grid's own columns when
\code{specs} is not supplied. Intended for reconstructing a fit's outer-grid
posterior weights (with \code{\link[=tulpa_theta_matrix]{tulpa_theta_matrix()}} and
\code{\link[=tulpa_normalise_weights_safe]{tulpa_normalise_weights_safe()}}) when the fit doesn't already carry
\code{fit$log_quad}.
}
\seealso{
\code{\link[=tulpa_theta_matrix]{tulpa_theta_matrix()}}, \code{\link[=tulpa_normalise_weights_safe]{tulpa_normalise_weights_safe()}},
\code{\link[=tulpa_joint_axis_specs_from_grid]{tulpa_joint_axis_specs_from_grid()}}
}
