% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ep.R
\name{tulpa_ep}
\alias{tulpa_ep}
\title{Expectation-Propagation fit for a GLM}
\usage{
tulpa_ep(
  formula,
  data,
  family = "binomial",
  phi = 1,
  phi2 = NULL,
  n_trials = NULL,
  beta_prior_sd = 10,
  control = list()
)
}
\arguments{
\item{formula}{Model formula.}

\item{data}{A data frame.}

\item{family}{Character family name (see \code{\link[=family_names]{family_names()}}).}

\item{phi}{Dispersion / precision passed to the family (held fixed).}

\item{phi2}{Optional second dispersion (Student-t degrees of freedom for
\code{family = "t"}; default 4 when \code{NULL}).}

\item{n_trials}{Binomial denominators (length \code{nrow(data)}), or \code{NULL} (= 1).}

\item{beta_prior_sd}{SD of the mean-zero Gaussian prior on every coefficient
(default 10).}

\item{control}{List: \code{max_sweeps} (default 50), \code{tol} (default 1e-6),
\code{damping} (default 0.8), \code{n_quad} (Gauss-Hermite nodes, default 20),
\code{n_draws} (default 2000), \code{seed}.}
}
\value{
A \code{tulpa_fit} (subclass \code{tulpa_ep}) with \code{coefficients} (posterior
mean), \code{vcov}, \code{draws}, \code{log_marginal} (the EP approximation), \code{converged}.
}
\description{
Fits a generalized linear model with a Gaussian coefficient prior by
Expectation Propagation: the posterior is approximated by a Gaussian whose
per-observation sites match the moments of the tilted distribution (via
Gauss-Hermite quadrature). EP is exact for a Gaussian likelihood and typically
more accurate than Laplace on skewed likelihoods, since it matches marginal
moments rather than the mode curvature.
}
\examples{
\donttest{
set.seed(1)
d <- data.frame(x = rnorm(200))
d$y <- rbinom(200, 1, plogis(-0.3 + 0.8 * d$x))
fit <- tulpa_ep(y ~ x, data = d, family = "binomial")
coef(fit)
}
}
\references{
Minka (2001). Expectation Propagation for approximate Bayesian
inference. UAI. Rasmussen & Williams (2006). Gaussian Processes for Machine
Learning, Algorithm 3.5.
}
\seealso{
\code{\link[=tulpa]{tulpa()}} (Laplace / sampler tiers), \code{\link[=pathfinder]{pathfinder()}} (VI).
}
