% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_core.R
\name{temporal_rw1}
\alias{temporal_rw1}
\title{RW1 temporal structure (First-order Random Walk)}
\usage{
temporal_rw1(time_var, group_var = NULL, cyclic = FALSE, shared = NULL)
}
\arguments{
\item{time_var}{A formula (\code{~ time}) or single character string naming the
time variable in the data.}

\item{group_var}{Optional formula (\code{~ g}) or character string naming a
grouping variable. When supplied, a separate random walk is fit per group;
\code{NULL} (default) fits a single walk shared across all observations.}

\item{cyclic}{Logical. If \code{TRUE}, the random walk wraps around so the last
time point is a neighbour of the first (cyclic boundary, e.g. month of
year). Default \code{FALSE}.}

\item{shared}{Whether the temporal effect is shared across processes in a
multi-process model. \code{NULL} (default) shares the effect; \code{FALSE} fits
process-specific effects and emits a warning about unshared confounding.}
}
\value{
A \code{tulpa_temporal} object.
}
\description{
Specify a first-order random walk temporal random effect. RW1 penalizes
first differences, so adjacent time points are smoothed toward each other:
\code{phi[t] - phi[t-1] ~ N(0, sigma^2)}.
}
\details{
The precision matrix is rank \code{T - 1} (one constraint needed). RW1 is the
least smooth of the random-walk priors; for smoother trends see
\code{\link[=temporal_rw2]{temporal_rw2()}}, and for a stationary alternative see \code{\link[=temporal_ar1]{temporal_ar1()}}.
}
\examples{
# Create temporal RW1 specification
temporal_rw1("year")
temporal_rw1("month", cyclic = TRUE)

}
\seealso{
\code{\link[=temporal_rw2]{temporal_rw2()}}, \code{\link[=temporal_ar1]{temporal_ar1()}} for other temporal priors.
}
