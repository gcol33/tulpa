% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_laplace_joint_helpers.R
\name{tulpa_joint_inner_vcov_blocks}
\alias{tulpa_joint_inner_vcov_blocks}
\title{Per-cell constrained inverse-precision sub-block for a joint fit}
\usage{
tulpa_joint_inner_vcov_blocks(
  Q_p_per_grid,
  Q_i_per_grid,
  Q_x_per_grid,
  n_x,
  idx,
  n_dense,
  A_cols_list,
  field_marginal = TRUE,
  n_threads = 1L
)
}
\arguments{
\item{Q_p_per_grid, Q_i_per_grid, Q_x_per_grid}{Per-outer-grid-cell CSC
storage of the latent precision \code{Q} (\code{fit$Q_csc_p_per_grid} /
\verb{_i_per_grid} / \verb{_x_per_grid}), one list element per cell.}

\item{n_x}{Integer, the latent dimension \code{Q} is built over
(\code{fit$Q_csc_n}).}

\item{idx}{Integer vector (1-based) of latent coordinates to extract the
sub-block for.}

\item{n_dense}{Integer, the number of leading \code{idx} entries that are
dense fixed effects rather than field coordinates (see Details).}

\item{A_cols_list}{A list of integer vectors, one per sum-to-zero
constraint group, each the (1-based) latent coordinates that group
averages to zero.}

\item{field_marginal}{Logical; if \code{TRUE} (default) and
\code{n_dense < length(idx)}, only the field coordinates' marginal variances
are computed (a single Takahashi pass) rather than their full dense
sub-block.}

\item{n_threads}{Integer, grid cells processed concurrently.}
}
\value{
A list of length \code{n_grid} (one per outer-grid cell), each element
either \code{NULL} (the cell stored no \code{Q}, or its sparse Cholesky failed)
or a dense \verb{length(idx) x length(idx)} matrix: the constrained
covariance sub-block for \code{idx}, with the sum-to-zero constraints in
\code{A_cols_list} conditioned out by the same kriging correction the fit's
own posterior draws use.
}
\description{
Runs the same per-outer-grid-cell selected-inversion the joint
nested-Laplace driver's own fixed-effect retention uses internally
(\code{extract_inner_vcov_block_cell()}, see the note above
\code{.joint_attach_grid_fixed()}), for a caller-chosen index set. A consumer
package computing its own standard-error correction on a subset of a
joint fit's latent coordinates -- a different \code{idx} than whatever the fit
itself retained on \verb{$cov_block_per_grid} -- reruns the extraction through
this door instead of reimplementing the sparse selected inversion.
}
\details{
When a field is present (\code{n_dense < length(idx)}), the
extraction takes a cheap selected-inversion path: the dense
(fixed-effect) block and the fixed-effect x field cross terms are
exact; the field x field block is either its marginal diagonal
(\code{field_marginal = TRUE}) or left at zero (\code{FALSE}) -- never the full
field x field covariance.
}
\examples{
\dontrun{
tulpa_joint_inner_vcov_blocks(
  fit$Q_csc_p_per_grid, fit$Q_csc_i_per_grid, fit$Q_csc_x_per_grid,
  n_x = fit$Q_csc_n, idx = 1:2, n_dense = 2L, A_cols_list = list()
)
}
}
