% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal_rtr_posteriors.R
\name{temporal}
\alias{temporal}
\alias{temporal.tulpa_fit}
\title{Extract temporal effects from a fitted model}
\usage{
temporal(
  object,
  component = "all",
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  ...
)

\method{temporal}{tulpa_fit}(
  object,
  component = "all",
  summary = FALSE,
  probs = c(0.025, 0.5, 0.975),
  ...
)
}
\arguments{
\item{object}{A \code{tulpa_fit} object fitted with \code{temporal} argument}

\item{component}{Which component to extract for multi-scale models:
\code{"all"} (default), \code{"trend"}, \code{"seasonal"}, or \code{"short_term"}.}

\item{summary}{Logical; if TRUE, return summary statistics instead of
full posterior draws.}

\item{probs}{Quantiles to compute if \code{summary = TRUE}.}

\item{...}{Ignored}
}
\value{
A \code{tulpa_temporal_posterior} object
}
\description{
Extract posterior distributions of temporal effects from a fitted tulpa
model with temporal specification.
}
\details{
\code{temporal()} is overloaded. Given a fitted model it is the accessor described
here. Given a one-sided formula (or a named \verb{formula =} / \verb{structure =}
argument) it is instead the inline varying-coefficient field constructor used
in a \code{tulpa()} model formula, the temporal mirror of \code{\link[=spatial]{spatial()}}:
\code{temporal(formula = ~ 1 + x || time, structure = "rw1")} declares a smooth
temporal level (the intercept column) plus a temporally varying slope on each
covariate column. \code{structure} is one of \code{"rw1"} (default), \code{"rw2"}, or
\code{"ar1"}; only the double bar \code{||} (independent fields) is supported.
}
\examples{
\dontrun{
# Fit model with multi-scale temporal effects (not run - experimental)
set.seed(131)
df <- data.frame(
  year = 1:40,
  x = rnorm(40),
  count = rpois(40, lambda = 22),
  effort = rgamma(40, shape = 4.5, rate = 1)
)

fit <- tulpa(
  count | effort ~ x,
  data = df,
  family = tulpa_poisson_gamma(),
  temporal = temporal_multiscale("year", trend = "rw2", seasonal = 12),
  iter = 200, warmup = 100, chains = 1
)

# Extract all temporal effects
temp_post <- temporal(fit)
summary(temp_post)
}

}
\seealso{
\code{\link[=temporal_multiscale]{temporal_multiscale()}}, \code{\link[=temporal_rw1]{temporal_rw1()}}
}
