% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{tulpa_family}
\alias{tulpa_family}
\title{Construct a minimal tulpa_family for simulation}
\usage{
tulpa_family(
  name,
  simulate_fn,
  process_names = "y",
  extra_params = list(),
  link_inv = NULL
)
}
\arguments{
\item{name}{Family name (character, length 1).}

\item{simulate_fn}{\verb{function(eta, params, n_obs, ...)} returning a numeric
vector of length \code{n_obs} (single-process) or a list of such vectors keyed
by \code{process_names} (multi-process). \code{eta} is a list of linear predictors,
one per process.}

\item{process_names}{Character vector. Defaults to \code{"y"} (single-process).}

\item{extra_params}{Named list of \code{tulpa_prior} objects for likelihood-
specific scalar parameters (e.g., dispersion \code{phi}). Drawn at each
prior-predictive iteration. Defaults to empty.}

\item{link_inv}{List of inverse-link functions per process; defaults to
identity for every process. tulpa passes the raw linear predictor to
\code{simulate_fn}, so most families implement the link inside \code{simulate_fn}
(e.g., \code{mu = exp(eta)} for Poisson). The \code{link_inv} slot exists for
families that prefer to keep the inverse-link separate.}
}
\value{
A \code{tulpa_family} object.
}
\description{
Lightweight constructor for a \code{tulpa_family} object that exposes the
contract required by \code{\link[=prior_predict]{prior_predict()}} and \code{\link[=tulpa_simulate]{tulpa_simulate()}}. Model
packages (tulpaRatio, tulpaObs) register richer families that also link to
C++ likelihoods; this helper is for tests and simple custom families that
only need simulation.
}
\examples{
fam <- tulpa_family(
  name = "poisson",
  simulate_fn = function(eta, params, n_obs, ...) {
    rpois(n_obs, exp(eta[[1]]))
  }
)

}
