% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_rsr_spde.R
\name{spatial_spde_custom}
\alias{spatial_spde_custom}
\title{SPDE Spatial Field from Custom Matrices}
\usage{
spatial_spde_custom(
  C,
  G,
  A,
  nu = 1,
  prior_range = NULL,
  prior_sigma = NULL,
  coords = NULL
)
}
\arguments{
\item{C}{Mass matrix (n_mesh x n_mesh sparse matrix, e.g. from \code{fmesher::fm_fem()$c0}).}

\item{G}{Stiffness matrix (n_mesh x n_mesh sparse matrix, e.g. from \code{fmesher::fm_fem()$g1}).}

\item{A}{Projection matrix (n_obs x n_mesh sparse matrix, e.g. from \code{fmesher::fm_basis()}).}

\item{nu}{Matern smoothness parameter. A positive number; integer values
give the exact FEM construction, fractional values the BRASIL rational SPDE
approximation (supported by \code{fit_spde()};). Default 1.}

\item{prior_range}{PC prior on the spatial range, \code{c(U, alpha)} with
P(range < U) = alpha. \code{NULL} (the default) anchors it on \code{coords} as
\code{\link[=spatial_spde]{spatial_spde()}} does, and needs them.}

\item{prior_sigma}{PC prior on the marginal standard deviation, \code{c(U, alpha)}
with P(sigma > U) = alpha. \code{NULL} (the default) is \code{c(3, 0.01)}.}

\item{coords}{Observation coordinates (an \verb{n_obs x 2} matrix), read only to
anchor the default range prior. Not needed when \code{prior_range} is given.}
}
\value{
A \code{tulpa_spatial} object with type \code{"spde"}.
}
\description{
Specify a continuous Matern spatial field using externally-provided FEM
matrices. Use this with meshes from fmesher, rSPDE, or any other source.
}
