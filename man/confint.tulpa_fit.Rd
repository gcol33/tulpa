% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods_generic.R
\name{confint.tulpa_fit}
\alias{confint.tulpa_fit}
\title{Credible intervals for the fixed effects}
\usage{
\method{confint}{tulpa_fit}(object, parm = NULL, level = 0.95, ...)
}
\arguments{
\item{object}{A \code{tulpa_fit} object.}

\item{parm}{Parameter names or indices (default: all fixed effects).
Indices address the fixed effects. A name that is not a fixed effect is
read off the fit's posterior draws when they carry a column of that name
(for example a sampler's \code{log_sigma_re}), as their equal-tailed quantile
interval; any other name is an error.}

\item{level}{Interval level, a single number in \verb{(0, 1)} (default 0.95).}

\item{...}{Ignored.}
}
\value{
Matrix with lower and upper columns, labelled as
\code{\link[stats:confint.default]{stats::confint.default()}} labels them (\code{"2.5 \%"} and \code{"97.5 \%"} at the
default level). A nested-Laplace fit carries
\code{interval_source} / \code{interval_declined} (which read produced the bounds --
by default the grid's Gaussian-mixture CDF), \code{retained_mass} (the share of
the grid weight the bounds are conditional on), and \code{skew_applied}, one
logical per reported coefficient saying whether its bounds are the
inner-Laplace skew-corrected quantiles or not. See \code{\link[=summary.tulpa_fit]{summary.tulpa_fit()}}.
}
\description{
Credible intervals for the fixed effects
}
