% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/varcorr.R
\name{VarCorr}
\alias{VarCorr}
\alias{VarCorr.tulpa_fit}
\title{Random-effect variances and correlations}
\usage{
VarCorr(x, sigma = 1, ...)

\method{VarCorr}{tulpa_fit}(x, sigma = 1, ...)
}
\arguments{
\item{x}{A \code{tulpa_fit}.}

\item{sigma}{Ignored, for compatibility with the \code{VarCorr} generic.}

\item{...}{Ignored.}
}
\value{
A data frame with one row per random-effect coefficient: \code{term},
\code{coef}, \code{sd}, and \code{source} (one of \code{"estimated"}, \code{"sampled"},
\code{"conditioned"}). Correlated terms additionally carry the covariance
matrices in the \code{"cov"} attribute, one per term, each with a \code{"correlation"}
attribute. Returns an empty data frame when the fit has no random effects.
}
\description{
Summarize the random-effect covariance of a fit: the standard deviation of
each random-effect coefficient, the correlations between them within a term,
and the covariance matrix itself.

Whether the covariance was estimated, sampled, or merely conditioned on is
reported alongside it, because the three are different claims. A fit from
\code{mode = "laplace"} conditions on \code{sigma_re}, so its values are the ones
supplied; \code{mode = "eb"} estimates them; a sampler tier integrates them.
}
\examples{
\donttest{
set.seed(1)
G <- 30L; per <- 10L; n <- G * per
grp <- rep(seq_len(G), each = per); x <- rnorm(n)
b <- rnorm(G, 0, 0.8)
d <- data.frame(y = rpois(n, exp(0.3 + 0.5 * x + b[grp])), x = x,
                g = factor(grp))
fit <- tulpa(y ~ x + (1 | g), data = d, family = "poisson", mode = "eb")
VarCorr(fit)
}
}
\seealso{
\code{\link[=ranef]{ranef()}} for the per-level deviations, \code{\link[=tulpa_eb]{tulpa_eb()}} to estimate the
covariance rather than condition on it.
}
