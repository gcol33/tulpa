% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_draws.R
\name{tulpa_posterior_draws}
\alias{tulpa_posterior_draws}
\title{Posterior draws from a nested-Laplace fit}
\usage{
tulpa_posterior_draws(fit, idx = NULL, n = 1000, ...)
}
\arguments{
\item{fit}{A nested-Laplace fit (\code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}} or
\code{\link[=tulpa_nested_laplace_joint]{tulpa_nested_laplace_joint()}}).}

\item{idx}{Optional integer vector of 1-based indices into whatever a draw
covers for this backend (see above); \code{NULL} (default) returns all of them.}

\item{n}{Number of posterior draws (default 1000).}

\item{...}{Unused; for S3 compatibility.}
}
\value{
A numeric matrix \verb{[n x length(idx)]}, one row per draw. Carries
\code{attr(., "draws_kind") = "iid"} (consistent with the draws-provenance
gate), \code{attr(., "cells")} -- the outer-grid cell index each row was drawn
from -- and \code{attr(., "scope")}, which of the two representations above the
columns are.
}
\description{
Draw from the outer-grid mixture posterior of a nested-Laplace fit -- the
engine analogue of \code{inla.posterior.sample()}. Each draw picks an outer-grid
cell \code{k ~ Categorical(weights)} and then samples that cell's inner Gaussian,
so the draws are i.i.d. samples from \verb{sum_k w_k N(m_k, V_k)}.

Sampling the mixture is the faithful primitive for marginalizing nonlinear
derived quantities (e.g. \code{plogis(eta_2) - plogis(eta_1)}, expected-cover
products \code{p * mu}): compute the derived quantity per draw, then summarize.
Collapsing the grid to a single moment-matched Gaussian biases skewed or
multimodal-over-grid posteriors.
}
\section{What a draw covers}{
It depends on which representation the backend retained, and the returned
matrix says so in its \code{scope} attribute.

\describe{
\item{\code{tulpa_nested_laplace_joint}}{the FULL latent vector -- per-arm fixed
effects, per-arm random effects, then the latent field(s) -- because the
joint fit retains each cell's sparse precision over that vector
(\code{control$store_Q = TRUE}). \code{scope} is \code{"latent"}.}
\item{\code{tulpa_nested_laplace} (single-block)}{the FIXED-EFFECT block. This
backend inverts each cell's precision into the marginal fixed-effect
block and releases the precision itself, so the latent field is not part
of the retained per-cell Gaussian and cannot be sampled from the fit.
\code{scope} is \code{"fixed"}.}
}
}

\seealso{
\code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}}, \code{\link[=tulpa_nested_laplace_joint]{tulpa_nested_laplace_joint()}},
\code{\link[=posterior_sample]{posterior_sample()}}
}
