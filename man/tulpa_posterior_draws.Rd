% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/posterior_draws_joint.R
\name{tulpa_posterior_draws}
\alias{tulpa_posterior_draws}
\title{Posterior draws from a joint nested-Laplace fit}
\usage{
tulpa_posterior_draws(fit, idx = NULL, n = 1000, ...)
}
\arguments{
\item{fit}{A \code{tulpa_nested_laplace_joint} fit (single-block or multi-block).
The fit must have been produced with \code{control$store_Q = TRUE} so the
per-grid sparse precision \verb{Q_csc_*_per_grid} is available.}

\item{idx}{Optional integer vector of 1-based latent indices to return.
\code{NULL} (default) returns the full latent vector. The latent vector stacks
per-arm fixed effects, per-arm random effects, then the latent field(s);
use \code{fit$arm_layout} (\code{beta_start}, \code{re_start}, \code{phi_start} / \code{theta_start}
/ \code{field_starts}, all 0-based) to map a sub-block to indices.}

\item{n}{Number of posterior draws (default 1000).}

\item{...}{Unused; for S3 compatibility.}
}
\value{
A numeric matrix \verb{[n x length(idx)]} of latent draws, one row per
draw, columns named \verb{x<idx>}. Carries \code{attr(., "draws_kind") = "iid"}
(consistent with the draws-provenance gate) and
\code{attr(., "cells")}, the outer-grid cell index each row was drawn from.
}
\description{
Draw from the outer-grid mixture posterior of a joint nested-Laplace fit
(\code{\link[=tulpa_nested_laplace_joint]{tulpa_nested_laplace_joint()}}) -- the engine analogue of
\code{inla.posterior.sample()}. Each draw picks an outer-grid cell
\code{k ~ Categorical(weights)} and then samples the inner latent vector from the
constrained Gaussian \code{N(m_k, V_k)} at that cell, where \code{m_k} is the cell's
inner mode and \code{V_k} is the inner-Laplace covariance with the ICAR / BYM2
sum-to-zero field constraint imposed (conditioning by kriging). The draws
are i.i.d. samples from \verb{sum_k weights_k * N(m_k, V_k)}.

Sampling the mixture is the faithful primitive for marginalizing nonlinear
derived quantities (e.g. \code{plogis(eta_2) - plogis(eta_1)}, expected-cover
products \code{p * mu}): compute the derived quantity per draw, then summarize.
Collapsing the grid to a single moment-matched Gaussian biases skewed or
multimodal-over-grid posteriors.
}
\details{
The constrained draw at cell \code{k} uses the sparse Cholesky of the stored
precision \code{Q_k} and the conditioning-by-kriging correction
\deqn{z_c = z - Q_k^{-1} A^\top (A Q_k^{-1} A^\top)^{-1} A z,}
where \code{z ~ N(0, Q_k^{-1})} and \code{A} stacks the field sum-to-zero rows (one for
an ICAR / CAR field; two -- structured and unstructured -- for a BYM2 field;
one per spatial block in a multi-block fit). The returned draw is
\code{m_k + z_c}, restricted to \code{idx}. Because \code{m_k} already satisfies the
constraint (the inner solve centres the field), the mean is left unchanged
and only the covariance is constrained, so the per-cell marginal matches the
inner-Laplace constrained covariance exactly.

Cells with zero outer-grid weight (e.g. pruned cells) or no stored \code{Q} are
dropped and the remaining weights renormalized. A degenerate single-cell
grid (quadrature ESS 1) returns draws from that cell's \code{N(m_1, V_1)}.
}
\seealso{
\code{\link[=tulpa_nested_laplace_joint]{tulpa_nested_laplace_joint()}}, \code{\link[=posterior_sample]{posterior_sample()}}
}
