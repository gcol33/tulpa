% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nested_laplace.R
\name{tulpa_normalise_weights_safe}
\alias{tulpa_normalise_weights_safe}
\title{Normalise outer-grid log-marginals to posterior cell weights}
\usage{
tulpa_normalise_weights_safe(lm, what = "grids / data", log_quad = NULL)
}
\arguments{
\item{lm}{Numeric vector of per-cell log-marginal-likelihood values.}

\item{what}{Label for the grid, used only in the degenerate-case warning.}

\item{log_quad}{Optional per-cell log quadrature weight (prior mass) of
the outer grid, e.g. from \code{\link[=tulpa_grid_log_quad]{tulpa_grid_log_quad()}}; \code{NULL} normalises the
likelihood alone.}
}
\value{
Numeric vector of posterior cell weights summing to 1, the same
length as \code{lm}, or all-\code{NA} if no cell carries finite mass.
}
\description{
The single weight normaliser for every outer hyperparameter grid: softmax
of \code{lm} (optionally plus a per-cell log quadrature weight) into weights
summing to 1. Non-finite nodes (an inner Newton diverging in a grid
corner) are dropped from the max-shift and zeroed before renormalising
over the finite cells; an all-non-finite grid returns all-\code{NA} with a
warning rather than \code{NaN}. Intended for reconstructing a fit's outer-grid
posterior weights (with \code{\link[=tulpa_theta_matrix]{tulpa_theta_matrix()}} and
\code{\link[=tulpa_grid_log_quad]{tulpa_grid_log_quad()}}) when the fit doesn't already carry \code{fit$weights}.
}
\seealso{
\code{\link[=tulpa_theta_matrix]{tulpa_theta_matrix()}}, \code{\link[=tulpa_grid_log_quad]{tulpa_grid_log_quad()}}
}
