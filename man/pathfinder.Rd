% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pathfinder.R
\name{pathfinder}
\alias{pathfinder}
\title{Pathfinder: variational warm-start via L-BFGS + ELBO scoring}
\usage{
pathfinder(
  log_posterior,
  init,
  grad_log_posterior = NULL,
  n_draws = 1000L,
  max_iter = 100L,
  tol = 1e-06,
  verbose = FALSE
)
}
\arguments{
\item{log_posterior}{Function \verb{function(theta) -> numeric} returning
the \emph{unnormalized} log posterior at \code{theta}.}

\item{init}{Numeric vector: initial point for L-BFGS. Should be in
the support of the posterior (finite log_posterior).}

\item{grad_log_posterior}{Optional function returning the gradient
of \code{log_posterior} at \code{theta}. If \code{NULL}, gradients are computed
numerically via \link[stats:optim]{stats::optim}'s built-in finite differences.}

\item{n_draws}{Number of draws from the fitted Gaussian (default 1000).}

\item{max_iter}{L-BFGS iteration cap (default 100).}

\item{tol}{Gradient-norm tolerance for L-BFGS convergence
(default 1e-6).}

\item{verbose}{Print L-BFGS / ELBO summary at end (default FALSE).}
}
\value{
A list with class \code{tulpa_fit} carrying:
\itemize{
\item \code{draws}: \verb{n_draws × d} matrix of Gaussian draws at the mode.
\item \code{means}: posterior means (= mode for a Gaussian fit).
\item \code{mode}: the L-BFGS mode.
\item \code{cov}: the proposal covariance (\code{solve(-hessian)}).
\item \code{elbo}: Monte-Carlo estimate of \verb{E_q[log p - log q]}.
\item \code{n_iter}: L-BFGS iterations.
\item \code{converged}: logical.
\item \code{inference_mode}: \code{"structured"}.
\item \code{inference_tier}: \code{2L}.
\item \code{backend}: \code{"pathfinder"}.
}
}
\description{
Single-path Pathfinder (Zhang, Carpenter, Gelman, Vehtari 2022):
run L-BFGS toward the posterior mode, fit a Gaussian at the optimum
using the inverse-Hessian estimate, and report draws plus the ELBO.
Cheap, derivative-only, embarrassingly parallel — meant as an HMC
warm-start, an initialiser for \code{\link[=imh_laplace]{imh_laplace()}}, or a quick
sanity check on the Laplace approximation.

This implementation is \strong{single-path only}. The full multi-path
Pathfinder (K parallel L-BFGS runs + mixture proposal + Pareto-
smoothed importance reweighting) is a follow-on. The single-path
version is what most users want as a Laplace-equivalent diagnostic.
}
\section{Tier}{

Tier 2 (Structured). The output is a Gaussian approximation, not
samples from the exact posterior — same epistemic class as
\code{\link[=tulpa_laplace]{tulpa_laplace()}}. Pair with \code{\link[=imh_laplace]{imh_laplace()}} for an exact-tier
upgrade.
}

\examples{
\dontrun{
  # Toy: 2-D conjugate normal.
  y <- c(0.5, -0.7)
  log_post <- function(t) {
    sum(dnorm(y, t, 1, log = TRUE)) +
      sum(dnorm(t, 0, sqrt(10), log = TRUE))
  }
  pf <- pathfinder(log_post, init = c(0, 0), n_draws = 2000)
  pf$mode      # near c(0.45, -0.64)
  pf$elbo
}

}
\references{
Zhang, L., Carpenter, B., Gelman, A., & Vehtari, A. (2022).
Pathfinder: parallel quasi-Newton variational inference.
\emph{Journal of Machine Learning Research}, 23(306), 1–49.
}
\seealso{
\code{\link[=imh_laplace]{imh_laplace()}} for an exact-tier MH using the Pathfinder
Gaussian as proposal; \code{\link[=bridge_sampling]{bridge_sampling()}} for marginal-likelihood
estimation on the resulting draws.
}
