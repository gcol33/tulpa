% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/temporal.R
\name{temporal_tvc}
\alias{temporal_tvc}
\title{Temporally-Varying Coefficients (TVC)}
\usage{
temporal_tvc(
  time_var,
  terms = 1,
  structure = c("rw1", "rw2", "ar1", "gp"),
  group_var = NULL,
  shared = NULL
)
}
\arguments{
\item{time_var}{Name of the time variable in data. Can be a formula
(e.g., \code{~ year}) or a character string (e.g., \code{"year"}).}

\item{terms}{Which coefficients should vary over time. Options:
\itemize{
\item Integer vector: Column indices of design matrix (1 = intercept)
\item Character vector: Coefficient names (e.g., \code{"(Intercept)"}, \code{"depth"})
\item Formula: \code{~ 1 + depth} for intercept and depth
}}

\item{structure}{Temporal structure for the varying coefficients:
\itemize{
\item \code{"rw1"}: First-order random walk (default)
\item \code{"rw2"}: Second-order random walk (smoother)
\item \code{"ar1"}: First-order autoregressive
\item \code{"gp"}: Gaussian process (for irregular time spacing)
}}

\item{group_var}{Optional name of grouping variable for panel data.}

\item{shared}{Logical; if TRUE (default), TVC effects enter both
all processes. Set to FALSE for process-specific TVCs
(triggers warning about potential confounding).}
}
\value{
A \code{tulpa_tvc} object
}
\description{
Specify temporally-varying coefficients for tulpa models. TVCs allow
regression coefficients to change smoothly over time using a Gaussian
process or random walk prior. This captures how effects evolve temporally.
}
\details{
The TVC model extends the linear predictor:

\deqn{\eta(t) = X\beta + \tilde{X}(t)w(t)}

where:
\itemize{
\item \eqn{\beta} are global (non-time-varying) coefficients
\item \eqn{\tilde{X}(t)} is the subset of covariates with TVCs
\item \eqn{w(t)} are time-varying adjustments
}

Each TVC follows a temporal process:
\deqn{w_j(t) \sim RW1(\sigma_j^2) \text{ or other temporal structure}}

\strong{Interpretation}: A positive TVC for depth at time t means the depth
effect is stronger at t than the global average. The temporal variance
\eqn{\sigma^2_j} quantifies how much the effect varies over time.

\strong{Identifiability}: TVCs are centered (sum-to-zero constraint) to separate
the time-varying component from the global effect.
}
\examples{
# Create TVC specification
tvc <- temporal_tvc("year", terms = 1)
print(tvc)

\dontrun{
# Generate synthetic data with time-varying effect (not run - TVC experimental)
set.seed(150)
df <- data.frame(
  year = rep(2010:2025, each = 5),
  x = rnorm(80),
  count = rpois(80, lambda = 20),
  effort = rgamma(80, shape = 4, rate = 1)
)

# Time-varying intercept (random temporal field)
fit <- tulpa(
  count | effort ~ x,
  data = df,
  family = tulpa_poisson_gamma(),
  tvc = temporal_tvc("year", terms = 1),
  backend = "hmc",
  iter = 200,
  warmup = 100,
  chains = 1
)
summary(fit)

# Time-varying effect of x
fit2 <- tulpa(
  count | effort ~ x,
  data = df,
  family = tulpa_poisson_gamma(),
  tvc = temporal_tvc("year", terms = c("(Intercept)", "x"), structure = "rw2"),
  backend = "hmc",
  iter = 200,
  warmup = 100,
  chains = 1
)

# Extract and plot the time-varying coefficients
tvc_effects <- tvc(fit2)
plot(tvc_effects, "x")
}

}
\seealso{
\code{\link[=spatial_svc]{spatial_svc()}} for spatially-varying coefficients,
\code{\link[=temporal_rw1]{temporal_rw1()}} for temporal random effects,
\code{\link[=tvc]{tvc()}} for extracting TVC posteriors
}
