% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial.R
\name{spatial_svc}
\alias{spatial_svc}
\title{Spatially-Varying Coefficients (SVC)}
\usage{
spatial_svc(
  coords,
  terms = 1,
  cov = c("exponential", "matern", "gaussian", "spherical"),
  nn = 15,
  shared = NULL,
  scale_coords = TRUE,
  approx = c("nngp", "hsgp"),
  m = 6,
  c_boundary = 1.5
)
}
\arguments{
\item{coords}{A one-sided formula specifying coordinate columns (e.g.,
\code{~ lon + lat}), or a character vector of length 2 with column names.}

\item{terms}{Which coefficients should vary spatially. Options:
\itemize{
\item Integer vector: Column indices of design matrix (1 = intercept)
\item Character vector: Coefficient names (e.g., \code{"(Intercept)"}, \code{"depth"})
\item Formula: \code{~ 1 + depth} for intercept and depth
}}

\item{cov}{Covariance function: \code{"exponential"} (default), \code{"matern"},
\code{"gaussian"}, or \code{"spherical"}.}

\item{nn}{Number of nearest neighbors for NNGP approximation. Default 15.
Larger values give better approximation but slower computation.}

\item{shared}{Logical; if TRUE (default), SVC effects enter both
all processes. Set to FALSE for process-specific SVCs
(triggers warning about potential confounding).}

\item{scale_coords}{Logical; if TRUE (default), coordinates are scaled to
unit variance before computing distances.}

\item{approx}{Approximation method: \code{"nngp"} (default) for Nearest Neighbor
GP; \code{"hsgp"} for Hilbert Space GP (30-40x faster).}

\item{m}{Number of HSGP basis functions per dimension (default 6). Only
used when \code{approx = "hsgp"}. Total basis functions will be m^2.}

\item{c_boundary}{Boundary factor for HSGP domain extension (default 1.5).
Only used when \code{approx = "hsgp"}.}
}
\value{
A \code{tulpa_svc} object
}
\description{
Specify spatially-varying coefficients for tulpa models. SVCs allow
regression coefficients to vary smoothly across space using a Gaussian
process prior. This captures local effects that may differ from global
relationships.

Uses Nearest Neighbor Gaussian Process (NNGP) approximation for
computational efficiency with large datasets.
}
\details{
The SVC model extends the linear predictor:

\deqn{\eta(s) = X\beta + \tilde{X}(s)w(s)}

where:
\itemize{
\item \eqn{\beta} are global (non-spatial) coefficients
\item \eqn{\tilde{X}(s)} is the subset of covariates with SVCs
\item \eqn{w(s)} are spatially-varying adjustments at location s
}

Each SVC follows an independent Gaussian process:
\deqn{w_j(s) \sim GP(0, \sigma^2_j \cdot C(\phi_j))}

where \eqn{C(\phi)} is the correlation function with range parameter
\eqn{\phi}.

\strong{NNGP approximation}: For computational tractability, we use the
Nearest Neighbor Gaussian Process (Datta et al., 2016), which conditions
each location on its k nearest neighbors. This reduces complexity from
O(n^3) to O(n*k^2), enabling models with thousands of locations.

\strong{Interpretation}: A positive SVC for depth at location s means the
depth effect is stronger at s than the global average. The spatial
variance \eqn{\sigma^2_j} quantifies how much the effect varies across
space.
}
\examples{
# Create SVC specification
svc_spec <- spatial_svc(~ lon + lat, terms = 1)
print(svc_spec)

\dontrun{
# Generate synthetic spatial data (not run - SVC not fully supported)
set.seed(202)
n <- 40
df <- data.frame(
  lon = runif(n, 0, 10),
  lat = runif(n, 0, 10),
  depth = rnorm(n),
  temp = rnorm(n),
  count = rpois(n, 20),
  effort = rgamma(n, shape = 4, rate = 1)
)

# Spatially-varying intercept (random spatial field)
fit <- tulpa(
  count | effort ~ depth,
  data = df,
  family = tulpa_poisson_gamma(),
  svc = spatial_svc(~ lon + lat, terms = 1),
  iter = 200, warmup = 100, chains = 1
)
summary(fit)

# Extract and plot the spatially-varying coefficients
svc_effects <- svc(fit)
plot(svc_effects, "depth")
}

}
\references{
Datta, A., Banerjee, S., Finley, A. O., & Gelfand, A. E. (
2016). Hierarchical nearest-neighbor Gaussian process models for large
geostatistical datasets. Journal of the American Statistical Association,
111(514), 800-812.
}
\seealso{
\code{\link[=spatial_car]{spatial_car()}}, \code{\link[=spatial_bym2]{spatial_bym2()}} for areal spatial effects,
\code{\link[=svc]{svc()}} for extracting SVC posteriors
}
