% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spatial_svc.R
\name{spatial_svc}
\alias{spatial_svc}
\title{Spatially varying coefficient structure}
\usage{
spatial_svc(
  coords,
  terms = 1,
  cov = c("exponential", "matern", "gaussian", "spherical"),
  nn = 15,
  shared = NULL,
  scale_coords = TRUE,
  approx = c("nngp", "hsgp"),
  m = 6,
  c_boundary = 1.5
)
}
\arguments{
\item{coords}{A formula (\code{~ lon + lat}) or character vector of length 2
naming the two coordinate variables in the data.}

\item{terms}{Which coefficients vary over space. A formula, an integer vector
of design-matrix column indices, or a character vector of term names.
Default \code{1} (the intercept).}

\item{cov}{Covariance function. One of \code{"exponential"}, \code{"matern"},
\code{"gaussian"}, or \code{"spherical"}.}

\item{nn}{Number of nearest neighbours used in the NNGP approximation
(\code{approx = "nngp"}).}

\item{shared}{Whether the effect is shared across processes in a
multi-process model. \code{NULL} (default) shares it; \code{FALSE} fits
process-specific effects and emits a warning.}

\item{scale_coords}{Logical. Standardize coordinates before fitting
(default \code{TRUE}).}

\item{approx}{Spatial approximation. \code{"nngp"} (nearest-neighbour GP) or
\code{"hsgp"} (Hilbert-space GP).}

\item{m}{Number of basis functions per dimension for the HSGP approximation
(\code{approx = "hsgp"}).}

\item{c_boundary}{Boundary-extension factor for the HSGP domain
(\code{approx = "hsgp"}).}
}
\value{
A \code{tulpa_svc} object (also of class \code{tulpa_spatial}).
}
\description{
Specify a spatially varying coefficient (SVC): one or more fixed-effect
coefficients are allowed to vary smoothly over space, with the variation
governed by a Gaussian process (NNGP or HSGP approximation).
}
\examples{
# Intercept that varies smoothly over space
spatial_svc(~ lon + lat)

}
\seealso{
\code{\link[=spatial_gp]{spatial_gp()}} for a spatial random effect (rather than a varying
coefficient).
}
