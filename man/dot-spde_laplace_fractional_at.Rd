% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rational_spde.R
\name{.spde_laplace_fractional_at}
\alias{.spde_laplace_fractional_at}
\title{Fractional rSPDE single-point Laplace fit at a fixed (range, sigma)}
\usage{
.spde_laplace_fractional_at(
  y,
  n_trials,
  X,
  spatial,
  family,
  phi,
  range,
  sigma,
  re_idx,
  n_re_groups,
  sigma_re,
  max_iter,
  tol,
  n_threads,
  offset,
  order = 4L
)
}
\description{
The fractional counterpart of the integer \code{cpp_laplace_fit_spde} branch in
\code{\link[=laplace_spde_at]{laplace_spde_at()}}. Assembles \verb{(Q, A_eff)} via \code{\link[=.spde_assemble_at]{.spde_assemble_at()}} and runs
the precomputed C++ solve, whose latent is the auxiliary weights \code{x}; the
returned \code{mode} mesh block is mapped back to the field \verb{u = Pr x} so every
downstream consumer reads field-space mesh effects exactly as on the integer
path. The log-marginal is the well-conditioned \code{B} / determinant-lemma marginal
(\code{cpp_spde_fractional_logmarginal()}) for the RE-free case (comparable across
the \verb{(range, sigma)} grid); a fit with an iid RE block keeps the precomputed
precision-space marginal.
}
\keyword{internal}
