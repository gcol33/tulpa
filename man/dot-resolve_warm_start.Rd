% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/warm_start.R
\name{.resolve_warm_start}
\alias{.resolve_warm_start}
\title{Resolve a \code{warm_start} request into sampler init / inverse-mass vectors}
\usage{
.resolve_warm_start(warm_start, args, re_terms, sigma_re, beta_prior, n_chains)
}
\arguments{
\item{warm_start}{\code{NULL}, \code{"eb"}, \code{"laplace"}, or a fitted \code{tulpa_fit}.}

\item{args}{The assembled sampler arguments (the same values the kernel will
receive), used both to run the source fit and to probe the target layout.}

\item{re_terms}{Random-effect terms in \code{tulpa_eb()} / \code{tulpa_laplace()} form.}

\item{sigma_re}{Random-effect SDs to condition on, for the Laplace source.}

\item{beta_prior}{Optional fixed-effect prior, threaded into the source fit.}

\item{n_chains}{Number of chains the sampler will run.}
}
\description{
Accepts the front door's \code{warm_start}: \code{"eb"} or \code{"laplace"} to run that fit
first, or an already-fitted object from either. Returns \code{NULL} when no warm
start was asked for, so the caller passes nothing and the kernel keeps its
own defaults.
}
\keyword{internal}
