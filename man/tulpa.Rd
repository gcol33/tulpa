% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tulpa.R
\name{tulpa}
\alias{tulpa}
\title{Fit a tulpa model}
\usage{
tulpa(
  formula,
  data,
  family = "gaussian",
  mode = "auto",
  sigma_re = NULL,
  n_trials = NULL,
  phi = 1,
  beta_prior = NULL,
  spatial = NULL,
  temporal = NULL,
  control = list(),
  ...
)
}
\arguments{
\item{formula}{A model formula. Fixed effects, \code{(1 | g)} / \code{(1 + x | g)}
random effects, and \code{offset(...)} terms are recognised.}

\item{data}{A data frame.}

\item{family}{Character family name: one of \code{\link[=family_names]{family_names()}}
(\code{"binomial"}, \code{"poisson"}, \code{"neg_binomial_2"}, \code{"gaussian"}, \code{"beta"}).}

\item{mode}{Inference mode or backend. \code{"auto"} (default) picks the most
reliable Tier 1/Tier 2 method expected to finish; a tier (\code{"exact"},
\code{"structured"}) or a backend name (\code{"laplace"}, \code{"mala"}, ...) forces it.}

\item{sigma_re}{Random-effect SDs to condition on: length 1 (recycled) or one
per RE term. Defaults to 1 per term with a message.}

\item{n_trials}{Binomial denominators (length \code{nrow(data)}), or \code{NULL}.}

\item{phi}{Dispersion/precision passed to the family (residual variance for
gaussian, size for neg_binomial_2, precision for beta).}

\item{beta_prior}{Optional \code{list(mean, sd)} Gaussian prior on the fixed
effects.}

\item{spatial}{Optional spatial-field spec. How it is addressed depends on the
field family:
\itemize{
\item \strong{Areal} (\code{"icar"}, \code{"car"}, \code{"bym2"}, \code{"car_proper"}): a list with \code{type}
and \code{adjacency}, paired with a \code{spatial(col)} term in \code{formula} naming the
per-observation unit column. Term and spec must be supplied together.
\item \strong{Continuous} (\code{spatial_gp(~ lon + lat)} for an NNGP field,
\code{spatial_hsgp(~ lon + lat)} for a Hilbert-space GP, \code{spatial_spde(~ lon + lat, data)} for a Matern SPDE field): the spec object carries the
coordinate columns (the SPDE spec also carries the mesh + FEM matrices),
so \strong{no} \code{spatial(col)} term is used -- observations are mapped to
locations from their coordinates.
}

The mode selects how the spatial hyperparameter is handled:
\itemize{
\item \code{mode = "nested_laplace"}, \code{"structured"}, and \code{"auto"} (when not the
binomial Gibbs case below) \strong{integrate} the hyperparameter -- the
designed Tier 2 path, mirroring \code{latent(...)} blocks. Areal \code{icar}/\code{car}/
\code{bym2}/\code{car_proper} and continuous \code{gp}/\code{nngp}/\code{hsgp} go through
\code{\link[=tulpa_nested_laplace]{tulpa_nested_laplace()}}; SPDE is redirected to \code{\link[=fit_spde]{fit_spde()}}, which
integrates \verb{(range, sigma)} with its own CCD / grid design.
\item \code{mode = "laplace"} \strong{conditions} on a fixed hyperparameter via
\code{\link[=tulpa_laplace]{tulpa_laplace()}} (the cheap explicit fit).
\item \code{mode = "gibbs"} routes the areal \code{icar}/\code{bym2} cases through the binomial
Polya-Gamma samplers (Tier 1 exact); \code{mode = "auto"} picks this for a
binomial \code{icar}/\code{bym2} field.
}}

\item{temporal}{Optional temporal field spec (\code{\link[=temporal_rw1]{temporal_rw1()}},
\code{\link[=temporal_rw2]{temporal_rw2()}}, or \code{\link[=temporal_ar1]{temporal_ar1()}}), integrated by nested Laplace. A
plain field routes the single-block temporal kernel; a \code{group_var} panel
spec fits a separate walk per group sharing one hyperparameter; combined
with an areal \code{spatial} field it forms an additive space-time joint prior.}

\item{control}{Optional list of backend tuning arguments (e.g. \code{n_iter},
\code{warmup}, \code{epsilon} for \code{mala}; \code{n_draws} for \code{pathfinder}).}

\item{...}{Reserved for future use.}
}
\value{
A \code{tulpa_fit} object carrying the backend's output plus
\code{inference_mode}, \code{inference_tier}, \code{backend}, \code{selection_reason},
\code{formula}, and \code{family}.
}
\description{
Single entry point for fitting a Bayesian hierarchical model. \code{tulpa()}
parses the formula, builds the model matrices, selects an inference backend
through the tier/mode system (see \code{\link[=inference_mode_info]{inference_mode_info()}}), assembles the
arguments that backend needs, and dispatches.

The fit conditions on the random-effect standard deviations \code{sigma_re} (and,
for non-Gaussian dispersion, \code{phi}): both the Laplace (Tier 2) and the
sampler (Tier 1) paths target the posterior given these. Integrating over the
hyperparameters is the role of the nested-Laplace / EM layer.
}
\section{Coverage}{

\itemize{
\item \strong{No random effects} and \strong{random intercepts} (\code{(1 | g)}) are supported on
the design path (\code{mode = "laplace"}) and the sampler path (\code{mode = "mala"},
\code{"pathfinder"}, \code{"imh_laplace"}).
\item \strong{Random slopes} are supported on the Laplace (Tier 2) path: there is no
scalar \code{sigma_re} to condition on, so the RE covariance \code{Sigma} is integrated
rather than fixed. This covers correlated terms (\code{(1 + x | g)}, a full
\code{Sigma}), uncorrelated terms (\code{(1 + x || g)}, a diagonal \code{Sigma}), and
several terms together (\code{(1 + x | g) + (1 | h)}) -- each term becomes a
covariance block, and any accompanying \code{(1 | g)} term is integrated as a 1x1
block (nothing is silently conditioned at \code{sigma_re = 1}). \code{mode = "laplace"}
routes to the nested-Laplace \code{Sigma} integrator (\code{\link[=tulpa_re_cov_nested]{tulpa_re_cov_nested()}},
CCD design + PC/LKJ prior); \code{control$re_cov = "gibbs"} switches to the exact
Metropolis-within-Gibbs debias (\code{\link[=tulpa_re_cov_gibbs]{tulpa_re_cov_gibbs()}}). Both also run on the
sampler path (\code{mode = "mala"} / \code{"pathfinder"}).
\item \code{mode = "gibbs"} (Polya-Gamma) fits a single random-intercept model for
\code{family = "binomial"} or \code{"neg_binomial_2"}, and \strong{samples} the RE sd
rather than conditioning on \code{sigma_re}; tune it via \code{control$prior_sigma_scale}
and a mean-zero \code{beta_prior}.
\item \strong{Latent prior blocks} (\code{latent(tgmrf(...))}) route to the nested-Laplace
path (Tier 2), which integrates over the block hyperparameters. \code{mode = "auto"} and \code{"structured"} select it automatically when latent blocks are
present; \code{mode = "nested_laplace"} forces it. At most one random-intercept
\code{(1 | g)} term may accompany the blocks (model richer grouping as an \code{iid}
block). Joint multi-arm nested models cannot be expressed by a
single-response formula -- call \code{\link[=tulpa_nested_laplace_joint]{tulpa_nested_laplace_joint()}} directly.
\item Selecting a backend whose kernel is C-ABI-only (e.g. \code{ess}, \code{sghmc}, \code{smc})
errors loudly -- those are reachable from model packages, not from R yet.
}
}

\seealso{
\code{\link[=inference_mode_info]{inference_mode_info()}}, \code{\link[=tulpa_laplace]{tulpa_laplace()}}, \code{\link[=mala]{mala()}}, \code{\link[=pathfinder]{pathfinder()}}
}
