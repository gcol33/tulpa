# Log-likelihood at the posterior mean

Log-likelihood at the posterior mean

## Usage

``` r
# S3 method for class 'tulpa_fit'
logLik(object, ...)
```

## Arguments

- object:

  A `tulpa_fit` object.

- ...:

  Ignored.

## Value

A `logLik` object.
