# Response-scale mean of y given eta for a family (trial-scaled where relevant).

Response-scale mean of y given eta for a family (trial-scaled where
relevant).

## Usage

``` r
family_response_mean(eta, family, n_trials = NULL, phi = 1)
```
