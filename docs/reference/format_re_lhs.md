# Render a slope spec as a display string

Used by the print method. Derives the textual form on demand from the
stored language objects so we never store deparsed slope text.

## Usage

``` r
format_re_lhs(re)
```
