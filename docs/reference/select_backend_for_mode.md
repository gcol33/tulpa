# Select best backend within a mode

Select best backend within a mode

## Usage

``` r
select_backend_for_mode(
  mode,
  family,
  n_obs,
  has_spatial,
  has_temporal,
  has_latent = FALSE,
  spatial_type = NULL
)
```
