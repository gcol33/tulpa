# One response draw per element of `eta` (posterior predictive), elementwise.

One response draw per element of `eta` (posterior predictive),
elementwise.

## Usage

``` r
family_sample(eta, family, n_trials = NULL, phi = 1, phi2 = NULL)
```
