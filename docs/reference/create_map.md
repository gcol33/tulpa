# Create the ggplot2 map

Create the ggplot2 map

## Usage

``` r
create_map(
  plot_data,
  obs_coords,
  title,
  palette,
  point_color,
  point_size,
  na_color,
  crs,
  legend_title,
  ...
)
```
